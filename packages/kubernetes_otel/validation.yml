errors:
  exclude_checks:
    # To by-pass the "kubernetes_otel-cluster-overview.json" is invalid: expected filter in dashboard: no filter found and at least one panel does not have a filter (SVR00002)
    # https://github.com/elastic/elastic-package/blob/2707d8efeb7e431bc5c20b7a0df308369f2ff4cf/docs/howto/filter_errors.md?plain=1#L28-L41
    - SVR00002
