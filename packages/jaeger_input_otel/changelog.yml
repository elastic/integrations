# newer versions go on top
- version: "0.1.0"
  changes:
    - description: Jaeger OTel input with per-protocol toggles, gRPC/Thrift HTTP/UDP (compact/binary), shared UDP tuning, and TLS for gRPC and Thrift HTTP
      type: enhancement
      link: https://github.com/elastic/integrations/pull/17231
