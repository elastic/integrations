service: jaeger_trace_sender
service_notify_signal: SIGHUP
vars:
  grpc_endpoint: "0.0.0.0:14250"
  # enable thrift HTTP to verify both variants are working
  thrift_http_enabled: true
  thrift_http_endpoint: "0.0.0.0:14268"
assert:
  min_count: 10
