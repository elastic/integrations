variants:
  grpc:
    JAEGER_PROTOCOL: grpc
  http:
    JAEGER_PROTOCOL: http
  thrift_compact:
    JAEGER_PROTOCOL: thrift_compact
    JAEGER_UDP_TARGET: elastic-agent:6831
  thrift_binary:
    JAEGER_PROTOCOL: thrift_binary
    JAEGER_UDP_TARGET: elastic-agent:6832
default: grpc
