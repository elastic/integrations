receivers:
  jaeger:
    protocols:
      grpc:
        endpoint: {{grpc_endpoint}}
processors:
  resourcedetection/system:
    detectors: ["system"]
service:
  pipelines:
    traces:
      receivers: [jaeger]
      processors: [resourcedetection/system]
