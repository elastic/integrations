input: httpjson
service: httpjson
data_stream:
  vars:
    data_stream:
      dataset: httpjson.generic
    username: test
    password: test
    request_url: http://{{Hostname}}:{{Port}}/testbasicauth/api
    enable_request_tracer: true
