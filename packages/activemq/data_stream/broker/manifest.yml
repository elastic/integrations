type: metrics
title: ActiveMQ broker metrics
release: beta
streams:
  - input: activemq/metrics
    vars:
      - name: period
        type: text
        title: Period
        multi: false
        required: true
        show_user: true
        default: 10s
    title: ActiveMQ broker metrics
    description: Collect ActiveMQ broker metrics
