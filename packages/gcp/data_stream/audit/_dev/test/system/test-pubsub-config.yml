service: gcppubsub-audit
service_notify_signal: SIGHUP
input: gcp-pubsub
vars:
  alternative_host: "{{Hostname}}:{{Port}}"
  credentials_json: "{\"is\":\"fake\"}"
  project_id: test
data_stream:
  vars:
    subscription_name: subscription
    topic: topic
