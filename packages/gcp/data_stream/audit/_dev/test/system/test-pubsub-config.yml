input: gcp-pubsub
vars:
  alternative_host: "{{Hostname}}:{{Port}}"
  credentials_json: "{\"is\":\"fake\"}"
  project_id: system-tests
data_stream:
  vars:
    subscription_name: audit-sub
    topic: audit
