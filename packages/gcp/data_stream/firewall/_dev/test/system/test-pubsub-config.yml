service: gcppubsub-emulator
input: gcp-pubsub
vars:
  alternative_host: "{{Hostname}}:{{Port}}"
  credentials_json: >
    {"foo": "bar"}

  project_id: firewall
data_stream:
  vars:
    subscription_name: subscription
    topic: topic
