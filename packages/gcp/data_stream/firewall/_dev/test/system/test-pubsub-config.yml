service: gcppubsub-emulator
input: gcp-pubsub
vars:
  credentials_json: >
    {"foo": "bar"}

  project_id: firewall
data_stream:
  vars:
    alternative_host: "{{Hostname}}:{{Port}}"
    subscription_name: subscription
    topic: topic
