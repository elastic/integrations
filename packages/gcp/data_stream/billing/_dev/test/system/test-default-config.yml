wait_for_data_timeout: 20m # AWS CloudWatch may delay metrics delivery for more than 10 minutes.
vars:
  project_id: "{{GCP_PROJECT_ID}}"
  # credentials_file: ""
data_stream:
  vars:
    # period: 24h
    dataset_id: "integration_testing_{{TEST_RUN_ID}}"
    table_pattern: "billing_export_test_data_{{TEST_RUN_ID}}"
    # cost_type: "regular"
