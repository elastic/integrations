input: cel
service: xm_cyber
vars:
  url: http://{{Hostname}}:{{Port}}
  api_key: mock-api-key
data_stream:
  vars:
    interval: 24h
    initial_interval: "30"
    preserve_original_event: true
assert:
  hit_count: 6
