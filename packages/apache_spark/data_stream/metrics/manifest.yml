title: Apache Spark metrics
type: metrics
release: beta
streams:
  - input: jolokia/metrics
    title: Apache Spark metrics
    description: Collect Apache Spark metrics using Jolokia agent.
    vars:
      - name: period
        type: text
        title: Period
        multi: false
        required: true
        show_user: true
        default: 60s
    template_path: "stream.yml.hbs"
