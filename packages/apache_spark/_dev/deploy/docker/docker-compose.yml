version: '2'
services:
  apache_spark:
    hostname: apache-spark-main
    build:
      context: ./Dockerfiles
      dockerfile: Dockerfile-main
    ports:
      - 7077
      - 7777
      - 8088
    volumes:
      - ./jolokia-configs/main:/spark/conf/:rw
