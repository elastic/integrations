errors:
  exclude_checks:
    # Maps panels must be saved as separate kibana/map/*.json files (they cannot
    # be embedded by-value in dashboard JSON).
    - SVR00004 # References in dashboards (false positive for Maps).
