config_version: 2
interval: {{interval}}

resource.url: {{url}}
resource.timeout: {{http_client_timeout}}
# Disable HTTP keep-alive so each poll opens a fresh connection.
# Without this, the server-side connection may be closed mid-read during
# the large NDJSON download, causing "read on closed response body".
resource.keep_alive.disable: true
resource.retry:
  max_attempts: 3
  wait_min: 30s
  wait_max: 5m
resource.tracer:
  enabled: {{enable_request_tracer}}
  # Include the data stream namespace so multiple Ticura integration instances
  # (e.g., one per feed configuration) don't write their tracer logs into the
  # same file. The trailing `*` is the standard CEL rotation wildcard.
  filename: "../../logs/cel/http-request-trace-{{data_stream.namespace}}-*.ndjson"
  maxbackups: 5

state:
  api_token: {{api_token}}

redact:
  fields:
    - api_token
  delete: true  # Remove api_token entirely from event documents (not just mask).

program: |
  state.with(
    request("GET", state.url).with({
      "Header": {
        "x-api-key": [state.api_token],
      }
    }).do_request().as(resp, resp.StatusCode == 200 ?
      {
        "events": string(resp.Body).split("\n")
          .filter(l, size(l) > 0)
          .map(l, {"message": l.trim_space()}),
        "want_more": false,
      }
    :
      {
        "events": [{
          "error": {
            "code": string(resp.StatusCode),
            "id": string(resp.Status),
            "message": "GET " + state.url + ": " + (
              size(resp.Body) != 0 ?
                string(resp.Body)
              :
                string(resp.Status) + " (" + string(resp.StatusCode) + ")"
            ),
          },
        }],
        "want_more": false,
      }
    )
  )

tags:
{{#if preserve_original_event}}
  - preserve_original_event
{{/if}}
{{#each tags as |tag|}}
  - {{tag}}
{{/each}}
{{#contains "forwarded" tags}}
publisher_pipeline.disable_host: true
{{/contains}}
{{#if processors}}
processors:
{{processors}}
{{/if}}
