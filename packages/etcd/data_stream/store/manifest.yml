title: etcd store metrics
type: metrics
streams:
  - input: etcd/metrics
    vars:
      - name: period
        type: text
        title: Period
        multi: false
        required: true
        show_user: true
        default: 10s
    title: etcd store metrics
    description: Collect etcd store metrics
