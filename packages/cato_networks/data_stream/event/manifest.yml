title: Cato Networks Event
type: logs
streams:
  - input: cel
    title: Event
    enabled: true
    description: Collect event logs from Cato Networks via API.
    template_path: cel.yml.hbs
    vars:
      - name: interval
        type: text
        title: Interval
        description: Duration between requests to the Cato Networks API. Supported units for this parameter are h/m/s.
        default: 5m
        multi: false
        required: true
        show_user: true
      - name: account_ids
        type: text
        title: Account IDs
        description: Account IDs of Cato Networks to collect data from.
        multi: true
        required: true
        show_user: true
      - name: resource_rate_limit_limit
        type: text
        title: Resource Rate Limit
        description: Maximum request rate per second. The Cato Networks API has a limit of 100 requests per minute (~1.67 req/sec).
        default: '1.6'
        show_user: false
        multi: false
        required: true
      - name: resource_rate_limit_burst
        type: integer
        title: Resource Rate Limit Burst
        default: 1
        description: In requests. The maximum number of requests that can be immediately made following an idle period.
        show_user: false
        multi: false
        required: true
      - name: http_client_timeout
        type: text
        title: HTTP Client Timeout
        description: Duration before declaring that the HTTP client connection has timed out. Valid time units are ns, us, ms, s, m, h.
        multi: false
        required: true
        show_user: false
        default: 30s
      - name: enable_request_tracer
        type: bool
        title: Enable request tracing
        default: false
        multi: false
        required: false
        show_user: false
        description: >-
          The request tracer logs requests and responses to the agent's local file-system for debugging configurations. Enabling this request tracing compromises security and should only be used for debugging. Disabling the request tracer will delete any stored traces. See [documentation](https://www.elastic.co/guide/en/beats/filebeat/current/filebeat-input-cel.html#_resource_tracer_enable) for details.
      - name: tags
        type: text
        title: Tags
        description: Tags for the data-stream.
        multi: true
        required: true
        show_user: false
        default:
          - forwarded
          - cato_networks-event
      - name: preserve_original_event
        required: false
        show_user: true
        title: Preserve original event
        description: Preserves a raw copy of the original event, added to the field `event.original`.
        type: bool
        multi: false
        default: false
      - name: processors
        type: yaml
        title: Processors
        multi: false
        required: false
        show_user: false
        description: >-
          Processors are used to reduce the number of fields in the exported event or to enhance the event with metadata. This executes in the agent before the logs are parsed. See [Processors](https://www.elastic.co/guide/en/beats/filebeat/current/filtering-and-enhancing-data.html) for details.
