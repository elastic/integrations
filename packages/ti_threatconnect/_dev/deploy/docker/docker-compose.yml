version: '2.3'
services:
  threatconnect-indicator:
    image: docker.elastic.co/observability/stream:v0.23.0
    hostname: threatconnect-indicator
    ports:
      - 8090
    volumes:
      - ./files:/files:ro
    command:
      - http-server
      - --addr=:8090
      - --config=/files/config-indicator.yml
