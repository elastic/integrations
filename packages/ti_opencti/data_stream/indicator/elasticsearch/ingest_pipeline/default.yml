---
description: Pipeline for processing indicators
processors:

  ###############################################
  # Send errors from CEL to the failure handler #
  ###############################################

  - terminate:
      tag: data_collection_error
      if: ctx.error?.message != null
      description: error message set and no data to process.


  ####################
  # Event ECS fields #
  ####################
  - set:
      tag: set_ecs_version_0f7334bb
      field: ecs.version
      value: '8.11.0'
  - set:
      tag: set_event_kind_a131107f
      field: event.kind
      value: enrichment
  - set:
      tag: set_event_category_cceb7c22
      field: event.category
      value: ['threat']
  - set:
      tag: set_event_type_97bb78b9
      field: event.type
      value: ['indicator']
  - rename:
      tag: rename_created_to_event_created_0f9cf48d
      field: created
      target_field: event.created
  - rename:
      tag: rename_id_to_event_id_4f49abf1
      field: id
      target_field: event.id


  ######################
  # Threat feed fields #
  ######################
  - set:
      tag: set_threat_feed_dashboard_id_9340fe1d
      field: threat.feed.dashboard_id
      value: ti_opencti-83b2bef0-591c-11ee-ba5f-49a63bb985cd
  - set:
      tag: set_threat_feed_description_8bd01795
      field: threat.feed.description
      value: Indicator data from OpenCTI
  - set:
      tag: set_threat_feed_name_60b56abf
      field: threat.feed.name
      value: OpenCTI
  - set:
      tag: set_threat_feed_reference_99cb7716
      field: threat.feed.reference
      value: https://docs.opencti.io/latest/usage/overview/


  ############################
  # Generic indicator fields #
  ############################
  - set:
      tag: set_threat_indicator_reference_1f855278
      field: threat.indicator.reference
      value: "{{{_conf.url}}}/dashboard/observations/indicators/{{{event.id}}}"
  - remove:
      tag: remove__conf_8e93c735
      field: _conf
      ignore_missing: true
  - rename:
      tag: rename_standard_id_to_opencti_indicator_standard_id_5c8532d0
      field: standard_id
      target_field: opencti.indicator.standard_id
  - rename:
      tag: rename_is_inferred_to_opencti_indicator_is_inferred_edd5adba
      field: is_inferred
      target_field: opencti.indicator.is_inferred
  - rename:
      tag: rename_revoked_to_opencti_indicator_revoked_1df3098a
      field: revoked
      target_field: opencti.indicator.revoked
  - set:
      tag: set_threat_indicator_confidence_a674fe52
      field: threat.indicator.confidence
      value: 'Not Specified'
  - script:
      tag: script_2fa9aea9
      # based on https://stix2.readthedocs.io/en/latest/api/confidence/stix2.confidence.scales.html#stix2.confidence.scales.value_to_none_low_medium_high
      lang: painless
      source: |
        if (ctx.confidence == null) {
          ctx.threat.indicator.confidence = 'Not Specified';
        } else if (ctx.confidence == 0) {
          ctx.threat.indicator.confidence = 'None';
        } else if (1 <= ctx.confidence && ctx.confidence <= 29) {
          ctx.threat.indicator.confidence = 'Low';
        } else if (30 <= ctx.confidence && ctx.confidence <= 69) {
          ctx.threat.indicator.confidence = 'Medium';
        } else if (70 <= ctx.confidence && ctx.confidence <= 100) {
          ctx.threat.indicator.confidence = 'High';
        } else {
          ctx.threat.indicator.confidence = 'Not Specified';
        }
  - remove:
      tag: remove_confidence_05d7236d
      field: confidence
  - rename:
      tag: rename_lang_to_opencti_indicator_lang_a557a930
      field: lang
      target_field: opencti.indicator.lang
  - rename:
      tag: rename_modified_to_threat_indicator_modified_at_890a6f22
      field: modified
      target_field: threat.indicator.modified_at
  - rename:
      tag: rename_updated_at_to_opencti_indicator_updated_at_fe4f5733
      field: updated_at
      target_field: opencti.indicator.updated_at
      ignore_missing: true
  - rename:
      tag: rename_pattern_type_to_opencti_indicator_pattern_type_ca5b7dc0
      field: pattern_type
      target_field: opencti.indicator.pattern_type
  - rename:
      tag: rename_pattern_version_to_opencti_indicator_pattern_version_fa977761
      field: pattern_version
      target_field: opencti.indicator.pattern_version
      if: ctx.pattern_version != null
      ignore_missing: true
  - remove:
      tag: remove_pattern_version_7fadaa07
      field: pattern_version
      ignore_missing: true
  - rename:
      tag: rename_pattern_to_opencti_indicator_pattern_96a05bd2
      field: pattern
      target_field: opencti.indicator.pattern
  - rename:
      tag: rename_name_to_threat_indicator_name_f77818ba
      field: name
      target_field: threat.indicator.name
  - rename:
      tag: rename_description_to_threat_indicator_description_fbb019ae
      field: description
      target_field: threat.indicator.description
  - remove:
      tag: remove_threat_indicator_description_bc5b2134
      field: threat.indicator.description
      if: ctx.threat.indicator.description == null || ctx.threat.indicator.description.isEmpty()
  - rename:
      tag: rename_valid_from_to_opencti_indicator_valid_from_c8653800
      field: valid_from
      target_field: opencti.indicator.valid_from
  - rename:
      tag: rename_valid_until_to_opencti_indicator_valid_until_74ae5a8c
      field: valid_until
      target_field: opencti.indicator.valid_until
  - script:
      tag: script_8bfd3177
      lang: painless
      source: |
        if (ctx.opencti.indicator.revoked == true &&
            ctx.threat.indicator.modified_at.compareTo(ctx.opencti.indicator.valid_until) < 0) {
            ctx.opencti.indicator.invalid_or_revoked_from = ctx.threat.indicator.modified_at;
        } else {
            // valid_until always has a value, will be epoch + 10^14 ms if no other value
            ctx.opencti.indicator.invalid_or_revoked_from = ctx.opencti.indicator.valid_until;
        }
  - rename:
      tag: rename_x_opencti_score_to_opencti_indicator_score_70107380
      field: x_opencti_score
      target_field: opencti.indicator.score
  - rename:
      tag: rename_x_opencti_detection_to_opencti_indicator_detection_728ab91a
      field: x_opencti_detection
      target_field: opencti.indicator.detection
  - rename:
      tag: rename_x_opencti_main_observable_type_to_threat_indicator_type_8d88887a
      field: x_opencti_main_observable_type
      target_field: threat.indicator.type
  - script:
      tag: script_30110556
      description: Reformat indicator type
      lang: painless
      source: |
        String type = ctx.threat.indicator.type;
        type = type.toLowerCase();
        type = type.replace('stixfile', 'file');
        ctx.threat.indicator.type = type;
  - rename:
      tag: rename_createdBy_name_to_threat_indicator_provider_48fa0454
      field: createdBy.name
      target_field: threat.indicator.provider
      ignore_missing: true
  - rename:
      tag: rename_createdBy_identity_class_to_opencti_indicator_creator_identity_class_c28b0503
      field: createdBy.identity_class
      target_field: opencti.indicator.creator_identity_class
      ignore_missing: true
  - remove:
      tag: remove_createdBy_ba8401db
      field: createdBy
      ignore_missing: true

  - gsub:
      tag: gsub_objectMarking_0_definition_to_threat_indicator_marking_tlp_902801f9
      if: |
        ctx.objectMarking != null &&
        !ctx.objectMarking.isEmpty() &&
        ctx.objectMarking[0].definition_type == "TLP"
      field: objectMarking.0.definition
      pattern: '^TLP:'
      replacement: ''
      target_field: threat.indicator.marking.tlp
  - remove:
      tag: remove_objectMarking_335e3bbd
      field: objectMarking

  - foreach:
      tag: foreach_objectLabel_a5be1ce4
      field: objectLabel
      ignore_missing: true
      processor:
        append:
          tag: append_tags
          field: tags
          value: "{{{_ingest._value.value}}}"
          allow_duplicates: false
  - remove:
      tag: remove_objectLabel_fabc71d6
      field: objectLabel

  - foreach:
      tag: foreach_killChainPhases_180ffef6
      field: killChainPhases
      ignore_missing: true
      processor:
        append:
          tag: append_opencti_indicator_kill_chain_phase
          field: opencti.indicator.kill_chain_phase
          value: "[{{{_ingest._value.kill_chain_name}}}] {{{_ingest._value.phase_name}}}"
  - remove:
      tag: remove_killChainPhases_a8d0f04c
      field: killChainPhases

  - script:
      tag: script_55962473
      description: Move external reference nodes to their target location, without nulls
      lang: painless
      source: |
        ArrayList edges = ctx.externalReferences.edges;
        if (edges == null) {
          return;
        }
        for (int i = 0; i < edges.length; i++) {
          if (!ctx.opencti?.indicator?.containsKey('external_reference') == true) {
            if (!ctx.containsKey('opencti')) {
              ctx.opencti = [:];
            }
            if (!ctx.opencti.containsKey('indicator')) {
              ctx.opencti.indicator = [:];
            }
            if (!ctx.opencti.indicator.containsKey('external_reference')) {
              ctx.opencti.indicator.external_reference = [];
            }
          }
          def newNode = [:];
          for (def key : edges[i]['node'].keySet()) {
            if (edges[i]['node'][key] != null) {
              newNode[key] = edges[i]['node'][key];
            }
          }
          ctx.opencti.indicator.external_reference.add(newNode);
        }
  - remove:
      tag: remove_externalReferences_8bdc8f12
      field: externalReferences


  #####################
  # Observable fields #
  #####################
  - rename:
      tag: rename_observables_pageInfo_globalCount_to_opencti_indicator_observables_count_f9217ef4
      field: observables.pageInfo.globalCount
      target_field: opencti.indicator.observables_count
      ignore_missing: true

  - foreach:
      tag: foreach_observables_edges_3db59843
      field: observables.edges
      ignore_missing: true
      processor:
        remove:
          tag: remove_ingest_value_node_value
          field: _ingest._value.node.value
          ignore_missing: true
  - foreach:
      tag: foreach_observables_edges_83957232
      field: observables.edges
      ignore_missing: true
      processor:
        rename:
          tag: rename_ingest_value_node_observable_value_to_ingest_value_node_value
          field: _ingest._value.node.observable_value
          target_field: _ingest._value.node.value

  - foreach:
      tag: foreach_observables_edges_be319b43
      field: observables.edges
      ignore_missing: true
      processor:
        rename:
          tag: rename_ingest_value_node_x_opencti_description_to_ingest_value_node_description
          field: _ingest._value.node.x_opencti_description
          target_field: _ingest._value.node.description
          ignore_missing: true

  - foreach:
      tag: foreach_observables_edges_e71fe9bb
      field: observables.edges
      ignore_missing: true
      processor:
        rename:
          tag: rename_ingest_value_node_x_opencti_additional_names_to_ingest_value_node_additional_names
          field: _ingest._value.node.x_opencti_additional_names
          target_field: _ingest._value.node.additional_names
          ignore_missing: true

  - foreach:
      tag: foreach_observables_edges_9e3da177
      field: observables.edges
      ignore_missing: true
      processor:
        rename:
          tag: rename_ingest_value_node_obsContent_to_ingest_value_node_content
          field: _ingest._value.node.obsContent
          target_field: _ingest._value.node.content
          ignore_missing: true

  - foreach:
      tag: foreach_observables_edges_8c0dd402
      field: observables.edges
      ignore_missing: true
      processor:
        foreach:
          tag: foreach_ingest_value_node_serviceDlls_edges
          field: _ingest._value.node.serviceDlls.edges
          ignore_missing: true
          processor:
            rename:
              tag: rename_ingest_value_node_x_opencti_additional_names_to_ingest_node_value_additional_names
              field: _ingest._value.node.x_opencti_additional_names
              target_field: _ingest.node._value.additional_names

  - foreach:
      tag: foreach_observables_edges_07149ffc
      field: observables.edges
      ignore_missing: true
      processor:
        foreach:
          tag: foreach_ingest_value_node_serviceDlls_edges_84c967f1
          field: _ingest._value.node.serviceDlls.edges
          ignore_missing: true
          processor:
            rename:
              tag: rename_ingest_value_node_obsContent_to_ingest_value_node_content_cc70dc3f
              field: _ingest._value.node.obsContent
              target_field: _ingest._value.node.content

  - foreach:
      tag: foreach_observables_edges_07635390
      field: observables.edges
      ignore_missing: true
      processor:
        foreach:
          tag: foreach_ingest_value_node_serviceDlls_edges_1098f7d8
          field: _ingest._value.node.serviceDlls.edges
          ignore_missing: true
          processor:
            rename:
              tag: rename_ingest_value_node_to_ingest_value
              field: _ingest._value.node
              target_field: _ingest._value

  - foreach:
      tag: foreach_observables_edges_c8e017ba
      field: observables.edges
      ignore_missing: true
      processor:
        rename:
          tag: rename_ingest_value_node_serviceDlls_edges_to_ingest_value_node_service_dlls
          field: _ingest._value.node.serviceDlls.edges
          target_field: _ingest._value.node.service_dlls
          ignore_missing: true

  - script:
      tag: script_14cfaa7a
      description: Merge array of objects into a single object for startup_info
      lang: painless
      source: |
        if (ctx.observables?.edges instanceof List) {
          for (def edge : ctx.observables.edges) {
            if (edge.node?.startup_info instanceof List) {
              def result = [:];
              for (def kv : edge.node.startup_info) {
                result[kv.key] = kv.value;
              }
              edge.node.startup_info = result;
            }
          }
        }

  - script:
      tag: script_79f01063
      description: Restructure hashes to ECS format
      lang: painless
      source: |
        Map hashesToECS(ArrayList hashes) {
          Map hash = [:];
          for (int i = 0; i < hashes.length; i++) {
            String algorithm = hashes[i].algorithm;
            algorithm = algorithm.toLowerCase();
            algorithm = algorithm.replace('sha-', 'sha');
            algorithm = algorithm.replace('sha3-', 'sha3_');
            hash[algorithm] = hashes[i].hash;
          }
          return hash;
        }
        ArrayList observables = ctx.observables.edges;
        for (int i = 0; i < observables.length; i++) {
          Map observable = observables[i].node;
          if (observable.containsKey('hashes')) {
            def ecsHash = hashesToECS(observable.hashes);
            if (ecsHash.size() > 0) {
              observable.hash = ecsHash;
            }
            observable.remove('hashes');
          }
          if (observable.containsKey('service_dlls')) {
            for (int ii = 0; ii < observable.service_dlls.length; ii++) {
              Map serviceDll = observable.service_dlls[ii];
              if (serviceDll.containsKey('hashes')) {
                def ecsHash = hashesToECS(serviceDll.hashes);
                if (ecsHash.size() > 0) {
                  serviceDll.hash = ecsHash;
                }
                serviceDll.remove('hashes');
              }
            }
          }
        }

  - script:
      tag: script_95fdd14d
      description: Move observable nodes to their target location
      lang: painless
      source: |
        if (!ctx.containsKey('opencti')) {
          ctx.opencti = [:];
        }
        if (!ctx.opencti.containsKey('observable')) {
          ctx.opencti.observable = [:];
        }
        ArrayList observables = ctx.observables.edges;
        for (int i = 0; i < observables.length; i++) {
          String entity = observables[i]['node']['entity_type'];
          entity = entity.replace('StixFile', 'file');
          entity = entity.toLowerCase();
          entity = entity.replace('-', '_');
          if (!ctx.opencti.observable.containsKey(entity)) {
            ctx.opencti.observable[entity] = [];
          }
          ctx.opencti.observable[entity].add(observables[i]['node']);
        }
  - remove:
      tag: remove_observables_ec3e1a2e
      field: observables
      ignore_missing: true

  - script:
      tag: script_0c64bfed
      description: Remove null values and empty fieldsets from observables
      lang: painless
      source: |
        def dropNulls(Object obj) {
          if (obj instanceof Map) {
            def map = (Map) obj;
            Map result = [:];
            for (def key : map.keySet()) {
              if (map[key] instanceof Map || map[key] instanceof List) {
                result[key] = dropNulls(map[key]);
              } else if (map[key] != null) {
                result[key] = map[key];
              }
            }
            return result;
          } else if (obj instanceof List) {
            def list = (List) obj;
            List result = [];
            for (def item : list) {
              if (item instanceof Map || item instanceof List) {
                result.add(dropNulls(item));
              } else if (item != null) {
                result.add(item);
              }
            }
            return result;
          } else {
            return obj;
          }
        }
        if (ctx.opencti?.containsKey('observable') == true) {
          ctx.opencti.observable = dropNulls(ctx.opencti.observable);
          for (def key : ctx.opencti.observable.keySet()) {
            if (ctx.opencti.observable[key].size() == 0) {
              ctx.opencti.observable.remove(key);
            }
          }
          if (ctx.opencti.observable.size() == 0) {
            ctx.opencti.remove('observable');
          }
        }


  #########################################
  # ECS indicator fields from observables #
  #########################################
  - foreach:
      tag: foreach_opencti_observable_email_addr_ecaeab6a
      field: opencti.observable.email_addr
      ignore_missing: true
      processor:
        append:
          tag: append_threat_indicator_email_address
          field: threat.indicator.email.address
          value: "{{{_ingest._value.value}}}"
          allow_duplicates: false

  - foreach:
      tag: foreach_opencti_observable_ipv4_addr_3987f58a
      field: opencti.observable.ipv4_addr
      ignore_missing: true
      processor:
        append:
          tag: append_threat_indicator_ip
          field: threat.indicator.ip
          value: "{{{_ingest._value.value}}}"
          allow_duplicates: false

  - foreach:
      tag: foreach_opencti_observable_ipv6_addr_c949b11c
      field: opencti.observable.ipv6_addr
      ignore_missing: true
      processor:
        append:
          tag: append_threat_indicator_ip_fcc2116a
          field: threat.indicator.ip
          value: "{{{_ingest._value.value}}}"
          allow_duplicates: false

  # For ECS, remove any subnet mask from IP values arriving in CIDR notation
  - foreach:
      tag: foreach_threat_indicator_ip_d127dfdc
      field: threat.indicator.ip
      ignore_missing: true
      processor:
        gsub:
          tag: gsub_ingest_value
          field: _ingest._value
          pattern: '/\d+$'
          replacement: ""

  - foreach:
      tag: foreach_opencti_observable_network_traffic_b91bd1a0
      field: opencti.observable.network_traffic
      ignore_missing: true
      processor:
        append:
          tag: append_threat_indicator_port
          field: threat.indicator.port
          value: "{{{_ingest._value.src_port}}}"
          allow_duplicates: false
  - foreach:
      tag: foreach_opencti_observable_network_traffic_e4916ba3
      field: opencti.observable.network_traffic
      ignore_missing: true
      processor:
        append:
          tag: append_threat_indicator_port_830fbf53
          field: threat.indicator.port
          value: "{{{_ingest._value.dst_port}}}"
          allow_duplicates: false

  - foreach:
      tag: foreach_opencti_observable_autonomous_system_9eddba75
      field: opencti.observable.autonomous_system
      ignore_missing: true
      processor:
        pipeline:
          tag: pipeline
          name: '{{ IngestPipeline "ecs_from_autonomous_system" }}'

  - foreach:
      tag: foreach_opencti_observable_windows_registry_key_72d20dbf
      field: opencti.observable.windows_registry_key
      ignore_missing: true
      processor:
        pipeline:
          tag: pipeline_057f9744
          name: '{{ IngestPipeline "ecs_from_windows_registry_key" }}'

  - foreach:
      tag: foreach_opencti_observable_windows_registry_value_type_3e52ad11
      field: opencti.observable.windows_registry_value_type
      ignore_missing: true
      processor:
        pipeline:
          tag: pipeline_1f8a5dd2
          name: '{{ IngestPipeline "ecs_from_windows_registry_value_type" }}'

  - foreach:
      tag: foreach_opencti_observable_x509_certificate_6435f22f
      field: opencti.observable.x509_certificate
      ignore_missing: true
      processor:
        pipeline:
          tag: pipeline_ee366c4b
          name: '{{ IngestPipeline "ecs_from_x509_certificate" }}'

  - foreach:
      tag: foreach_opencti_observable_url_dc833672
      field: opencti.observable.url
      ignore_missing: true
      processor:
        append:
          tag: append_tmp_found_urls
          field: _tmp_found_urls
          value: "{{{_ingest._value.value}}}"
          allow_duplicates: false
  - foreach:
      tag: foreach_opencti_observable_artifact_6aaa5375
      field: opencti.observable.artifact
      ignore_missing: true
      processor:
        append:
          tag: append_tmp_found_urls_a9938349
          field: _tmp_found_urls
          value: "{{{_ingest._value.url}}}"
          allow_duplicates: false
  - foreach:
      tag: foreach_opencti_observable_file_22c17167
      field: opencti.observable.file
      ignore_missing: true
      processor:
        append:
          tag: append_tmp_found_urls_c9ce3ff9
          field: _tmp_found_urls
          value: "{{{_ingest._value.content?.url}}}"
          allow_duplicates: false
  - foreach:
      tag: foreach_opencti_observable_media_content_90bd22cd
      field: opencti.observable.media_content
      ignore_missing: true
      processor:
        append:
          tag: append_tmp_found_urls_a23364ec
          field: _tmp_found_urls
          value: "{{{_ingest._value.url}}}"
          allow_duplicates: false
  - foreach:
      tag: foreach_opencti_observable_process_37cc767e
      field: opencti.observable.process
      ignore_missing: true
      processor:
        foreach:
          tag: foreach_ingest_value_service_dll
          field: _ingest._value.service_dll
          ignore_missing: true
          processor:
            append:
              tag: append_tmp_found_urls_2f11da43
              field: _tmp_found_urls
              value: "{{{_ingest._value.content?.url}}}"
              allow_duplicates: false
  - script:
      tag: script_d8c725a6
      description: Remove null or empty items
      lang: painless
      source: |
        if (ctx._tmp_found_urls != null) {
          def result = [];
          for (item in ctx._tmp_found_urls) {
              if (item != null && item != '') {
                  result.add(item);
              }
          }
          ctx._tmp_found_urls = result;
        }
  - foreach:
      tag: foreach__tmp_found_urls_5bcbbb30
      field: _tmp_found_urls
      ignore_missing: true
      processor:
        pipeline:
          tag: pipeline_e1c73fd6
          name: '{{ IngestPipeline "ecs_from_url_field" }}'
  - remove:
      tag: remove__tmp_found_urls_420f0d34
      field: _tmp_found_urls
      ignore_missing: true

  - foreach:
      tag: foreach_opencti_observable_domain_name_64bd73b7
      field: opencti.observable.domain_name
      ignore_missing: true
      processor:
        append:
          tag: append_tmp_found_domain_names_and_hostnames
          field: _tmp_found_domain_names_and_hostnames
          value: "{{{_ingest._value.value}}}"
          allow_duplicates: false
  - foreach:
      tag: foreach_opencti_observable_hostname_56f0eb3c
      field: opencti.observable.hostname
      ignore_missing: true
      processor:
        append:
          tag: append_tmp_found_domain_names_and_hostnames_23a8b6c6
          field: _tmp_found_domain_names_and_hostnames
          value: "{{{_ingest._value.value}}}"
          allow_duplicates: false
  - foreach:
      tag: foreach__tmp_found_domain_names_and_hostnames_558c3efe
      field: _tmp_found_domain_names_and_hostnames
      ignore_missing: true
      processor:
        pipeline:
          tag: pipeline_d10d9028
          name: '{{ IngestPipeline "ecs_from_domain_name_or_hostname" }}'
  - remove:
      tag: remove__tmp_found_domain_names_and_hostnames_ebaf9c4a
      field: _tmp_found_domain_names_and_hostnames
      ignore_missing: true

  - foreach:
      tag: foreach_opencti_observable_file_8011320f
      field: opencti.observable.file
      ignore_missing: true
      processor:
        pipeline:
          tag: pipeline_34633f97
          name: '{{ IngestPipeline "ecs_from_file" }}'

  - foreach:
      tag: foreach_opencti_observable_process_79a8f024
      field: opencti.observable.process
      ignore_missing: true
      processor:
        foreach:
          tag: foreach_ingest_value_service_dll_4e86c638
          field: _ingest._value.service_dll
          ignore_missing: true
          processor:
            pipeline:
              tag: pipeline_1e36a118
              name: '{{ IngestPipeline "ecs_from_file" }}'

  - foreach:
      tag: foreach_opencti_observable_directory_95bb2cf7
      field: opencti.observable.directory
      ignore_missing: true
      processor:
        pipeline:
          tag: pipeline_5d30c115
          name: '{{ IngestPipeline "ecs_from_directory" }}'

  - foreach:
      tag: foreach_opencti_observable_artifact_77e14cb3
      field: opencti.observable.artifact
      ignore_missing: true
      processor:
        pipeline:
          tag: pipeline_ae3fe7a9
          name: '{{ IngestPipeline "ecs_from_artifact" }}'

  #####################################
  # ECS indicator fields from pattern #
  #####################################

  # If there are no observables, get the indicator from the pattern
  - split:
      tag: split_pattern_if_no_observables
      if: ctx.opencti?.indicator?.observables_count == 0 && ctx.opencti?.indicator?.pattern != null
      field: opencti.indicator.pattern
      separator: '\s+AND\s+|\s+OR\s+'
      target_field: opencti.indicator._patterns
      ignore_missing: true

  - foreach:
      tag: foreach_opencti_indicator__patterns_83e78c47
      field: opencti.indicator._patterns
      ignore_missing: true
      processor:
        pipeline:
          tag: pipeline_006ce604
          name: '{{ IngestPipeline "ecs_from_pattern" }}'

  - remove:
      tag: remove_opencti_indicator__patterns_18e67427
      field: opencti.indicator._patterns
      ignore_missing: true


  ######################
  # ECS related fields #
  ######################
  - foreach:
      tag: foreach_opencti_observable_x509_certificate_ba403611
      field: opencti.observable.x509_certificate
      ignore_missing: true
      processor:
        foreach:
          tag: foreach_ingest_value_hash
          field: _ingest._value.hash
          ignore_missing: true
          processor:
            append:
              tag: append_related_hash
              field: related.hash
              value: "{{{_ingest._value}}}"
              allow_duplicates: false
  - script:
      tag: script_d3f7fd43
      description: Extract file hashes to related.hash, handling both array and single object cases
      lang: painless
      source: |
        if (ctx.threat?.indicator?.file != null) {
          def files = ctx.threat.indicator.file instanceof List ? ctx.threat.indicator.file : [ctx.threat.indicator.file];
          ctx.related = ctx.related ?: [:];
          ctx.related.hash = ctx.related.hash ?: [];
          def hashSet = new HashSet(ctx.related.hash);
          for (def file : files) {
            if (file?.hash != null) {
              if (file.hash.md5 != null) {
                hashSet.add(file.hash.md5);
              }
              if (file.hash.sha1 != null) {
                hashSet.add(file.hash.sha1);
              }
              if (file.hash.sha256 != null) {
                hashSet.add(file.hash.sha256);
              }
            }
          }
          if (hashSet.size() > 0) {
            ctx.related.hash = new ArrayList(hashSet);
          } else {
            ctx.related.remove('hash');
          }
        }
  - foreach:
      tag: foreach_opencti_observable_artifact_4c1ad4e3
      field: opencti.observable.artifact
      ignore_missing: true
      processor:
        foreach:
          tag: foreach_ingest_value_hash_56857a2e
          field: _ingest._value.hash
          ignore_missing: true
          processor:
            append:
              tag: append_related_hash_b178d925
              field: related.hash
              value: "{{{_ingest._value}}}"
              allow_duplicates: false
  - foreach:
      tag: foreach_opencti_observable_process_d502ee1e
      field: opencti.observable.process
      ignore_missing: true
      processor:
        foreach:
          tag: foreach_ingest_value_service_dll_c9aebbde
          field: _ingest._value.service_dll
          ignore_missing: true
          processor:
            foreach:
              tag: foreach_ingest_value_hash_b2d29982
              field: _ingest._value.hash
              ignore_missing: true
              processor:
                append:
                  tag: append_related_hash_45742abd
                  field: related.hash
                  value: "{{{_ingest._value}}}"
                  allow_duplicates: false

  - foreach:
      tag: foreach_opencti_observable_hostname_55369f20
      field: opencti.observable.hostname
      ignore_missing: true
      processor:
        append:
          tag: append_related_hosts
          field: related.hosts
          value: "{{{_ingest._value.value}}}"
          allow_duplicates: false
  - foreach:
      tag: foreach_threat_indicator_url_441074f7
      field: threat.indicator.url
      ignore_missing: true
      processor:
        append:
          tag: append_related_hosts_961fe142
          field: related.hosts
          value: "{{{_ingest._value.domain}}}"
          allow_duplicates: false

  - set:
      tag: set_related_ip_776c7669
      field: related.ip
      copy_from: threat.indicator.ip
      if: ctx.threat.indicator.ip instanceof List

  - foreach:
      tag: foreach_threat_indicator_email_address_81fcc02c
      field: threat.indicator.email.address
      ignore_missing: true
      processor:
        append:
          tag: append_related_user
          field: related.user
          value: "{{{_ingest._value}}}"
          allow_duplicates: false
  - foreach:
      tag: foreach_threat_indicator_url_6544aad7
      field: threat.indicator.url
      ignore_missing: true
      processor:
        append:
          tag: append_related_user_91f0560a
          field: related.user
          value: "{{{_ingest._value.username}}}"
          allow_duplicates: false
  - foreach:
      tag: foreach_opencti_observable_user_account_e4428b4e
      field: opencti.observable.user_account
      ignore_missing: true
      processor:
        append:
          tag: append_related_user_f808821c
          field: related.user
          value: "{{{_ingest._value.user_id}}}"
          allow_duplicates: false
  - foreach:
      tag: foreach_opencti_observable_user_account_31dd5216
      field: opencti.observable.user_account
      ignore_missing: true
      processor:
        append:
          tag: append_related_user_759a12d9
          field: related.user
          value: "{{{_ingest._value.account_login}}}"
          allow_duplicates: false

  - script:
      tag: script_38ed5043
      description: Remove null or empty items, remove list if empty
      lang: painless
      source: |
        def cleanUp(Map obj, String key) {
          if (obj.containsKey(key) && obj[key] instanceof List) {
            def result = [];
            for (item in obj[key]) {
                if (item != null && item != '') {
                    result.add(item);
                }
            }
            if (result.isEmpty()) {
              obj.remove(key);
            } else {
              obj[key] = result;
            }
          }
          return obj;
        }
        if (ctx.containsKey('related') && ctx.related != null) {
          cleanUp(ctx.related, 'hash');
          cleanUp(ctx.related, 'hosts');
          cleanUp(ctx.related, 'ip');
          cleanUp(ctx.related, 'user');
        }


  ##############################################
  # Restructure to avoid non-leaf lists, under #
  # - opencti.observable.*                     #
  # - opencti.indicator.external_reference     #
  # - threat.indicator.as                      #
  # - threat.indicator.file                    #
  # - threat.indicator.registry                #
  # - threat.indicator.url                     #
  # - threat.indicator.x509                    #
  ##############################################
  - script:
      description: Restructure some lists of maps to avoid non-leaf lists
      tag: merge_maps
      lang: painless
      source: |
        def mergeMaps(Map map1, Map map2) {
          for (def key : map2.keySet()) {
            if (map1.containsKey(key) && map1[key] != map2[key]) {
              if (map1[key] instanceof Map && map2[key] instanceof Map) {
                map1[key] = mergeMaps(map1[key], map2[key]);
              } else {
                if (!(map1[key] instanceof List)) {
                  map1[key] = [map1[key]];
                }
                def combined = new HashSet(map1[key]);
                if (map2[key] instanceof List) {
                  combined.addAll(map2[key]);
                } else {
                  combined.add(map2[key]);
                }
                map1[key] = new ArrayList(combined);
              }
            } else {
              map1[key] = map2[key];
            }
          }
          return map1;
        }
        def mergeListOfMaps(List list) {
          def merged = new HashMap();
          for (def map : list) {
            merged = mergeMaps(merged, map);
          }
          return merged;
        }
        if (ctx.opencti?.containsKey('observable') == true) {
          for (def key : ctx.opencti.observable.keySet()) {
            if (ctx.opencti.observable[key] instanceof List) {
              ctx.opencti.observable[key] = mergeListOfMaps(ctx.opencti.observable[key]);
            }
          }
        }
        if (ctx.opencti?.indicator?.containsKey('external_reference') == true && ctx.opencti.indicator.external_reference instanceof List) {
          ctx.opencti.indicator.external_reference = mergeListOfMaps(ctx.opencti.indicator.external_reference);
        }
        if (ctx.threat.indicator.containsKey('file') && ctx.threat.indicator.file instanceof List) {
          ctx.threat.indicator.file = mergeListOfMaps(ctx.threat.indicator.file);
        }
        if (ctx.threat.indicator.containsKey('as') && ctx.threat.indicator.as instanceof List) {
          ctx.threat.indicator.as = mergeListOfMaps(ctx.threat.indicator.as);
        }
        if (ctx.threat.indicator.containsKey('url') && ctx.threat.indicator.url instanceof List) {
          ctx.threat.indicator.url = mergeListOfMaps(ctx.threat.indicator.url);
        }
        if (ctx.threat.indicator.containsKey('registry') && ctx.threat.indicator.registry instanceof List) {
          ctx.threat.indicator.registry = mergeListOfMaps(ctx.threat.indicator.registry);
        }
        if (ctx.threat.indicator.containsKey('x509') && ctx.threat.indicator.x509 instanceof List) {
          ctx.threat.indicator.x509 = mergeListOfMaps(ctx.threat.indicator.x509);
        }

  ###################################
  # Fingerprint for deduplication   #
  ###################################

  # Generate a consistent document ID based on the indicator's unique STIX ID
  # This prevents duplicates when multiple agents fetch the same data
  - fingerprint:
      fields:
        - opencti.indicator.standard_id # STIX ID is globally unique and consistent
        - threat.indicator.modified_at
        - opencti.indicator.updated_at
      target_field: _id
      ignore_missing: true
      description: Generate consistent document ID for deduplication
      tag: fingerprint_id


  ###########################################################
  # Tag indicators suitable for Security rule creation     #
  ###########################################################
  - set:
      tag: set_opencti_indicator_rule_compatible_6efdbb7d
      field: opencti.indicator.rule_compatible
      value: true
      if: >-
        ctx.opencti?.indicator?.pattern_type != null && (
        ctx.opencti.indicator.pattern_type == 'kql' ||
        ctx.opencti.indicator.pattern_type == 'lucene' ||
        ctx.opencti.indicator.pattern_type == 'eql' ||
        ctx.opencti.indicator.pattern_type == 'esql'
        ) && ctx.opencti?.indicator?.revoked != true
      description: Mark indicators that can be converted to detection rules

  - append:
      tag: append_tags_bcf1b90e
      field: tags
      value: detection-rule-candidate
      if: ctx.opencti?.indicator?.rule_compatible == true
      allow_duplicates: false

  - set:
      tag: set_opencti_indicator_detection_rule_type_c5e9a6fa
      field: opencti.indicator.detection_rule.type
      value: query
      if: ctx.opencti?.indicator?.pattern_type == 'kql' || ctx.opencti?.indicator?.pattern_type == 'lucene'

  - set:
      tag: set_opencti_indicator_detection_rule_type_fec943c5
      field: opencti.indicator.detection_rule.type
      value: eql
      if: ctx.opencti?.indicator?.pattern_type == 'eql'

  - set:
      tag: set_opencti_indicator_detection_rule_type_33211ec5
      field: opencti.indicator.detection_rule.type
      value: esql
      if: ctx.opencti?.indicator?.pattern_type == 'esql'

  - set:
      tag: set_opencti_indicator_detection_rule_query_7d52745b
      field: opencti.indicator.detection_rule.query
      copy_from: opencti.indicator.pattern
      if: ctx.opencti?.indicator?.rule_compatible == true


  #######################################################
  # Tag to show if ECS indicator details were populated #
  #######################################################
  - append:
      tag: append_tags_5d8c4c89
      field: tags
      value: ecs-indicator-detail
      if: >
        ctx.threat.indicator.containsKey('as') ||
        ctx.threat.indicator.containsKey('email') ||
        ctx.threat.indicator.containsKey('file') ||
        ctx.threat.indicator.containsKey('ip') ||
        ctx.threat.indicator.containsKey('port') ||
        ctx.threat.indicator.containsKey('registry') ||
        ctx.threat.indicator.containsKey('url') ||
        ctx.threat.indicator.containsKey('x509') ||
        false
      allow_duplicates: false

  - script:
      tag: remove-nulls
      lang: painless
      description: This script processor iterates over the whole document to remove fields with null values.
      source: |
        void handleMap(Map map) {
          map.values().removeIf(v -> {
            if (v instanceof Map) {
                handleMap(v);
            } else if (v instanceof List) {
                handleList(v);
            }
            return v == null || v == '' || v == '-' || v == 'none' || (v instanceof Map && v.size() == 0) || (v instanceof List && v.size() == 0)
          });
        }
        void handleList(List list) {
          list.removeIf(v -> {
            if (v instanceof Map) {
                handleMap(v);
            } else if (v instanceof List) {
                handleList(v);
            }
            return v == null || v == '' || v == '-' || v == 'none' || (v instanceof Map && v.size() == 0) || (v instanceof List && v.size() == 0)
          });
        }
        handleMap(ctx);

###################
# Failure handler #
###################

on_failure:
  - append:
      tag: append_error_message
      field: error.message
      value: >
        Processor "{{{ _ingest.on_failure_processor_type }}}"
        with tag "{{{ _ingest.on_failure_processor_tag }}}"
        in pipeline "{{{ _ingest.on_failure_pipeline }}}"
        failed with message "{{{ _ingest.on_failure_message }}}"
  - set:
      tag: set_event_kind
      field: event.kind
      value: pipeline_error
  - append:
      tag: append_tags_0d5052d2
      field: tags
      value: preserve_original_event
      allow_duplicates: false
