wait_for_data_timeout: 1m
input: cel
service: anduril_lattice
vars:
  url: http://{{Hostname}}:{{Port}}
  client_id: test-client-id
  client_secret: test-client-secret
  sandbox_token: test-sandbox-token-xxxx
data_stream:
  vars:
    interval: 2s
    batch_size: 1
    preserve_original_event: true
assert:
  hit_count: 2
