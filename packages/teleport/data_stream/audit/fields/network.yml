- name: teleport.audit.network
  type: group
  description: "Network metadata"
  fields:
    #    - name: source_address
    #      type: ip
    #      description: >
    #        SrcAddr is the source IP address of the connection.
    #    - name: destination_address
    #      type: ip
    #      description: >
    #        DstAddr is the destination IP address of the connection.
    #    - name: destination_port
    #      type: integer
    #      description: >
    #        DstPort is the destination port of the connection.
    - name: tcp_version
      type: integer
      description: "TCPVersion is the version of TCP (4 or 6)."
    - name: operation
      type: keyword
      description: "Operation denotes what network operation was performed (e.g. connect)"
    - name: action
      type: keyword
      description: "Action denotes what happened in response to the event"
    - name: bytes_transmitted
      type: long
      description: "BytesTransmitted is the amount of bytes transmitted"
    - name: bytes_received
      type: long
      description: "BytesReceived is the amount of bytes received"
