- name: teleport.audit.connection
  type: group
  description: >
    Connection contains connection info
  fields:
    - name: local_address
      type: keyword
      description: >
        LocalAddr is a target address on the host
    - name: remote_address
      type: keyword
      description: >
        RemoteAddr is a client (user's) address
