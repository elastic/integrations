title: "Teleport audit logs"
type: logs
streams:
  - input: logfile
    title: Teleport audit logs
    description: Collect Teleport audit logs
    vars:
      - name: paths
        type: text
        title: Paths
        multi: true
        default:
          - /var/log/*.log
