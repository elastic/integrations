type: metrics
title: NATS stats metrics
streams:
  - input: nats/metrics
    vars:
      - name: period
        type: text
        title: Period
        multi: false
        required: true
        show_user: true
        default: 10s
    title: NATS stats
    description: Collect NATS stats
elasticsearch:
  index_mode: "time_series"
