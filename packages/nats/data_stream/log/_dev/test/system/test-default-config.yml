skip:
  reason: Test is skipped for now since it has been reported as flaky. We can rely on pipeline tests for now.
  link: https://github.com/elastic/integrations/issues/1576
vars: ~
data_stream:
  vars:
    paths:
      - "{{SERVICE_LOGS_DIR}}/nats.log*"
