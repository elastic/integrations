errors:
  exclude_checks:
    - SVR00006 # ingest pipeline processors missing required tag; pipeline pre-dates this requirement.
    - SVR00009 # ingest pipeline on_failure handler must set event.kind to "pipeline_error"; pipeline pre-dates this requirement.