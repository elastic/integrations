version: '2.3'
services:
  # package_registry:
  #   image: docker.elastic.co/package-registry/distribution:lite-v2-experimental
  #   healthcheck:
  #     test: ["CMD", "curl", "--cacert", "/etc/ssl/package-registry/ca-cert.pem", "-f", "https://localhost:8080"]
  #     retries: 300
  #     interval: 1s
  #   environment:
  #     - EPR_METRICS_ADDRESS="0.0.0.0:9000"
  #   ports:
  #     - 9100:9000
  #     - 8888:8000
  elastic_package_registry:
    image: alpine
    command: tail -F anything
