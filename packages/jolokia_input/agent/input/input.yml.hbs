metricsets: ["jmx"]
hosts:
{{#each hosts}}
  - {{this}}
{{/each}}
period: {{period}}
metrics_path: {{metrics_path}}
namespace: "metrics"
http_method: 'GET' 
jmx.mappings: {{jmx.mappings}}
data_stream:
  dataset: {{data_stream.dataset}}
{{#if processors}}
processors:
{{processors}}
{{/if}}