# Explicit Temporal Cloud OpenMetrics scrape with Bearer auth.
data_stream:
  vars:
    targets:
      - "metrics.temporal.io:443"
    scrape_interval: "60s"
    scrape_timeout: "30s"
    metrics_path: "/v1/metrics"
    scheme: "https"
    bearer_token: "test-temporal-api-key"
