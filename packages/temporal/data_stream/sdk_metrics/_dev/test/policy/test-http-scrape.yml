# Explicit Worker/SDK Prometheus scrape over HTTP.
data_stream:
  vars:
    targets:
      - "host.docker.internal:8077"
    scrape_interval: "15s"
    scrape_timeout: "10s"
    metrics_path: "/metrics"
    scheme: "http"
