# Harness scratch — never commit (can hold multi-hundred-MB corpora and run logs).
# Re-run the harness to regenerate.
logs/
*/work/
work-all/

# Belt and braces: nothing here should ever read a tenant response, but if someone drops
# one in to compare against, keep it out of the repo.
samples/

__pycache__/
