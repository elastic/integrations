input: httpjson
service: ping_one
vars:
  api_path: http://{{Hostname}}:{{Port}}
  token_url: http://{{Hostname}}:{{Port}}/xxxx/as/token
  environment_id: xxxx
  client_id: xxxx
  client_secret: xxxx
data_stream:
  vars:
    preserve_original_event: true
    preserve_duplicate_custom_fields: true
    enable_request_tracer: true
