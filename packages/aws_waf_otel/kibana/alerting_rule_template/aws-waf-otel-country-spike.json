{
    "id": "aws-waf-otel-country-spike",
    "type": "alerting_rule_template",
    "attributes": {
        "name": "[AWS WAF OTEL] Unexpected country spike",
        "tags": ["AWS WAF Logs OpenTelemetry Assets"],
        "ruleTypeId": ".es-query",
        "schedule": {
            "interval": "10m"
        },
        "params": {
            "searchType": "esqlQuery",
            "timeWindowSize": 10,
            "timeWindowUnit": "m",
            "esqlQuery": {
                "esql": "// Alert triggers when any country generated requests exceed a threshold (e.g. > 1000 in 10 minutes)\n// You can adjust the threshold value by modifying the req_count in the WHERE clause.\nFROM logs-aws.waf.otel-default\n| STATS req_count = COUNT() BY geo.country.iso_code\n| WHERE req_count > 1000\n| SORT req_count DESC"
            },
            "groupBy": "row",
            "timeField": "@timestamp"
        },
        "alertDelay": {
            "active": 1
        }
    },
    "managed": true,
    "coreMigrationVersion": "8.8.0",
    "typeMigrationVersion": "10.1.0"
}