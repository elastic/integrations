{
    "id": "aws-waf-otel-block-allow-ratio",
    "type": "alerting_rule_template",
    "attributes": {
        "name": "[AWS WAF OTEL] High BLOCK/ALLOW ratio",
        "tags": [
            "AWS",
            "AWS WAF",
            "OTel"
        ],
        "ruleTypeId": ".es-query",
        "schedule": {
            "interval": "10m"
        },
        "params": {
            "searchType": "esqlQuery",
            "timeWindowSize": 10,
            "timeWindowUnit": "m",
            "esqlQuery": {
                "esql": "// Alert triggers when the BLOCK to ALLOW ratio is higher than the set value (default 0.5 - 50%)\n// You can adjust the threshold value by modifying the ratio in the WHERE clause.\nFROM logs-aws.waf.otel-default\n| STATS block_count = COUNT(*) WHERE aws.waf.action == \"BLOCK\", allow_count = COUNT(*) WHERE aws.waf.action == \"ALLOW\" BY cloud.resource_id, cloud.region\n| EVAL ratio = block_count / TO_DOUBLE(allow_count)\n| WHERE ratio >= 0.5"
            },
            "groupBy": "row",
            "timeField": "@timestamp"
        },
        "alertDelay": {
            "active": 1
        }
    },
    "managed": true,
    "coreMigrationVersion": "8.8.0",
    "typeMigrationVersion": "10.1.0"
}