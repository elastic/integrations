version: '3.8'
services:
  ti_flashpoint:
    image: docker.elastic.co/observability/stream:v0.23.0
    hostname: ti_flashpoint
    ports:
      - 8090
    volumes:
      - ./files:/files:ro
    command:
      - http-server
      - --addr=:8090
      - --config=/files/config.yml
