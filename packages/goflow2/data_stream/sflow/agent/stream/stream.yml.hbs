paths:
{{#each paths as |path i|}}
  - {{path}}
{{/each}}
exclude_files: [".gz$"]
processors:
  - add_locale: ~
  - decode_json_fields:
        fields: message
        target: ""
        add_error_key: true