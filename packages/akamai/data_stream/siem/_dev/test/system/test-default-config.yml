input: httpjson
service: akamai
vars: ~
data_stream:
  vars:
    preserve_original_event: true
    host: {{Hostname}}:{{Port}}
    client_token: qwerasdf
    client_secret: qazwsx
    access_token: abcd
    scheme: http
    config_ids: aaaa
    ssl.verification_mode: none
