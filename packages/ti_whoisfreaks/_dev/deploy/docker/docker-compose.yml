services:
  whoisfreaks-mock:
    image: python:3.12-alpine
    volumes:
      - ./files/mock_server.py:/mock/mock_server.py:ro
    command: ["python", "/mock/mock_server.py"]
    ports:
      - "8080"
