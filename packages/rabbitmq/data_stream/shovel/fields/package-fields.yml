- name: rabbitmq
  type: group
  fields:
    - name: vhost
      type: keyword
      description: |
        Virtual host name with non-ASCII characters escaped as in C.
    - name: node.name
      type: keyword
      description: |
        Node name
