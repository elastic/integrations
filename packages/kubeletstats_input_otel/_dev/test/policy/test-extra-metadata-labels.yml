vars:
  endpoint: "https://localhost:10250"
  collection_interval: 30s
  initial_delay: 5s
  auth_type: serviceAccount
  extra_metadata_labels:
    - container.id
    - k8s.volume.type
  metric_groups:
    - container
    - pod
    - node
    - volume
