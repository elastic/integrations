vars:
  endpoint: "https://localhost:10250"
  collection_interval: 30s
  initial_delay: 5s
  auth_type: serviceAccount
  node: my-node
  k8s_api_config_auth_type: kubeConfig
  k8s_api_config_context: my-context
  metric_groups:
    - container
    - pod
    - node
    - volume
