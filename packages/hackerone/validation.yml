errors:
  exclude_checks:
    # Every dashboard panel uses ES|QL against the bounded transform alias
    # `logs-hackerone_latest.report`, so the implicit dataset filter required
    # by SVR00002 is unnecessary here.
    - SVR00002 # Mandatory filters in dashboards.
