{
  "id": "azure_activity_otel-sensitive-key-access",
  "type": "alerting_rule_template",
  "managed": true,
  "attributes": {
    "name": "[Azure Activity OTel] Sensitive key access",
    "ruleTypeId": ".es-query",
    "tags": ["observability", "azure", "activity-logs", "security"],
    "schedule": {
      "interval": "5m"
    },
    "alertDelay": {
      "active": 1
    },
    "flapping": {
      "enabled": true,
      "lookBackWindow": 10,
      "statusChangeThreshold": 4
    },
    "params": {
      "searchType": "esqlQuery",
      "esqlQuery": {
        "esql": "FROM logs-*\n// Restrict to Azure Activity Logs\n| WHERE azure.category IS NOT NULL\n| WHERE azure.category == \"Administrative\"\n// listKeys/action exposes storage account keys — high-privilege, security-sensitive\n| WHERE azure.operation.name LIKE \"*LISTKEYS*\" OR azure.operation.name LIKE \"*LISTKEY*\"\n// Include both success and failure — attempted access is the signal\n| STATS access_count = COUNT(*), result_types = VALUES(azure.result.type) BY user.name, azure.identity.authorization.evidence.principal.type, network.peer.address, cloud.resource_id\n| SORT access_count DESC\n| LIMIT 20"
      },
      "size": 0,
      "threshold": [0],
      "thresholdComparator": ">",
      "timeField": "@timestamp",
      "timeWindowSize": 15,
      "timeWindowUnit": "m",
      "groupBy": "row",
      "termField": "user.name",
      "termSize": 10,
      "excludeHitsFromPreviousRun": true
    },
    "artifacts": {
      "dashboards": [
        { "id": "azure_activity_otel-overview" },
        { "id": "azure_activity_otel-administrative-operations" }
      ]
    }
  }
}
