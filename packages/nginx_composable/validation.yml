errors:
  exclude_checks:
    - SVR00010 # Composable inputs use `package:` (empty `type:`); name disambiguation is applied by elastic-package at build time. Misfire in package-spec v3.6.2 validate_integration_input_qualifier.go — see Gap F in elastic-package/CONTEXT_composable_gaps.md.
