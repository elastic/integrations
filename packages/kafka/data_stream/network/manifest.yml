title: Apache Kafka network metrics
type: metrics
release: beta
streams:
  - input: jolokia/metrics
    title: Apache Kafka network metrics
    description: Collect Apache Kafka network metrics using Jolokia agent.
    enabled: false
    vars:
      - name: period
        type: text
        title: Period
        default: 60s
        multi: false
        show_user: true
      - name: processors
        type: yaml
        title: Processors
        multi: false
        required: false
        show_user: false
        description: >
          Processors are used to reduce the number of fields in the exported event or to enhance the event with metadata. This executes in the agent before the logs are parsed. See [Processors](https://www.elastic.co/guide/en/beats/filebeat/current/filtering-and-enhancing-data.html) for details.
elasticsearch:
  index_mode: "time_series"
  index_template:
    settings:
      index:
        mapping:
          dimension_fields:
            limit: 32