paths:
{{#each paths}}
  - {{this}}
{{/each}}
exclude_files: [".gz$"]
{{#if tags}}
tags:
{{#each tags as |tag|}}
  - {{tag}}
{{/each}}
{{#if preserve_original_event}}
  - preserve_original_event
{{/if}}
{{else}}
{{#if preserve_original_event}}
tags:
  - preserve_original_event
{{/if}}
{{/if}}
{{#if processors}}
processors:
{{processors}}
{{/if}}
