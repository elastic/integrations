host: "{{syslog_host}}:{{syslog_port}}"
tags:
{{#each tags as |tag i|}}
 - {{tag}}
{{/each}}
publisher_pipeline.disable_host: true
processors:
  - add_fields:
      target: ''
      fields:
        ecs.version: 1.5.0
