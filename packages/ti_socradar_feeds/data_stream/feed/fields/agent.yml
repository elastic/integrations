- name: cloud
  title: Cloud
  group: 2
  type: group
  description: Fields related to the cloud or infrastructure the events are coming from.
  footnote: "Examples: If Metricbeat is running on an EC2 host and fetches data from its host, the cloud info contains the data about this machine. If Metricbeat runs on a remote machine outside the cloud and fetches data from a service running in the cloud, the field contains cloud data from the machine the service is running on."
  fields:
    - name: image.id
      type: keyword
      description: Image ID for the cloud instance.
- name: host
  title: Host
  group: 2
  type: group
  description: A general observer that is defined by the `host` namespace. Most fields in `host` can apply to `observer` as well.
  fields:
    - name: containerized
      type: boolean
      description: If the host is a container.
    - name: os.build
      type: keyword
      description: OS build information.
    - name: os.codename
      type: keyword
      description: OS codename, if any.
