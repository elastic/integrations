format_version: 3.0.2
name: benchmark_output
title: "Benchmark output"
version: 0.0.1
source:
  license: "Elastic-2.0"
description: "Benchmark output configuration"
type: integration
categories:
  - custom
conditions:
  kibana:
    version: "^8.10.1"
  elastic:
    subscription: "basic"
icons:
  - src: /img/sample-logo.svg
    title: Sample logo
    size: 32x32
    type: image/svg+xml
policy_templates:
  - name: benchmark
    title: Benchmark output
    description: Benchmark output configuration, by sending generated messages
    inputs:
      - type: benchmark
        title: Generate sample events and benchmark output
        description: Generate sample events and benchmark output
owner:
  github: elastic/elastic-agent-data-plane
  type: elastic
