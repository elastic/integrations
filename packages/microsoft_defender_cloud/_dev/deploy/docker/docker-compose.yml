version: '3.8'
services:
  microsoft_defender_cloud:
    image: docker.elastic.co/observability/stream:v0.23.0
    hostname: microsoft_defender_cloud
    ports:
      - 8090
    volumes:
      - ./files:/files:ro
    command:
      - http-server
      - --addr=:8090
      - --config=/files/config.yml
