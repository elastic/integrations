input: httpjson
service: okta
vars:
  url: http://{{Hostname}}:{{Port}}/api/v1/logs
  interval: 10s
  api_key: testing
data_stream:
  vars:
    preserve_original_event: true
    enable_request_tracer: true
assert:
  hit_count: 4
