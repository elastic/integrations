__ui: {{__ui}}
type: {{type}}
name: {{name}}
hosts: {{hosts}}
{{#if service.name}}
service.name: {{service.name}}
{{/if}}
schedule: {{schedule}}
wait: {{wait}}
timeout: {{timeout}}
{{#if tags}}
tags: {{tags}}
{{/if}}
processors:
  - add_observer_metadata:
      geo: 
        name: Fleet managed
  - add_fields:
      target: ''
      fields:
        monitor.fleet_managed: true