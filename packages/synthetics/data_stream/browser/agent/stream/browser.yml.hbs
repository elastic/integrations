type: {{type}}
name: {{name}}
{{#if service.name}}
service.name: {{service.name}}
{{/if}}
schedule: {{schedule}}
timeout: {{timeout}}
{{#if tags}}
tags: {{tags}}
{{/if}}
{{#if source.zip_url.username}}
source.zip_url.username: {{source.zip_url.username}}
{{/if}}
{{#if source.zip_url.password}}
source.zip_url.password: {{source.zip_url.password}}
{{/if}}
{{#if source.zip_url.url}}
source.zip_url.url: {{source.zip_url.url}}
{{/if}}
{{#if source.zip_url.folder}}
source.zip_url.folder: {{source.zip_url.folder}}
{{/if}}
{{#if source.inline.script}}
source.inline.script: {{source.inline.script}}
{{/if}}
{{#if params}}
params: {{params}}
{{/if}}
{{#if screenshots}}
screenshots: {{screenshots}}
{{/if}}
{{#if synthetics_args}}
synthetics_args: {{synthetics_args}}
{{/if}}
{{#if ignore_https_errors}}
ignore_https_errors: {{ignore_https_errors}}
{{/if}}
processors:
  - add_observer_metadata:
      geo: 
        name: Fleet managed
  - add_fields:
      target: ''
      fields:
        monitor.fleet_managed: true