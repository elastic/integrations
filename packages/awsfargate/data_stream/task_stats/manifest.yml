type: metrics
title: AWS Fargate task_stats metrics
release: experimental
streams:
- input: awsfargate/metrics
  vars:
  - name: period
    type: text
    title: Period
    multi: false
    required: true
    show_user: true
    default: 10s
  title: AWS Fargate task_stats metrics
  description: Collect AWS Fargate task_stats metrics
