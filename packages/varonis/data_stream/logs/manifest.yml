title: "Varonis alerts"
type: logs
streams:
  - input: udp
    enabled: false
    template_path: udp.yml.hbs
    title: Syslog UDP input
    description: Collect Varonis alerts using syslog input over UDP
    vars:
      - name: listen_address
        type: text
        title: Listen Address
        description: |
          Bind address for the syslog listener. Use 0.0.0.0 to listen on all interfaces.
        required: true
        show_user: true
        default: 0.0.0.0
      - name: listen_port
        type: text
        title: Listen port
        description: |
          Bind port for the syslog listener.
        required: true
        show_user: true
        default: 9030
      - name: preserve_original_event
        required: true
        show_user: true
        title: Preserve original event
        description: Preserves a raw copy of the original event, added to the field `event.original`
        type: bool
        multi: false
        default: false
      - name: processors
        type: yaml
        title: Processors
        multi: false
        required: false
        show_user: false
        description: >
          Processors are used to reduce the number of fields in the exported event or to enhance the event with metadata.  This executes in the agent before the logs are parsed. See [Processors](https://www.elastic.co/guide/en/beats/filebeat/current/filtering-and-enhancing-data.html) for details.

