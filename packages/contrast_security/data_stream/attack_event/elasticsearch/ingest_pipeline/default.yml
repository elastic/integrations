---
description: Minimal ingest pipeline for Contrast Security attack events.
processors:
  - set:
      tag: set_event_dataset
      field: event.dataset
      value: "contrast_security.attack_event"
      override: false
  - set:
      tag: set_event_module
      field: event.module
      value: "contrast_security"
      override: false
  - set:
      tag: set_event_kind
      field: event.kind
      value: "event"
      override: false
  - date:
      tag: date_timestamp
      field: "@timestamp"
      target_field: "@timestamp"
      formats:
        - ISO8601
        - UNIX_MS
      ignore_failure: true
on_failure:
  - append:
      tag: append_error_message
      field: error.message
      value: >-
        Processor '{{{ _ingest.on_failure_processor_type }}}' in pipeline '{{{ _ingest.pipeline }}}'
        {{{#_ingest.on_failure_processor_tag}}}with tag '{{{ _ingest.on_failure_processor_tag }}}'
        {{{/_ingest.on_failure_processor_tag}}}failed with message '{{{ _ingest.on_failure_message }}}'
  - set:
      tag: set_pipeline_error
      field: event.kind
      value: pipeline_error
  - append:
      tag: append_preserve_original_event
      field: tags
      value: preserve_original_event
      allow_duplicates: false
