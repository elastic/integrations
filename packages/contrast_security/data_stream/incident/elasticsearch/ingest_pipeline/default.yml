---
description: Minimal ingest pipeline for Contrast Security incidents.
processors:
  - set:
      tag: set_event_ingested
      field: event.ingested
      value: "{{_ingest.timestamp}}"
  - set:
      tag: set_event_dataset
      field: event.dataset
      value: "contrast_security.incident"
      override: false
  - set:
      tag: set_event_module
      field: event.module
      value: "contrast_security"
      override: false
  - set:
      tag: set_event_kind
      field: event.kind
      value: "alert"
      override: false
  - date:
      tag: date_timestamp
      field: "@timestamp"
      target_field: "@timestamp"
      formats:
        - ISO8601
        - UNIX_MS
      ignore_failure: true
on_failure:
  - set:
      tag: set_pipeline_error
      field: event.kind
      value: pipeline_error
  - append:
      tag: append_error_message
      field: error.message
      value: "Ingest pipeline error: {{{_ingest.on_failure_message}}}"
