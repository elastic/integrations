---
description: Pipeline for processing audit logs.
processors:
  - remove:
      field:
        - organization
        - division
        - team
      ignore_missing: true
      if: ctx.organization instanceof String && ctx.division instanceof String && ctx.team instanceof String
      tag: remove_agentless_tags
      description: >-
        Removes the fields added by Agentless as metadata,
        as they can collide with ECS fields.
  - set:
      field: ecs.version
      tag: set_ecs_version
      value: 8.17.0
  - terminate:
      tag: data_collection_error
      if: ctx.error?.message != null && ctx.message == null && ctx.event?.original == null
  - rename:
      field: message
      tag: rename_message_to_event_original
      target_field: event.original
      ignore_missing: true
      description: Renames the original `message` field to `event.original` to store a copy of the original message. The `event.original` field is not touched if the document already has one; it may happen when Logstash sends the document.
      if: ctx.event?.original == null
  - remove:
      field: message
      tag: remove_message
      ignore_missing: true
      description: The `message` field is no longer required if the document has an `event.original` field.
      if: ctx.event?.original != null
  - json:
      field: event.original
      tag: json_event_original
      target_field: json
      on_failure:
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - set:
      field: event.kind
      tag: set_event_kind
      value: event
  - fingerprint:
      fields:
        - json.frontegg_id
        - json.createdAt
      tag: fingerprint_audit
      target_field: _id
  - rename:
      field: json.action
      tag: rename_action
      target_field: cyera.audit.action
      ignore_missing: true
  - set:
      field: event.action
      tag: set_event_action_from_audit_action
      copy_from: cyera.audit.action
      ignore_empty_value: true
  - date:
      field: json.createdAt
      tag: date_createdAt
      target_field: cyera.audit.created_at
      formats:
        - ISO8601
      if: ctx.json?.createdAt != null && ctx.json.createdAt != ''
      on_failure:
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - set:
      field: '@timestamp'
      tag: set_@timestamp_from_audit_created_at
      copy_from: cyera.audit.created_at
      ignore_empty_value: true
  - date:
      field: json.updatedAt
      tag: date_updatedAt
      target_field: cyera.audit.updated_at
      formats:
        - ISO8601
      if: ctx.json?.updatedAt != null && ctx.json.updatedAt != ''
      on_failure:
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - rename:
      field: json.description
      tag: rename_description
      target_field: cyera.audit.description
      ignore_missing: true
  - set:
      field: message
      tag: set_message_from_audit_description
      copy_from: cyera.audit.description
      ignore_empty_value: true
  - rename:
      field: json.email
      tag: rename_email
      target_field: cyera.audit.email
      ignore_missing: true
  - set:
      field: user.email
      tag: set_user_email_from_audit_email
      copy_from: cyera.audit.email
      ignore_empty_value: true
  - append:
      field: related.user
      tag: append_audit_email_into_related_user
      value: '{{{cyera.audit.email}}}'
      allow_duplicates: false
      if: ctx.cyera?.audit?.email != null
  - rename:
      field: json.environmentName
      tag: rename_environmentName
      target_field: cyera.audit.environment_name
      ignore_missing: true
  - rename:
      field: json.frontegg_id
      tag: rename_frontegg_id
      target_field: cyera.audit.frontegg_id
      ignore_missing: true
  - set:
      field: event.id
      tag: set_event_id_from_audit_frontegg_id
      copy_from: cyera.audit.frontegg_id
      ignore_empty_value: true
  - convert:
      field: json.ip
      tag: convert_ip_to_ip
      target_field: cyera.audit.ip
      type: ip
      ignore_missing: true
      on_failure:
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - set:
      field: source.ip
      tag: set_source_ip_from_audit_ip
      copy_from: cyera.audit.ip
      ignore_empty_value: true
  - append:
      field: related.ip
      tag: append_audit_ip_into_related_ip
      value: '{{{cyera.audit.ip}}}'
      allow_duplicates: false
      if: ctx.cyera?.audit?.ip != null
  - rename:
      field: json.severity
      tag: rename_severity
      target_field: cyera.audit.severity
      ignore_missing: true
  - set:
      field: log.level
      tag: set_log_level_from_audit_severity
      copy_from: cyera.audit.severity
      ignore_empty_value: true
  - rename:
      field: json.tenantId
      tag: rename_tenantId
      target_field: cyera.audit.tenant_id
      ignore_missing: true
  - rename:
      field: json.userAgent
      tag: rename_userAgent
      target_field: cyera.audit.user_agent
      ignore_missing: true
  - user_agent:
      field: cyera.audit.user_agent
      tag: user_agent_audit_user_agent
      ignore_missing: true
      ignore_failure: true
  - rename:
      field: json.vendorId
      tag: rename_vendorId
      target_field: cyera.audit.vendor_id
      ignore_missing: true
  - script:
      lang: painless
      tag: script_set_ecs_categorization
      description: Set ECS categorization fields based on the audit action.
      if: ctx.event?.action != null
      params:
        exact:
          User logged in:
            category:
              - authentication
              - session
            type:
              - start
            outcome: success
          User logged out:
            category:
              - authentication
              - session
            type:
              - end
            outcome: success
          Created API key:
            category:
              - iam
            type:
              - creation
          Created personal API key:
            category:
              - iam
            type:
              - creation
          Deleted API key:
            category:
              - iam
            type:
              - deletion
          Deleted personal API key:
            category:
              - iam
            type:
              - deletion
          Added user:
            category:
              - iam
            type:
              - user
              - creation
          Invited user:
            category:
              - iam
            type:
              - user
              - creation
          Accepted invitation:
            category:
              - iam
            type:
              - user
              - info
          Assigned roles:
            category:
              - iam
            type:
              - user
              - change
          Unassigned roles:
            category:
              - iam
            type:
              - user
              - change
          Updated profile:
            category:
              - iam
            type:
              - user
              - change
          Activated account:
            category:
              - iam
            type:
              - user
              - change
          Enabled MFA:
            category:
              - iam
            type:
              - user
              - change
          Disabled MFA:
            category:
              - iam
            type:
              - user
              - change
      source: |-
        String action = ctx.event.action;
        Map entry = params.exact.get(action);
        if (entry == null) {
          String a = action.toLowerCase();
          if (a.contains('logged in') || a.contains('login') || a.contains('log in')) {
            entry = ['category': ['authentication', 'session'], 'type': ['start'], 'outcome': a.contains('fail') ? 'failure' : 'success'];
          } else if (a.contains('logged out') || a.contains('logout') || a.contains('log out')) {
            entry = ['category': ['authentication', 'session'], 'type': ['end'], 'outcome': 'success'];
          } else if (a.startsWith('removed user') || a.startsWith('deleted user')) {
            entry = ['category': ['iam'], 'type': ['user', 'deletion']];
          } else if (a.startsWith('added user') || a.startsWith('invited user')) {
            entry = ['category': ['iam'], 'type': ['user', 'creation']];
          }
        }
        if (entry == null) {
          return;
        }
        if (entry.category != null) {
          ctx.event.category = entry.category;
        }
        if (entry.type != null) {
          ctx.event.type = entry.type;
        }
        if (entry.outcome != null) {
          ctx.event.outcome = entry.outcome;
        }
  - remove:
      field:
        - cyera.audit.action
        - cyera.audit.created_at
        - cyera.audit.description
        - cyera.audit.email
        - cyera.audit.frontegg_id
        - cyera.audit.ip
        - cyera.audit.severity
        - cyera.audit.user_agent
      tag: remove_custom_duplicate_fields
      ignore_missing: true
      if: ctx.tags == null || !ctx.tags.contains('preserve_duplicate_custom_fields')
  - remove:
      field: json
      tag: remove_json
      ignore_missing: true
  - script:
      tag: script_to_drop_null_values
      lang: painless
      description: This script processor iterates over the whole document to remove fields with null values.
      source: |-
        void handleMap(Map map) {
          map.values().removeIf(v -> {
            if (v instanceof Map) {
              handleMap(v);
            } else if (v instanceof List) {
              handleList(v);
            }
            return v == null || v == '' || (v instanceof Map && v.size() == 0) || (v instanceof List && v.size() == 0)
          });
        }
        void handleList(List list) {
          list.removeIf(v -> {
            if (v instanceof Map) {
              handleMap(v);
            } else if (v instanceof List) {
              handleList(v);
            }
            return v == null || v == '' || (v instanceof Map && v.size() == 0) || (v instanceof List && v.size() == 0)
          });
        }
        handleMap(ctx);
  - set:
      field: event.kind
      tag: set_pipeline_error_into_event_kind
      value: pipeline_error
      if: ctx.error?.message != null
  - append:
      field: tags
      value: preserve_original_event
      allow_duplicates: false
      if: ctx.error?.message != null
on_failure:
  - append:
      field: error.message
      value: |-
        Processor '{{{ _ingest.on_failure_processor_type }}}'
        {{{#_ingest.on_failure_processor_tag}}}with tag '{{{ _ingest.on_failure_processor_tag }}}'
        {{{/_ingest.on_failure_processor_tag}}}failed with message '{{{ _ingest.on_failure_message }}}'
  - set:
      field: event.kind
      tag: set_pipeline_error_to_event_kind
      value: pipeline_error
  - append:
      field: tags
      value: preserve_original_event
      allow_duplicates: false
