paths:
{{#each paths as |path i|}}
  - {{path}}
{{/each}}
tags: {{tags}}
publisher_pipeline.disable_host: {{nList tags forwarded"}}
processors:
  - add_fields:
      target: ''
      fields:
        ecs.version: 1.10.0