service: bbot-all-logfile
input: logfile
policy_template: bbot
data_stream:
  vars:
    paths:
      - "{{SERVICE_LOGS_DIR}}/bbot-v*-logfile*.log"
    preserve_original_event: true
    preserve_duplicate_custom_fields: true
    tags:
      - forwarded
      - bbot