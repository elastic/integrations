service: exporter
vars:
  host_sync_gateway:
    - http://Administrator:password@{{Hostname}}:{{Port[0]}}
    - http://{{Hostname}}:{{Port[0]}}
  username: Administrator
  password: password  
input: prometheus/metrics
data_stream:
  vars: ~
