service: exporter
vars:
  host_sync_gateway:
    - http://Administrator:password@{{Hostname}}:{{Port}}
    - http://{{Hostname}}:{{Port}}
  username: Administrator
  password: password  
input: prometheus/metrics
data_stream:
  vars: ~
