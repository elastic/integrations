metricsets: ["collector"]
hosts:
{{#each host_sync_gateway}}
  - {{this}}
{{/each}}
period: {{period}}
metrics_path: /metrics
metrics_filters:
  include: ["sgw_cache*"]
{{#if ssl_sync_gateway}}
ssl:
  {{ssl_sync_gateway}}
{{/if}}
tags:
{{#each tags as |tag|}}
  - {{tag}}
{{/each}}
{{#if processors}}
processors:
{{processors}}
{{/if}}
