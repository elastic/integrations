receivers:
  nginx:
    endpoint: {{nginx_endpoint}}
    collection_interval: {{period}}
processors:
  resourcedetection:
    detectors: ["system", "ec2"]
service:
  pipelines:
    metrics:
      receivers: [nginx]
      processors: [resourcedetection]
