<!-- NOTICE: Do not edit this file manually.-->
<!-- This file is automatically generated by Elastic Package -->

# HarfangLab EDR Integration for Elastic

## Overview

The HarfangLab EDR integration collects security telemetry from [HarfangLab](https://www.harfanglab.io/) (Hurukai) Endpoint Detection and Response. It supports two collection methods that can be enabled independently or in parallel:

- **REST API** — Elastic Agent polls the HarfangLab Manager REST API for **alerts**, **threats**, and **agent inventory** using a static API token.
- **Syslog** — Elastic Agent listens for RFC 5424 messages with a JSON body forwarded by the HarfangLab Syslog connector, covering 14 vendor `log_type` values (alerts, threats, agent events, process, network, file, driverload, eventlog, auditlog, connectionlog, injectedthread, investigation, fallback for unknown types).

Events are normalized to the Elastic Common Schema (ECS) by the integration's ingest pipelines, with vendor-specific fields preserved under `harfang_labs.*` for forensic context.

### Compatibility

This integration has been tested against the HarfangLab REST API as exposed by Manager versions `9.x` (API path prefix `/api/data/alert/alert/` and `/api/data/endpoint/`) and the HarfangLab Syslog connector emitting RFC 5424 with a JSON payload.

It is compatible with Elastic stack `8.19+` and `9.1+` (see manifest `conditions.kibana`).

### How it works

For the **API** policy template, the `cel` input authenticates with a static `Token` header and walks the standard Django REST Framework paginated endpoints (`?offset=...&limit=...&ordering=...`). Each data stream persists a `last_seen` cursor (latest `alert_time` / `creation_date` / `lastseen` in the returned page) and resumes from it on the next poll. Three streams run in parallel with independent intervals: `alert` (1m default), `threat` (1m), and `agent` (5m).

For the **Syslog** policy template, the `tcp` and `udp` inputs receive raw RFC 5424 lines, the agent's `syslog` processor extracts the header into `log.syslog.*` and exposes the JSON body in `message`. The default ingest pipeline parses the JSON, reads `log_type`, and routes events to a per-type sub-pipeline that performs the ECS mapping.

## What data does this integration collect?

The HarfangLab EDR integration collects events of the following types:

- **Alerts** (REST API `alert` and Syslog `log_type: alert`): YARA, Sigma, HLAI, IOC, ransomware, antivirus and other detections including alert metadata, agent context, process / file / network indicators and matched rules.
- **Threats** (REST API `threat` and Syslog `log_type: threat`): correlated incidents aggregating related alerts, impacted agents and users, MITRE ATT&CK tactics, and the top contributing rules.
- **Agent inventory** (REST API `agent` and Syslog `log_type: agent`/`agentlog`): EDR agent state (status, last_seen, OS, IP) for asset visibility and posture monitoring.
- **Endpoint telemetry** (Syslog only): process events, network connections, file operations, driver loads, Windows event logs, audit logs, injected thread detections, investigation actions, and uncategorized telemetry routed through a fallback pipeline.

### Supported use cases

- Security operations: triage HarfangLab alerts in Elastic Security with full ECS categorization and correlation against host, user, and process context.
- Threat hunting: search vendor-specific endpoint telemetry (process trees, hash matches, signature data) using ES|QL or Discover.
- Asset visibility: track HarfangLab-managed endpoints, their last check-in time, OS, and connectivity status.
- Compliance / audit: retain HarfangLab audit logs and authentication events alongside the rest of your security data.

## What do I need to use this integration?

You need:

- A running Elastic stack (`8.19+` or `9.1+`) — self-managed, Elastic Cloud, or Elastic Cloud Serverless.
- A HarfangLab Manager reachable from the host that runs Elastic Agent.
- For the **API** template: a static API token generated by an administrator from **Administration → Users → Generate token** in the HarfangLab Manager.
- For the **Syslog** template: the HarfangLab **Syslog connector** configured to forward events to the host running Elastic Agent over TCP or UDP.

## How do I deploy this integration?

### Agent-based deployment

Elastic Agent must be installed on a host that can reach (for the API) or be reached by (for syslog) the HarfangLab Manager. Follow the [installation instructions](https://www.elastic.co/guide/en/fleet/current/elastic-agent-installation.html). You can install only one Elastic Agent per host.

### Set up steps in HarfangLab

#### For the API policy template

1. Sign in to the HarfangLab Manager as an administrator.
2. Go to **Administration → Users**.
3. Click **Generate token** for a service account that has read access to alerts, threats, and endpoint inventory.
4. Copy the generated token; it will not be displayed again.
5. Note the full Manager URL including scheme and port (for example `https://tenant.example.com:8443`).

#### For the Syslog policy template

1. Sign in to the HarfangLab Manager as an administrator.
2. Go to **Administration → Connectors → Syslog**.
3. Add a new Syslog connector targeting the Elastic Agent host on TCP `9514` (or UDP `9515`).
4. Set the protocol to RFC 5424 with the JSON payload format.
5. Select which event types should be forwarded (alerts, threats, telemetry, etc.).

#### Vendor resources

- [HarfangLab documentation](https://docs.harfanglab.io/)
- [HarfangLab Manager API reference](https://docs.harfanglab.io/api/)
- [HarfangLab Syslog connector guide](https://docs.harfanglab.io/connectors/)

### Set up steps in Kibana

1. In Kibana, go to **Integrations** and search for **HarfangLab EDR**. Click **Add HarfangLab EDR**.
2. Choose a policy template:
   - **HarfangLab EDR (API)** — paste the Manager URL and the API token. Optionally adjust the per-stream polling interval (alerts, threats, agents) and the initial backfill window (`initial_interval`).
   - **HarfangLab EDR (Syslog)** — set the bind address (default `0.0.0.0`) and listen port (`9514` for TCP, `9515` for UDP). Enable TLS by populating the `ssl` block for TCP if your HarfangLab connector forwards over TLS.
3. Assign the integration to an agent policy that targets the host running Elastic Agent.
4. Save and deploy.

### Validation

1. In Kibana, open **Discover** and select one of the following data views:
   - `logs-harfang_labs.alert-*`
   - `logs-harfang_labs.threat-*`
   - `logs-harfang_labs.agent-*`
   - `logs-harfang_labs.event-*`
2. Confirm documents are flowing in. The HarfangLab Manager's UI counts should align with the indexed document counts over a comparable time window.
3. For the API template, the **Fleet** UI's per-agent logs will show CEL request activity (one HTTP request per polling round, with the persisted cursor).

## Troubleshooting

- No data on the API streams: verify the API token is valid (HarfangLab Manager → Administration → Users → token status). Confirm that the Manager URL is reachable from the agent host and that the agent's network can establish TLS to the configured port.
- 401 / 403 responses: regenerate the API token in HarfangLab and update the integration's `api_token` variable.
- API pagination loops or duplicate events: confirm the data stream's `initial_interval` is reasonable for your retention and is not set to a window longer than the API allows. Each stream tracks its own `last_seen` cursor.
- TCP syslog: no connection from HarfangLab: confirm the listener binds to `0.0.0.0` (default), the firewall on the agent host allows inbound TCP `9514`, and the HarfangLab Syslog connector uses framing `delimiter` with line delimiter `\n` (the default RFC 5424 over TCP framing). If TLS is enabled, the connector's CA must match the agent's `ssl.certificate_authorities`.
- UDP syslog: messages larger than `max_message_size` (default `10KiB`) are silently truncated. If HarfangLab emits oversized events (some thread / dump events contain large binary blobs), switch the connector to TCP.
- Pipeline errors visible in the `event.kind: pipeline_error` documents: search for `tags: preserve_original_event` to find affected events; the original payload is preserved in `event.original` for replay.

## Performance and scaling

For very high-volume HarfangLab deployments, prefer the syslog `tcp` input over `udp` so the kernel applies back-pressure and oversized messages are not truncated.

For very large endpoint inventories, increase the `agent` stream's polling `interval` (default `5m`) and reduce its `page_size` (default `100`) to reduce Manager API load.

The integration is stateless on the Elasticsearch side; standard data stream ILM, index lifecycle, and snapshot policies apply.

For more information on architectures that can be used for scaling this integration, check the [Ingest Architectures](https://www.elastic.co/docs/manage-data/ingest/ingest-reference-architectures) documentation.

## Reference

### Inputs used

These inputs can be used with this integration:
<details>
<summary>cel</summary>

## Setup

For more details about the CEL input settings, check the [Filebeat documentation](https://www.elastic.co/guide/en/beats/filebeat/current/filebeat-input-cel.html).

Before configuring the CEL input, make sure you have:
- Network connectivity to the target API endpoint
- Valid authentication credentials (API keys, tokens, or certificates as required)
- Appropriate permissions to read from the target data source

### Collecting logs from CEL

To configure the CEL input, you must specify the `request.url` value pointing to the API endpoint. The interval parameter controls how frequently requests are made and is the primary way to balance data freshness with API rate limits and costs. Authentication is often configured through the `request.headers` section using the appropriate method for the service.

NOTE: To access the API service, make sure you have the necessary API credentials and that the Filebeat instance can reach the endpoint URL. Some services may require IP whitelisting or VPN access.

To collect logs via API endpoint, configure the following parameters:

- API Endpoint URL
- API credentials (tokens, keys, or username/password)
- Request interval (how often to fetch data)
</details>
<details>
<summary>tcp</summary>

## Setup

For more details about the TCP input settings, check the [Filebeat documentation](https://www.elastic.co/docs/reference/beats/filebeat/filebeat-input-tcp).

### Collecting logs from TCP

To collect logs via TCP, select **Collect logs via TCP** and configure the following parameters:

**Required Settings:**
- Host
- Port

**Common Optional Settings:**
- Max Message Size - Maximum size of incoming messages
- Max Connections - Maximum number of concurrent connections
- Timeout - How long to wait for data before closing idle connections
- Line Delimiter - Character(s) that separate log messages

## SSL/TLS Configuration

To enable encrypted connections, configure the following SSL settings:

**SSL Settings:**
- Enable SSL - Toggle to enable SSL/TLS encryption
- Certificate - Path to the SSL certificate file (`.crt` or `.pem`)
- Certificate Key - Path to the private key file (`.key`)
- Certificate Authorities - Path to CA certificate file for client certificate validation (optional)
- Client Authentication - Require client certificates (`none`, `optional`, or `required`)
- Supported Protocols - TLS versions to support (e.g., `TLSv1.2`, `TLSv1.3`)

**Example SSL Configuration:**
```yaml
ssl.enabled: true
ssl.certificate: "/path/to/server.crt"
ssl.key: "/path/to/server.key"
ssl.certificate_authorities: ["/path/to/ca.crt"]
ssl.client_authentication: "optional"
```
</details>
<details>
<summary>udp</summary>

## Setup

For more details about the UDP input settings, check the [Filebeat documentation](https://www.elastic.co/docs/reference/beats/filebeat/filebeat-input-udp).

### Collecting logs from UDP

To collect logs via UDP, select **Collect logs via UDP** and configure the following parameters:

**Required Settings:**
- Host
- Port

**Common Optional Settings:**
- Max Message Size - Maximum size of UDP packets to accept (default: 10KB, max: 64KB)
- Read Buffer - UDP socket read buffer size for handling bursts of messages
- Read Timeout - How long to wait for incoming packets before checking for shutdown
</details>


### API usage

These HarfangLab REST API endpoints are polled by the integration:

- `GET /api/version` — health check on initial poll.
- `GET /api/data/alert/alert/Alert/` — alert listing with filters (`status`, `alert_type`, `level`, `alert_time__gte`, `ordering`).
- `GET /api/data/alert/alert/Threat/` — threat listing with filters (`status`, `level`, `creation_date__gte`, `ordering`).
- `GET /api/data/endpoint/Agent/` — agent inventory listing with filters (`status`, `lastseen__gte`, `ordering`).

All endpoints follow Django REST Framework pagination (`count`, `next`, `previous`, `results`).

### Vendor documentation links

- [HarfangLab documentation portal](https://docs.harfanglab.io/)
- [HarfangLab REST API reference](https://docs.harfanglab.io/api/)
- [HarfangLab Syslog connector reference](https://docs.harfanglab.io/connectors/)

### Data streams

#### alert

The `alert` data stream collects security alerts (YARA, Sigma, HLAI, IOC, ransomware, antivirus, etc.) from the HarfangLab REST API.

##### alert fields

**Exported fields**

| Field | Description | Type |
|---|---|---|
| @timestamp | Date/time when the event originated. This is the date/time extracted from the event, typically representing when the event was generated by the source. If the event source has no original timestamp, this value is typically populated by the first time the event was received by the pipeline. Required field for all events. | date |
| agent.id | Unique identifier of this agent (if one exists). Example: For Beats this would be beat.id. | keyword |
| data_stream.dataset | The field can contain anything that makes sense to signify the source of the data. Examples include `nginx.access`, `prometheus`, `endpoint` etc. For data streams that otherwise fit, but that do not have dataset set we use the value "generic" for the dataset value. `event.dataset` should have the same value as `data_stream.dataset`. Beyond the Elasticsearch data stream naming criteria noted above, the `dataset` value has additional restrictions:   \* Must not contain `-`   \* No longer than 100 characters | constant_keyword |
| data_stream.namespace | A user defined namespace. Namespaces are useful to allow grouping of data. Many users already organize their indices this way, and the data stream naming scheme now provides this best practice as a default. Many users will populate this field with `default`. If no value is used, it falls back to `default`. Beyond the Elasticsearch index naming criteria noted above, `namespace` value has the additional restrictions:   \* Must not contain `-`   \* No longer than 100 characters | constant_keyword |
| data_stream.type | An overarching type for the data stream. Currently allowed values are "logs" and "metrics". We expect to also add "traces" and "synthetics" in the near future. | constant_keyword |
| ecs.version | ECS version this event conforms to. `ecs.version` is a required field and must exist in all events. When querying across multiple indices -- which may conform to slightly different ECS versions -- this field lets integrations adjust to the schema version of the events. | keyword |
| error.message | Error message. | match_only_text |
| event.category | This is one of four ECS Categorization Fields, and indicates the second level in the ECS category hierarchy. `event.category` represents the "big buckets" of ECS categories. For example, filtering on `event.category:process` yields all events relating to process activity. This field is closely related to `event.type`, which is used as a subcategory. This field is an array. This will allow proper categorization of some events that fall in multiple categories. | keyword |
| event.dataset | Name of the dataset. If an event source publishes more than one type of log or events (e.g. access log, error log), the dataset is used to specify which one the event comes from. It's recommended but not required to start the dataset name with the module name, followed by a dot, then the dataset name. | constant_keyword |
| event.end | `event.end` contains the date when the event ended or when the activity was last observed. | date |
| event.id | Unique ID to describe the event. | keyword |
| event.kind | This is one of four ECS Categorization Fields, and indicates the highest level in the ECS category hierarchy. `event.kind` gives high-level information about what type of information the event contains, without being specific to the contents of the event. For example, values of this field distinguish alert events from metric events. The value of this field can be used to inform how these kinds of events should be handled. They may warrant different retention, different access control, it may also help understand whether the data is coming in at a regular interval or not. | keyword |
| event.module | Name of the module this data is coming from. If your monitoring agent supports the concept of modules or plugins to process events of a given source (e.g. Apache logs), `event.module` should contain the name of this module. | constant_keyword |
| event.original | Raw text message of entire event. Used to demonstrate log integrity or where the full log message (before splitting it up in multiple parts) may be required, e.g. for reindex. This field is not indexed and doc_values are disabled. It cannot be searched, but it can be retrieved from `_source`. If users wish to override this and index this field, please see `Field data types` in the `Elasticsearch Reference`. | keyword |
| event.outcome | This is one of four ECS Categorization Fields, and indicates the lowest level in the ECS category hierarchy. `event.outcome` simply denotes whether the event represents a success or a failure from the perspective of the entity that produced the event. Note that when a single transaction is described in multiple events, each event may populate different values of `event.outcome`, according to their perspective. Also note that in the case of a compound event (a single event that contains multiple logical events), this field should be populated with the value that best captures the overall success or failure from the perspective of the event producer. Further note that not all events will have an associated outcome. For example, this field is generally not populated for metric events, events with `event.type:info`, or any events for which an outcome does not make logical sense. | keyword |
| event.severity | The numeric severity of the event according to your event source. What the different severity values mean can be different between sources and use cases. It's up to the implementer to make sure severities are consistent across events from the same source. The Syslog severity belongs in `log.syslog.severity.code`. `event.severity` is meant to represent the severity according to the event source (e.g. firewall, IDS). If the event source does not publish its own severity, you may optionally copy the `log.syslog.severity.code` to `event.severity`. | long |
| event.start | `event.start` contains the date when the event started or when the activity was first observed. | date |
| event.type | This is one of four ECS Categorization Fields, and indicates the third level in the ECS category hierarchy. `event.type` represents a categorization "sub-bucket" that, when used along with the `event.category` field values, enables filtering events down to a level appropriate for single visualization. This field is an array. This will allow proper categorization of some events that fall in multiple event types. | keyword |
| harfang_labs.agent.version | HarfangLab agent version at time of detection. | keyword |
| harfang_labs.alert.aggregation_key | Alert deduplication key. | keyword |
| harfang_labs.alert.eventlog.channel |  | keyword |
| harfang_labs.alert.eventlog.event_data |  | flattened |
| harfang_labs.alert.eventlog.event_id |  | keyword |
| harfang_labs.alert.eventlog.source_name |  | keyword |
| harfang_labs.alert.execution_mode | Execution mode indicator. | long |
| harfang_labs.alert.id | HarfangLab alert UUID. | keyword |
| harfang_labs.alert.level | Raw alert severity level string. | keyword |
| harfang_labs.alert.logonid | Windows logon identifier. | long |
| harfang_labs.alert.matched_rules.namespace |  | keyword |
| harfang_labs.alert.matched_rules.rulename |  | keyword |
| harfang_labs.alert.matched_rules.source |  | keyword |
| harfang_labs.alert.maturity | Rule maturity level. | keyword |
| harfang_labs.alert.payload_type | Payload type indicator (e.g. rtlogs). | keyword |
| harfang_labs.alert.process.integrity_level | Process integrity level. | keyword |
| harfang_labs.alert.process.issuer_name | Code signature issuer name when present. | keyword |
| harfang_labs.alert.process.product_version | PE product version from compile-time metadata. | keyword |
| harfang_labs.alert.process.size | Process executable size in bytes. | long |
| harfang_labs.alert.score | YARA or detection score. | long |
| harfang_labs.alert.session_id | Windows session identifier. | long |
| harfang_labs.alert.status | Alert workflow status. | keyword |
| harfang_labs.alert.subtype | Alert subtype (process, file, eventlog, etc.). | keyword |
| harfang_labs.alert.threat_key | Parent threat identifier. | long |
| harfang_labs.alert.type | Detection engine type (sigma, yara, hlai, ioc, etc.). | keyword |
| host.domain | Name of the domain of which the host is a member. For example, on Windows this could be the host's Active Directory domain or NetBIOS domain name. For Linux this could be the domain of the host's LDAP provider. | keyword |
| host.hostname | Hostname of the host. It normally contains what the `hostname` command returns on the host machine. | keyword |
| host.ip | Host ip addresses. | ip |
| host.name | Name of the host. It can contain what hostname returns on Unix systems, the fully qualified domain name (FQDN), or a name specified by the user. The recommended value is the lowercase FQDN of the host. | keyword |
| host.os.full | Operating system name, including the version or code name. | keyword |
| host.os.full.text | Multi-field of `host.os.full`. | match_only_text |
| host.os.platform | Operating system platform (such centos, ubuntu, windows). | keyword |
| host.os.version | Operating system version as a raw string. | keyword |
| input.type | Input type. | keyword |
| message | For log events the message field contains the log message, optimized for viewing in a log viewer. For structured logs without an original message field, other fields can be concatenated to form a human-readable summary of the event. If multiple messages exist, they can be combined into one message. | match_only_text |
| observer.type | The type of the observer the data is coming from. There is no predefined list of observer types. Some examples are `forwarder`, `firewall`, `ids`, `ips`, `proxy`, `poller`, `sensor`, `APM server`. | keyword |
| organization.id | Unique identifier for the organization. | keyword |
| process.code_signature.exists | Boolean to capture if a signature is present. | boolean |
| process.code_signature.subject_name | Subject name of the code signer | keyword |
| process.code_signature.valid | Boolean to capture if the digital signature is verified against the binary content. Leave unpopulated if a certificate was unchecked. | boolean |
| process.command_line | Full command line that started the process, including the absolute path to the executable, and all arguments. Some arguments may be filtered to protect sensitive information. | wildcard |
| process.command_line.text | Multi-field of `process.command_line`. | match_only_text |
| process.entity_id | Unique identifier for the process. The implementation of this is specified by the data source, but some examples of what could be used here are a process-generated UUID, Sysmon Process GUIDs, or a hash of some uniquely identifying components of a process. Constructing a globally unique identifier is a common practice to mitigate PID reuse as well as to identify a specific process over time, across multiple monitored hosts. | keyword |
| process.executable | Absolute path to the process executable. | keyword |
| process.executable.text | Multi-field of `process.executable`. | match_only_text |
| process.hash.md5 | MD5 hash. | keyword |
| process.hash.sha1 | SHA1 hash. | keyword |
| process.hash.sha256 | SHA256 hash. | keyword |
| process.name | Process name. Sometimes called program name or similar. | keyword |
| process.name.text | Multi-field of `process.name`. | match_only_text |
| process.parent.command_line | Full command line that started the process, including the absolute path to the executable, and all arguments. Some arguments may be filtered to protect sensitive information. | wildcard |
| process.parent.command_line.text | Multi-field of `process.parent.command_line`. | match_only_text |
| process.parent.entity_id | Unique identifier for the process. The implementation of this is specified by the data source, but some examples of what could be used here are a process-generated UUID, Sysmon Process GUIDs, or a hash of some uniquely identifying components of a process. Constructing a globally unique identifier is a common practice to mitigate PID reuse as well as to identify a specific process over time, across multiple monitored hosts. | keyword |
| process.parent.executable | Absolute path to the process executable. | keyword |
| process.parent.executable.text | Multi-field of `process.parent.executable`. | match_only_text |
| process.parent.pid | Process id. | long |
| process.pe.company | Internal company name of the file, provided at compile-time. | keyword |
| process.pe.description | Internal description of the file, provided at compile-time. | keyword |
| process.pe.file_version | Internal version of the file, provided at compile-time. | keyword |
| process.pe.imphash | A hash of the imports in a PE file. An imphash -- or import hash -- can be used to fingerprint binaries even after recompilation or other code-level transformations have occurred, which would change more traditional hash values. Learn more at https://www.fireeye.com/blog/threat-research/2014/01/tracking-malware-import-hashing.html. | keyword |
| process.pe.original_file_name | Internal name of the file, provided at compile-time. | keyword |
| process.pe.product | Internal product name of the file, provided at compile-time. | keyword |
| process.pid | Process id. | long |
| process.start | The time the process started. | date |
| process.working_directory | The working directory of the process. | keyword |
| process.working_directory.text | Multi-field of `process.working_directory`. | match_only_text |
| related.hash | All the hashes seen on your event. Populating this field, then using it to search for hashes can help in situations where you're unsure what the hash algorithm is (and therefore which key name to search). | keyword |
| related.hosts | All hostnames or other host identifiers seen on your event. Example identifiers include FQDNs, domain names, workstation names, or aliases. | keyword |
| related.ip | All of the IPs seen on your event. | ip |
| related.user | All the user names or other user identifiers seen on the event. | keyword |
| rule.category | A categorization value keyword used by the entity using the rule for detection of this event. | keyword |
| rule.id | A rule ID that is unique within the scope of an agent, observer, or other entity using the rule for detection of this event. | keyword |
| rule.name | The name of the rule or signature generating the event. | keyword |
| rule.version | The version / revision of the rule being used for analysis. | keyword |
| tags | List of keywords used to tag each event. | keyword |
| user.domain | Name of the directory the user is a member of. For example, an LDAP or Active Directory domain name. | keyword |
| user.id | Unique identifier of the user. | keyword |
| user.name | Short name or login of the user. | keyword |
| user.name.text | Multi-field of `user.name`. | match_only_text |


##### alert sample event

An example event for `alert` looks as following:

```json
{
    "@timestamp": "2024-06-15T10:21:20.704Z",
    "agent": {
        "ephemeral_id": "735e9ee6-8aa6-423f-bbc7-15c458c96830",
        "id": "11111111-1111-1111-1111-111111111111",
        "name": "elastic-agent-66629",
        "type": "filebeat",
        "version": "9.4.1"
    },
    "data_stream": {
        "dataset": "harfang_labs.alert",
        "namespace": "98320",
        "type": "logs"
    },
    "ecs": {
        "version": "9.3.0"
    },
    "elastic_agent": {
        "id": "a025606b-6b28-41e4-a9ef-e084c1da5173",
        "snapshot": false,
        "version": "9.4.1"
    },
    "event": {
        "agent_id_status": "mismatch",
        "category": [
            "malware",
            "intrusion_detection",
            "process"
        ],
        "dataset": "harfang_labs.alert",
        "end": "2024-06-15T10:22:01.000Z",
        "id": "b2c3d4e5-f6a7-8901-bcde-f12345678901",
        "ingested": "2026-05-25T11:09:09Z",
        "kind": "alert",
        "module": "harfang_labs",
        "original": "{\"agent\":{\"agentid\":\"11111111-1111-1111-1111-111111111111\",\"dnsdomainname\":\"example.local\",\"domainname\":\"example.com\",\"hostname\":\"ws01.example.com\",\"ipaddress\":\"198.51.100.10\",\"osproducttype\":\"Windows 10 Pro\",\"ostype\":\"windows\",\"osversion\":\"10.0.19041\",\"version\":\"4.9.8\"},\"alert_subtype\":\"process\",\"alert_time\":\"2024-06-15T10:21:20.704000Z\",\"alert_type\":\"yara\",\"alert_unique_id\":\"b2c3d4e5-f6a7-8901-bcde-f12345678901\",\"detection_origin\":\"agent\",\"execution\":0,\"id\":\"a1b2c3d4-e5f6-7890-abcd-ef1234567890\",\"last_update\":\"2024-06-15T10:22:01.000000Z\",\"level\":\"high\",\"maturity\":\"stable\",\"msg\":\"Binary was found malicious by 2 rule(s) with a score of 120\",\"process\":{\"commandline\":\"C:\\\\Program Files\\\\Example\\\\suspicious.exe -arg\",\"hashes\":{\"md5\":\"d41d8cd98f00b204e9800998ecf8427e\",\"sha1\":\"356a192b7913b04c54574d18c28d46e6395428ab\",\"sha256\":\"e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855\"},\"image_name\":\"C:\\\\Program Files\\\\Example\\\\suspicious.exe\",\"integrity_level\":\"High\",\"parent_commandline\":\"\\\"C:\\\\Windows\\\\System32\\\\WindowsPowerShell\\\\v1.0\\\\powershell.exe\\\"\",\"parent_image\":\"C:\\\\Windows\\\\System32\\\\WindowsPowerShell\\\\v1.0\\\\powershell.exe\",\"pid\":5736,\"ppid\":1700,\"process_name\":\"suspicious.exe\",\"process_unique_id\":\"22222222-2222-2222-2222-222222222222\",\"signed\":false,\"username\":\"EXAMPLE\\\\jdoe\",\"usersid\":\"S-1-5-21-0000000000-0000000000-0000000000-1100\"},\"rule_id\":\"YARA binary check\",\"rule_name\":\"YARA binary check\",\"status\":\"new\",\"tags\":[\"investigation\"],\"tenant\":\"tenant-example\",\"threat_key\":42}",
        "outcome": "unknown",
        "severity": 75,
        "start": "2024-06-15T10:21:20.704Z",
        "type": [
            "info",
            "start"
        ]
    },
    "harfang_labs": {
        "agent": {
            "version": "4.9.8"
        },
        "alert": {
            "execution_mode": 0,
            "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
            "level": "high",
            "maturity": "stable",
            "process": {
                "integrity_level": "High"
            },
            "status": "new",
            "subtype": "process",
            "threat_key": 42,
            "type": "yara"
        }
    },
    "host": {
        "domain": "example.local",
        "hostname": "ws01.example.com",
        "ip": [
            "198.51.100.10"
        ],
        "name": "ws01.example.com",
        "os": {
            "full": "Windows 10 Pro",
            "platform": "windows",
            "version": "10.0.19041"
        }
    },
    "input": {
        "type": "cel"
    },
    "message": "Binary was found malicious by 2 rule(s) with a score of 120",
    "observer": {
        "type": "agent"
    },
    "organization": {
        "id": "tenant-example"
    },
    "process": {
        "code_signature": {
            "valid": false
        },
        "command_line": "C:\\Program Files\\Example\\suspicious.exe -arg",
        "entity_id": "22222222-2222-2222-2222-222222222222",
        "executable": "C:\\Program Files\\Example\\suspicious.exe",
        "hash": {
            "md5": "d41d8cd98f00b204e9800998ecf8427e",
            "sha1": "356a192b7913b04c54574d18c28d46e6395428ab",
            "sha256": "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
        },
        "name": "suspicious.exe",
        "parent": {
            "command_line": "\"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe\"",
            "executable": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe",
            "pid": 1700
        },
        "pid": 5736
    },
    "related": {
        "hash": [
            "d41d8cd98f00b204e9800998ecf8427e",
            "356a192b7913b04c54574d18c28d46e6395428ab",
            "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
        ],
        "hosts": [
            "ws01.example.com"
        ],
        "ip": [
            "198.51.100.10"
        ],
        "user": [
            "jdoe"
        ]
    },
    "rule": {
        "category": "yara",
        "id": "YARA binary check",
        "name": "YARA binary check"
    },
    "tags": [
        "preserve_original_event",
        "forwarded",
        "investigation"
    ],
    "user": {
        "domain": "EXAMPLE",
        "id": "S-1-5-21-0000000000-0000000000-0000000000-1100",
        "name": "jdoe"
    }
}
```

#### threat

The `threat` data stream collects correlated threats (groups of related alerts) from the HarfangLab REST API, including impacted agents/users, MITRE ATT&CK tactics, and top contributing rules.

##### threat fields

**Exported fields**

| Field | Description | Type |
|---|---|---|
| @timestamp | Date/time when the event originated. This is the date/time extracted from the event, typically representing when the event was generated by the source. If the event source has no original timestamp, this value is typically populated by the first time the event was received by the pipeline. Required field for all events. | date |
| data_stream.dataset | The field can contain anything that makes sense to signify the source of the data. Examples include `nginx.access`, `prometheus`, `endpoint` etc. For data streams that otherwise fit, but that do not have dataset set we use the value "generic" for the dataset value. `event.dataset` should have the same value as `data_stream.dataset`. Beyond the Elasticsearch data stream naming criteria noted above, the `dataset` value has additional restrictions:   \* Must not contain `-`   \* No longer than 100 characters | constant_keyword |
| data_stream.namespace | A user defined namespace. Namespaces are useful to allow grouping of data. Many users already organize their indices this way, and the data stream naming scheme now provides this best practice as a default. Many users will populate this field with `default`. If no value is used, it falls back to `default`. Beyond the Elasticsearch index naming criteria noted above, `namespace` value has the additional restrictions:   \* Must not contain `-`   \* No longer than 100 characters | constant_keyword |
| data_stream.type | An overarching type for the data stream. Currently allowed values are "logs" and "metrics". We expect to also add "traces" and "synthetics" in the near future. | constant_keyword |
| ecs.version | ECS version this event conforms to. `ecs.version` is a required field and must exist in all events. When querying across multiple indices -- which may conform to slightly different ECS versions -- this field lets integrations adjust to the schema version of the events. | keyword |
| error.message | Error message. | match_only_text |
| event.category | This is one of four ECS Categorization Fields, and indicates the second level in the ECS category hierarchy. `event.category` represents the "big buckets" of ECS categories. For example, filtering on `event.category:process` yields all events relating to process activity. This field is closely related to `event.type`, which is used as a subcategory. This field is an array. This will allow proper categorization of some events that fall in multiple categories. | keyword |
| event.dataset | Name of the dataset. If an event source publishes more than one type of log or events (e.g. access log, error log), the dataset is used to specify which one the event comes from. It's recommended but not required to start the dataset name with the module name, followed by a dot, then the dataset name. | constant_keyword |
| event.end | `event.end` contains the date when the event ended or when the activity was last observed. | date |
| event.id | Unique ID to describe the event. | keyword |
| event.kind | This is one of four ECS Categorization Fields, and indicates the highest level in the ECS category hierarchy. `event.kind` gives high-level information about what type of information the event contains, without being specific to the contents of the event. For example, values of this field distinguish alert events from metric events. The value of this field can be used to inform how these kinds of events should be handled. They may warrant different retention, different access control, it may also help understand whether the data is coming in at a regular interval or not. | keyword |
| event.module | Name of the module this data is coming from. If your monitoring agent supports the concept of modules or plugins to process events of a given source (e.g. Apache logs), `event.module` should contain the name of this module. | constant_keyword |
| event.original | Raw text message of entire event. Used to demonstrate log integrity or where the full log message (before splitting it up in multiple parts) may be required, e.g. for reindex. This field is not indexed and doc_values are disabled. It cannot be searched, but it can be retrieved from `_source`. If users wish to override this and index this field, please see `Field data types` in the `Elasticsearch Reference`. | keyword |
| event.outcome | This is one of four ECS Categorization Fields, and indicates the lowest level in the ECS category hierarchy. `event.outcome` simply denotes whether the event represents a success or a failure from the perspective of the entity that produced the event. Note that when a single transaction is described in multiple events, each event may populate different values of `event.outcome`, according to their perspective. Also note that in the case of a compound event (a single event that contains multiple logical events), this field should be populated with the value that best captures the overall success or failure from the perspective of the event producer. Further note that not all events will have an associated outcome. For example, this field is generally not populated for metric events, events with `event.type:info`, or any events for which an outcome does not make logical sense. | keyword |
| event.severity | The numeric severity of the event according to your event source. What the different severity values mean can be different between sources and use cases. It's up to the implementer to make sure severities are consistent across events from the same source. The Syslog severity belongs in `log.syslog.severity.code`. `event.severity` is meant to represent the severity according to the event source (e.g. firewall, IDS). If the event source does not publish its own severity, you may optionally copy the `log.syslog.severity.code` to `event.severity`. | long |
| event.start | `event.start` contains the date when the event started or when the activity was first observed. | date |
| event.type | This is one of four ECS Categorization Fields, and indicates the third level in the ECS category hierarchy. `event.type` represents a categorization "sub-bucket" that, when used along with the `event.category` field values, enables filtering events down to a level appropriate for single visualization. This field is an array. This will allow proper categorization of some events that fall in multiple event types. | keyword |
| harfang_labs.threat.agent_count | Number of impacted agents. | long |
| harfang_labs.threat.agents.domainname |  | keyword |
| harfang_labs.threat.agents.hostname |  | keyword |
| harfang_labs.threat.agents.id |  | keyword |
| harfang_labs.threat.agents.ipaddress |  | ip |
| harfang_labs.threat.agents.osproducttype |  | keyword |
| harfang_labs.threat.agents.ostype |  | keyword |
| harfang_labs.threat.alert_count | Total count of related security events (alerts). | long |
| harfang_labs.threat.alert_count_by_level | Related alert counts grouped by severity level. | object |
| harfang_labs.threat.first_seen | Timestamp of the first related security event. | date |
| harfang_labs.threat.id | HarfangLab numeric threat identifier. | long |
| harfang_labs.threat.impacted_users.full_name |  | keyword |
| harfang_labs.threat.impacted_users.user_sid |  | keyword |
| harfang_labs.threat.last_seen | Timestamp of the most recent related security event. | date |
| harfang_labs.threat.last_update | Timestamp of the last threat update. | date |
| harfang_labs.threat.level | Raw threat severity level string. | keyword |
| harfang_labs.threat.mitre_tactics | MITRE ATT&CK tactic identifiers associated with the threat. | keyword |
| harfang_labs.threat.note.content |  | text |
| harfang_labs.threat.note.creation_date |  | date |
| harfang_labs.threat.note.last_update |  | date |
| harfang_labs.threat.note.title |  | keyword |
| harfang_labs.threat.rule_count | Number of contributing detection rules. | long |
| harfang_labs.threat.rules.rule_level |  | keyword |
| harfang_labs.threat.rules.rule_name |  | keyword |
| harfang_labs.threat.rules.security_event_count |  | long |
| harfang_labs.threat.slug | Threat display slug. | keyword |
| harfang_labs.threat.status | Threat workflow status. | keyword |
| harfang_labs.threat.top_agents.agent_hostname |  | keyword |
| harfang_labs.threat.top_agents.agent_id |  | keyword |
| harfang_labs.threat.top_impacted_users.user_name |  | keyword |
| harfang_labs.threat.top_rules.rule_name |  | keyword |
| input.type | Input type. | keyword |
| related.hosts | All hostnames or other host identifiers seen on your event. Example identifiers include FQDNs, domain names, workstation names, or aliases. | keyword |
| related.ip | All of the IPs seen on your event. | ip |
| related.user | All the user names or other user identifiers seen on the event. | keyword |
| tags | List of keywords used to tag each event. | keyword |
| threat.tactic.name | Name of the type of tactic used by this threat. You can use a MITRE ATT&CK® tactic, for example. (ex. https://attack.mitre.org/tactics/TA0002/) | keyword |


##### threat sample event

An example event for `threat` looks as following:

```json
{
    "@timestamp": "2024-06-15T10:21:20.704Z",
    "agent": {
        "ephemeral_id": "b16d1b5a-3db8-429f-9dd4-a195479e47f9",
        "id": "8b890f94-6fdf-4bd3-b1e0-d98e8c908523",
        "name": "elastic-agent-77552",
        "type": "filebeat",
        "version": "9.4.1"
    },
    "data_stream": {
        "dataset": "harfang_labs.threat",
        "namespace": "17886",
        "type": "logs"
    },
    "ecs": {
        "version": "9.3.0"
    },
    "elastic_agent": {
        "id": "8b890f94-6fdf-4bd3-b1e0-d98e8c908523",
        "snapshot": false,
        "version": "9.4.1"
    },
    "event": {
        "agent_id_status": "verified",
        "category": [
            "intrusion_detection"
        ],
        "dataset": "harfang_labs.threat",
        "end": "2024-06-15T10:22:00.000Z",
        "id": "critical-yara-activity-on-ws01",
        "ingested": "2026-05-25T11:11:59Z",
        "kind": "alert",
        "module": "harfang_labs",
        "original": "{\"agent_count\":1,\"agents\":[{\"domainname\":\"example.com\",\"hostname\":\"ws01.example.com\",\"id\":\"11111111-1111-1111-1111-111111111111\",\"osproducttype\":\"Windows 10 Pro\",\"ostype\":\"windows\"}],\"creation_date\":\"2024-06-15T10:21:20.704000Z\",\"first_seen\":\"2024-06-15T10:21:20.704000Z\",\"id\":42,\"impacted_users\":[{\"full_name\":\"EXAMPLE\\\\jdoe\",\"user_sid\":\"S-1-5-21-0000000000-0000000000-0000000000-1100\"}],\"last_seen\":\"2024-06-15T10:22:00.000000Z\",\"last_update\":\"2024-06-15T10:22:30.000000Z\",\"level\":\"critical\",\"mitre_tactics\":[\"execution\",\"defense_evasion\"],\"note\":{\"content\":\"Auto-grouped threat\",\"creation_date\":\"2024-06-15T10:21:20.704000Z\",\"last_update\":\"2024-06-15T10:21:20.704000Z\",\"title\":\"critical-yara-activity-on-ws01\"},\"rule_count\":1,\"rules\":[{\"rule_level\":\"high\",\"rule_name\":\"YARA binary check\",\"security_event_count\":3}],\"security_event_count_by_level\":{\"critical\":1,\"high\":2,\"low\":0,\"medium\":0},\"slug\":\"critical-yara-activity-on-ws01\",\"status\":\"new\",\"top_agents\":[{\"agent_hostname\":\"ws01.example.com\",\"agent_id\":\"11111111-1111-1111-1111-111111111111\"}],\"top_impacted_users\":[{\"user_name\":\"jdoe\"}],\"top_rules\":[{\"rule_name\":\"YARA binary check\"}],\"total_security_event_count\":3}",
        "outcome": "unknown",
        "severity": 99,
        "start": "2024-06-15T10:21:20.704Z",
        "type": [
            "info"
        ]
    },
    "harfang_labs": {
        "threat": {
            "agent_count": 1,
            "agents": {
                "domainname": "example.com",
                "hostname": "ws01.example.com",
                "id": "11111111-1111-1111-1111-111111111111",
                "osproducttype": "Windows 10 Pro",
                "ostype": "windows"
            },
            "alert_count": 3,
            "alert_count_by_level": {
                "critical": 1,
                "high": 2,
                "low": 0,
                "medium": 0
            },
            "first_seen": "2024-06-15T10:21:20.704Z",
            "id": 42,
            "impacted_users": {
                "full_name": "EXAMPLE\\jdoe",
                "user_sid": "S-1-5-21-0000000000-0000000000-0000000000-1100"
            },
            "last_seen": "2024-06-15T10:22:00.000Z",
            "last_update": "2024-06-15T10:22:30.000Z",
            "level": "critical",
            "mitre_tactics": [
                "execution",
                "defense_evasion"
            ],
            "note": {
                "content": "Auto-grouped threat",
                "creation_date": "2024-06-15T10:21:20.704Z",
                "last_update": "2024-06-15T10:21:20.704Z",
                "title": "critical-yara-activity-on-ws01"
            },
            "rule_count": 1,
            "rules": {
                "rule_level": "high",
                "rule_name": "YARA binary check",
                "security_event_count": 3
            },
            "slug": "critical-yara-activity-on-ws01",
            "status": "new",
            "top_agents": {
                "agent_hostname": "ws01.example.com",
                "agent_id": "11111111-1111-1111-1111-111111111111"
            },
            "top_impacted_users": {
                "user_name": "jdoe"
            },
            "top_rules": {
                "rule_name": "YARA binary check"
            }
        }
    },
    "input": {
        "type": "cel"
    },
    "related": {
        "hosts": [
            "ws01.example.com"
        ],
        "user": [
            "jdoe"
        ]
    },
    "tags": [
        "preserve_original_event",
        "forwarded"
    ],
    "threat": {
        "tactic": {
            "name": [
                "execution",
                "defense_evasion"
            ]
        }
    }
}
```

#### agent

The `agent` data stream collects HarfangLab EDR agent inventory (state, OS, IP, last check-in) from the REST API.

##### agent fields

**Exported fields**

| Field | Description | Type |
|---|---|---|
| @timestamp | Date/time when the event originated. This is the date/time extracted from the event, typically representing when the event was generated by the source. If the event source has no original timestamp, this value is typically populated by the first time the event was received by the pipeline. Required field for all events. | date |
| agent.id | Unique identifier of this agent (if one exists). Example: For Beats this would be beat.id. | keyword |
| agent.name | Custom name of the agent. This is a name that can be given to an agent. This can be helpful if for example two Filebeat instances are running on the same host but a human readable separation is needed on which Filebeat instance data is coming from. | keyword |
| agent.type | Type of the agent. The agent type always stays the same and should be given by the agent used. In case of Filebeat the agent would always be Filebeat also if two Filebeat instances are run on the same machine. | keyword |
| data_stream.dataset | The field can contain anything that makes sense to signify the source of the data. Examples include `nginx.access`, `prometheus`, `endpoint` etc. For data streams that otherwise fit, but that do not have dataset set we use the value "generic" for the dataset value. `event.dataset` should have the same value as `data_stream.dataset`. Beyond the Elasticsearch data stream naming criteria noted above, the `dataset` value has additional restrictions:   \* Must not contain `-`   \* No longer than 100 characters | constant_keyword |
| data_stream.namespace | A user defined namespace. Namespaces are useful to allow grouping of data. Many users already organize their indices this way, and the data stream naming scheme now provides this best practice as a default. Many users will populate this field with `default`. If no value is used, it falls back to `default`. Beyond the Elasticsearch index naming criteria noted above, `namespace` value has the additional restrictions:   \* Must not contain `-`   \* No longer than 100 characters | constant_keyword |
| data_stream.type | An overarching type for the data stream. Currently allowed values are "logs" and "metrics". We expect to also add "traces" and "synthetics" in the near future. | constant_keyword |
| ecs.version | ECS version this event conforms to. `ecs.version` is a required field and must exist in all events. When querying across multiple indices -- which may conform to slightly different ECS versions -- this field lets integrations adjust to the schema version of the events. | keyword |
| error.message | Error message. | match_only_text |
| event.category | This is one of four ECS Categorization Fields, and indicates the second level in the ECS category hierarchy. `event.category` represents the "big buckets" of ECS categories. For example, filtering on `event.category:process` yields all events relating to process activity. This field is closely related to `event.type`, which is used as a subcategory. This field is an array. This will allow proper categorization of some events that fall in multiple categories. | keyword |
| event.dataset | Name of the dataset. If an event source publishes more than one type of log or events (e.g. access log, error log), the dataset is used to specify which one the event comes from. It's recommended but not required to start the dataset name with the module name, followed by a dot, then the dataset name. | constant_keyword |
| event.kind | This is one of four ECS Categorization Fields, and indicates the highest level in the ECS category hierarchy. `event.kind` gives high-level information about what type of information the event contains, without being specific to the contents of the event. For example, values of this field distinguish alert events from metric events. The value of this field can be used to inform how these kinds of events should be handled. They may warrant different retention, different access control, it may also help understand whether the data is coming in at a regular interval or not. | keyword |
| event.module | Name of the module this data is coming from. If your monitoring agent supports the concept of modules or plugins to process events of a given source (e.g. Apache logs), `event.module` should contain the name of this module. | constant_keyword |
| event.original | Raw text message of entire event. Used to demonstrate log integrity or where the full log message (before splitting it up in multiple parts) may be required, e.g. for reindex. This field is not indexed and doc_values are disabled. It cannot be searched, but it can be retrieved from `_source`. If users wish to override this and index this field, please see `Field data types` in the `Elasticsearch Reference`. | keyword |
| event.outcome | This is one of four ECS Categorization Fields, and indicates the lowest level in the ECS category hierarchy. `event.outcome` simply denotes whether the event represents a success or a failure from the perspective of the entity that produced the event. Note that when a single transaction is described in multiple events, each event may populate different values of `event.outcome`, according to their perspective. Also note that in the case of a compound event (a single event that contains multiple logical events), this field should be populated with the value that best captures the overall success or failure from the perspective of the event producer. Further note that not all events will have an associated outcome. For example, this field is generally not populated for metric events, events with `event.type:info`, or any events for which an outcome does not make logical sense. | keyword |
| event.type | This is one of four ECS Categorization Fields, and indicates the third level in the ECS category hierarchy. `event.type` represents a categorization "sub-bucket" that, when used along with the `event.category` field values, enables filtering events down to a level appropriate for single visualization. This field is an array. This will allow proper categorization of some events that fall in multiple event types. | keyword |
| harfang_labs.agent.antivirus.name |  | keyword |
| harfang_labs.agent.antivirus.profile |  | keyword |
| harfang_labs.agent.antivirus.version |  | keyword |
| harfang_labs.agent.avg_cpu | Average CPU utilization percentage. | double |
| harfang_labs.agent.avg_memory | Average memory utilization percentage. | double |
| harfang_labs.agent.cpu_count | Number of CPU cores on the endpoint. | long |
| harfang_labs.agent.cpu_frequency | CPU frequency in MHz. | long |
| harfang_labs.agent.domain | Windows domain or NetBIOS domain name. | keyword |
| harfang_labs.agent.effective_policy_id | Effective policy identifier after inheritance. | keyword |
| harfang_labs.agent.effective_policy_revision | Effective policy revision after inheritance. | long |
| harfang_labs.agent.external_ip | External IP address reported by the agent. | ip |
| harfang_labs.agent.first_seen | Timestamp when the agent was first observed. | date |
| harfang_labs.agent.group_count | Number of agent groups the endpoint belongs to. | long |
| harfang_labs.agent.groupname | Primary agent group name. | keyword |
| harfang_labs.agent.groups.id |  | keyword |
| harfang_labs.agent.groups.name |  | keyword |
| harfang_labs.agent.id | HarfangLab agent UUID. | keyword |
| harfang_labs.agent.install_date | Agent installation date when reported by the API. | date |
| harfang_labs.agent.isolation_policy | Isolation policy applied to the endpoint. | keyword |
| harfang_labs.agent.isolation_state | Whether the endpoint is network-isolated. | boolean |
| harfang_labs.agent.last_seen | Timestamp when the agent was last seen. | date |
| harfang_labs.agent.online_status | Agent connectivity status when distinct from status. | keyword |
| harfang_labs.agent.os_build | OS build number reported by the agent. | long |
| harfang_labs.agent.policy.id |  | keyword |
| harfang_labs.agent.policy.name |  | keyword |
| harfang_labs.agent.policy.revision |  | long |
| harfang_labs.agent.policy_id | Assigned policy identifier. | keyword |
| harfang_labs.agent.policy_name | Assigned policy display name. | keyword |
| harfang_labs.agent.status | Agent online status (online/offline). | keyword |
| harfang_labs.agent.subnet | Subnet associated with the endpoint. | keyword |
| harfang_labs.agent.tenant | HarfangLab tenant identifier. | keyword |
| harfang_labs.agent.total_memory | Total system memory in bytes. | double |
| harfang_labs.agent.update_status | Agent update status when reported by the API. | keyword |
| harfang_labs.agent.version | HarfangLab EDR agent client version. | keyword |
| host.architecture | Operating system architecture. | keyword |
| host.domain | Name of the domain of which the host is a member. For example, on Windows this could be the host's Active Directory domain or NetBIOS domain name. For Linux this could be the domain of the host's LDAP provider. | keyword |
| host.hostname | Hostname of the host. It normally contains what the `hostname` command returns on the host machine. | keyword |
| host.id | Unique host id. As hostname is not always unique, use values that are meaningful in your environment. Example: The current usage of `beat.name`. | keyword |
| host.ip | Host ip addresses. | ip |
| host.name | Name of the host. It can contain what hostname returns on Unix systems, the fully qualified domain name (FQDN), or a name specified by the user. The recommended value is the lowercase FQDN of the host. | keyword |
| host.os.full | Operating system name, including the version or code name. | keyword |
| host.os.full.text | Multi-field of `host.os.full`. | match_only_text |
| host.os.name | Operating system name, without the version. | keyword |
| host.os.name.text | Multi-field of `host.os.name`. | match_only_text |
| host.os.platform | Operating system platform (such centos, ubuntu, windows). | keyword |
| host.os.type | Use the `os.type` field to categorize the operating system into one of the broad commercial families. If the OS you're dealing with is not listed as an expected value, the field should not be populated. Please let us know by opening an issue with ECS, to propose its addition. | keyword |
| host.os.version | Operating system version as a raw string. | keyword |
| host.user.domain | Logged-in user domain on the endpoint. | keyword |
| host.user.name | Logged-in user name on the endpoint. | keyword |
| input.type | Input type. | keyword |
| related.hosts | All hostnames or other host identifiers seen on your event. Example identifiers include FQDNs, domain names, workstation names, or aliases. | keyword |
| related.ip | All of the IPs seen on your event. | ip |
| related.user | All the user names or other user identifiers seen on the event. | keyword |
| tags | List of keywords used to tag each event. | keyword |


##### agent sample event

An example event for `agent` looks as following:

```json
{
    "@timestamp": "2024-06-15T10:21:20.704Z",
    "agent": {
        "ephemeral_id": "d9e2c4c6-38cf-4beb-8a03-f73b5f1405ee",
        "id": "11111111-1111-1111-1111-111111111111",
        "name": "harfang_labs",
        "type": "harfang_labs",
        "version": "9.4.1"
    },
    "data_stream": {
        "dataset": "harfang_labs.agent",
        "namespace": "49312",
        "type": "logs"
    },
    "ecs": {
        "version": "9.3.0"
    },
    "elastic_agent": {
        "id": "c562df48-37bd-4660-80f3-565f8c145986",
        "snapshot": false,
        "version": "9.4.1"
    },
    "event": {
        "agent_id_status": "mismatch",
        "category": [
            "host"
        ],
        "dataset": "harfang_labs.agent",
        "ingested": "2026-05-25T11:08:18Z",
        "kind": "event",
        "module": "harfang_labs",
        "original": "{\"bitness\":\"x64\",\"cpu_count\":2,\"cpu_frequency\":3393,\"dnsdomainname\":\"example.local\",\"domainname\":\"EXAMPLE\",\"external_ipaddress\":\"198.51.100.10\",\"firstseen\":\"2024-05-01T08:00:00.000000Z\",\"group_count\":1,\"groups\":[{\"id\":\"33333333-3333-3333-3333-333333333333\",\"name\":\"DEFAULT\"}],\"hostname\":\"WS-EXAMPLE-01.example.com\",\"id\":\"11111111-1111-1111-1111-111111111111\",\"ipaddress\":\"192.0.2.10\",\"isolation_state\":false,\"lastseen\":\"2024-06-15T10:21:20.704000Z\",\"osbuild\":19041,\"osmajor\":10,\"osminor\":0,\"osproducttype\":\"Windows 10 Pro\",\"ostype\":\"windows\",\"osversion\":\"10.0.19041\",\"policy\":{\"id\":\"44444444-4444-4444-4444-444444444444\",\"name\":\"Default policy\",\"revision\":5},\"status\":\"online\",\"total_memory\":4277866496,\"version\":\"4.9.8\"}",
        "outcome": "success",
        "type": [
            "info"
        ]
    },
    "harfang_labs": {
        "agent": {
            "cpu_count": 2,
            "cpu_frequency": 3393,
            "external_ip": "198.51.100.10",
            "first_seen": "2024-05-01T08:00:00.000Z",
            "group_count": 1,
            "groupname": "DEFAULT",
            "groups": {
                "id": "33333333-3333-3333-3333-333333333333",
                "name": "DEFAULT"
            },
            "id": "11111111-1111-1111-1111-111111111111",
            "isolation_state": false,
            "last_seen": "2024-06-15T10:21:20.704Z",
            "os_build": 19041,
            "policy": {
                "id": "44444444-4444-4444-4444-444444444444",
                "name": "Default policy",
                "revision": 5
            },
            "policy_id": "44444444-4444-4444-4444-444444444444",
            "policy_name": "Default policy",
            "status": "online",
            "total_memory": 4277866496,
            "version": "4.9.8"
        }
    },
    "host": {
        "architecture": "x86_64",
        "domain": "example.local",
        "hostname": "WS-EXAMPLE-01.example.com",
        "id": "11111111-1111-1111-1111-111111111111",
        "ip": [
            "192.0.2.10",
            "198.51.100.10"
        ],
        "name": "WS-EXAMPLE-01.example.com",
        "os": {
            "full": "Windows 10 Pro",
            "name": "Windows",
            "platform": "windows",
            "type": "windows",
            "version": "10.0.19041"
        }
    },
    "input": {
        "type": "cel"
    },
    "related": {
        "hosts": [
            "WS-EXAMPLE-01.example.com"
        ],
        "ip": [
            "192.0.2.10",
            "198.51.100.10"
        ]
    },
    "tags": [
        "preserve_original_event",
        "forwarded"
    ]
}
```

#### event

The `event` data stream receives RFC 5424 syslog messages with JSON payloads from the HarfangLab Syslog connector, supporting 14 vendor `log_type` values. The pipeline routes each message to a sub-pipeline that performs the type-specific ECS mapping.

##### event fields

**Exported fields**

| Field | Description | Type |
|---|---|---|
| @timestamp | Date/time when the event originated. This is the date/time extracted from the event, typically representing when the event was generated by the source. If the event source has no original timestamp, this value is typically populated by the first time the event was received by the pipeline. Required field for all events. | date |
| _conf.tz_offset | Timezone offset applied to syslog timestamps without an explicit timezone. | keyword |
| agent.id | Unique identifier of this agent (if one exists). Example: For Beats this would be beat.id. | keyword |
| agent.name | Custom name of the agent. This is a name that can be given to an agent. This can be helpful if for example two Filebeat instances are running on the same host but a human readable separation is needed on which Filebeat instance data is coming from. | keyword |
| agent.type | Type of the agent. The agent type always stays the same and should be given by the agent used. In case of Filebeat the agent would always be Filebeat also if two Filebeat instances are run on the same machine. | keyword |
| data_stream.dataset | The field can contain anything that makes sense to signify the source of the data. Examples include `nginx.access`, `prometheus`, `endpoint` etc. For data streams that otherwise fit, but that do not have dataset set we use the value "generic" for the dataset value. `event.dataset` should have the same value as `data_stream.dataset`. Beyond the Elasticsearch data stream naming criteria noted above, the `dataset` value has additional restrictions:   \* Must not contain `-`   \* No longer than 100 characters | constant_keyword |
| data_stream.namespace | A user defined namespace. Namespaces are useful to allow grouping of data. Many users already organize their indices this way, and the data stream naming scheme now provides this best practice as a default. Many users will populate this field with `default`. If no value is used, it falls back to `default`. Beyond the Elasticsearch index naming criteria noted above, `namespace` value has the additional restrictions:   \* Must not contain `-`   \* No longer than 100 characters | constant_keyword |
| data_stream.type | An overarching type for the data stream. Currently allowed values are "logs" and "metrics". We expect to also add "traces" and "synthetics" in the near future. | constant_keyword |
| destination.ip | IP address of the destination (IPv4 or IPv6). | ip |
| destination.port | Port of the destination. | long |
| dns.question.name | The name being queried. If the name field contains non-printable characters (below 32 or above 126), those characters should be represented as escaped base 10 integers (\DDD). Back slashes and quotes should be escaped. Tabs, carriage returns, and line feeds should be converted to \t, \r, and \n respectively. | keyword |
| ecs.version | ECS version this event conforms to. `ecs.version` is a required field and must exist in all events. When querying across multiple indices -- which may conform to slightly different ECS versions -- this field lets integrations adjust to the schema version of the events. | keyword |
| error.message | Error message. | match_only_text |
| event.action | The action captured by the event. This describes the information in the event. It is more specific than `event.category`. Examples are `group-add`, `process-started`, `file-created`. The value is normally defined by the implementer. | keyword |
| event.category | This is one of four ECS Categorization Fields, and indicates the second level in the ECS category hierarchy. `event.category` represents the "big buckets" of ECS categories. For example, filtering on `event.category:process` yields all events relating to process activity. This field is closely related to `event.type`, which is used as a subcategory. This field is an array. This will allow proper categorization of some events that fall in multiple categories. | keyword |
| event.created | `event.created` contains the date/time when the event was first read by an agent, or by your pipeline. This field is distinct from `@timestamp` in that `@timestamp` typically contain the time extracted from the original event. In most situations, these two timestamps will be slightly different. The difference can be used to calculate the delay between your source generating an event, and the time when your agent first processed it. This can be used to monitor your agent's or pipeline's ability to keep up with your event source. In case the two timestamps are identical, `@timestamp` should be used. | date |
| event.dataset | Name of the dataset. If an event source publishes more than one type of log or events (e.g. access log, error log), the dataset is used to specify which one the event comes from. It's recommended but not required to start the dataset name with the module name, followed by a dot, then the dataset name. | constant_keyword |
| event.end | `event.end` contains the date when the event ended or when the activity was last observed. | date |
| event.id | Unique ID to describe the event. | keyword |
| event.kind | This is one of four ECS Categorization Fields, and indicates the highest level in the ECS category hierarchy. `event.kind` gives high-level information about what type of information the event contains, without being specific to the contents of the event. For example, values of this field distinguish alert events from metric events. The value of this field can be used to inform how these kinds of events should be handled. They may warrant different retention, different access control, it may also help understand whether the data is coming in at a regular interval or not. | keyword |
| event.module | Name of the module this data is coming from. If your monitoring agent supports the concept of modules or plugins to process events of a given source (e.g. Apache logs), `event.module` should contain the name of this module. | constant_keyword |
| event.original | Raw text message of entire event. Used to demonstrate log integrity or where the full log message (before splitting it up in multiple parts) may be required, e.g. for reindex. This field is not indexed and doc_values are disabled. It cannot be searched, but it can be retrieved from `_source`. If users wish to override this and index this field, please see `Field data types` in the `Elasticsearch Reference`. | keyword |
| event.outcome | This is one of four ECS Categorization Fields, and indicates the lowest level in the ECS category hierarchy. `event.outcome` simply denotes whether the event represents a success or a failure from the perspective of the entity that produced the event. Note that when a single transaction is described in multiple events, each event may populate different values of `event.outcome`, according to their perspective. Also note that in the case of a compound event (a single event that contains multiple logical events), this field should be populated with the value that best captures the overall success or failure from the perspective of the event producer. Further note that not all events will have an associated outcome. For example, this field is generally not populated for metric events, events with `event.type:info`, or any events for which an outcome does not make logical sense. | keyword |
| event.reason | Reason why this event happened, according to the source. This describes the why of a particular action or outcome captured in the event. Where `event.action` captures the action from the event, `event.reason` describes why that action was taken. For example, a web proxy with an `event.action` which denied the request may also populate `event.reason` with the reason why (e.g. `blocked site`). | keyword |
| event.severity | The numeric severity of the event according to your event source. What the different severity values mean can be different between sources and use cases. It's up to the implementer to make sure severities are consistent across events from the same source. The Syslog severity belongs in `log.syslog.severity.code`. `event.severity` is meant to represent the severity according to the event source (e.g. firewall, IDS). If the event source does not publish its own severity, you may optionally copy the `log.syslog.severity.code` to `event.severity`. | long |
| event.start | `event.start` contains the date when the event started or when the activity was first observed. | date |
| event.timezone | This field should be populated when the event's timestamp does not include timezone information already (e.g. default Syslog timestamps). It's optional otherwise. Acceptable timezone formats are: a canonical ID (e.g. "Europe/Amsterdam"), abbreviated (e.g. "EST") or an HH:mm differential (e.g. "-05:00"). | keyword |
| event.type | This is one of four ECS Categorization Fields, and indicates the third level in the ECS category hierarchy. `event.type` represents a categorization "sub-bucket" that, when used along with the `event.category` field values, enables filtering events down to a level appropriate for single visualization. This field is an array. This will allow proper categorization of some events that fall in multiple event types. | keyword |
| file.accessed | Last time the file was accessed. Note that not all filesystems keep track of access time. | date |
| file.code_signature.exists | Boolean to capture if a signature is present. | boolean |
| file.code_signature.subject_name | Subject name of the code signer | keyword |
| file.code_signature.valid | Boolean to capture if the digital signature is verified against the binary content. Leave unpopulated if a certificate was unchecked. | boolean |
| file.hash.md5 | MD5 hash. | keyword |
| file.hash.sha1 | SHA1 hash. | keyword |
| file.hash.sha256 | SHA256 hash. | keyword |
| file.name | Name of the file including the extension, without the directory. | keyword |
| file.path | Full path to the file, including the file name. It should include the drive letter, when appropriate. | keyword |
| file.path.text | Multi-field of `file.path`. | match_only_text |
| file.pe.company | Internal company name of the file, provided at compile-time. | keyword |
| file.pe.description | Internal description of the file, provided at compile-time. | keyword |
| file.pe.file_version | Internal version of the file, provided at compile-time. | keyword |
| file.pe.imphash | A hash of the imports in a PE file. An imphash -- or import hash -- can be used to fingerprint binaries even after recompilation or other code-level transformations have occurred, which would change more traditional hash values. Learn more at https://www.fireeye.com/blog/threat-research/2014/01/tracking-malware-import-hashing.html. | keyword |
| file.pe.original_file_name | Internal name of the file, provided at compile-time. | keyword |
| file.pe.product | Internal product name of the file, provided at compile-time. | keyword |
| file.size | File size in bytes. Only relevant when `file.type` is "file". | long |
| harfang_labs.agent.avg_cpu |  | double |
| harfang_labs.agent.avg_memory |  | double |
| harfang_labs.agent.cpu_count |  | long |
| harfang_labs.agent.cpu_frequency |  | long |
| harfang_labs.agent.domain |  | keyword |
| harfang_labs.agent.effective_policy_id |  | keyword |
| harfang_labs.agent.effective_policy_revision |  | long |
| harfang_labs.agent.external_ip |  | ip |
| harfang_labs.agent.first_seen |  | date |
| harfang_labs.agent.group_count |  | long |
| harfang_labs.agent.groupname |  | keyword |
| harfang_labs.agent.groups.id |  | keyword |
| harfang_labs.agent.groups.name |  | keyword |
| harfang_labs.agent.id |  | keyword |
| harfang_labs.agent.install_date |  | date |
| harfang_labs.agent.isolation_policy |  | keyword |
| harfang_labs.agent.isolation_state |  | boolean |
| harfang_labs.agent.last_seen |  | date |
| harfang_labs.agent.online_status |  | keyword |
| harfang_labs.agent.os_build |  | long |
| harfang_labs.agent.policy.id |  | keyword |
| harfang_labs.agent.policy.name |  | keyword |
| harfang_labs.agent.policy.revision |  | long |
| harfang_labs.agent.policy_id |  | keyword |
| harfang_labs.agent.policy_name |  | keyword |
| harfang_labs.agent.status |  | keyword |
| harfang_labs.agent.subnet |  | keyword |
| harfang_labs.agent.tenant |  | keyword |
| harfang_labs.agent.total_memory |  | double |
| harfang_labs.agent.update_status |  | keyword |
| harfang_labs.agent.version |  | keyword |
| harfang_labs.alert.aggregation_key |  | keyword |
| harfang_labs.alert.eventlog.channel |  | keyword |
| harfang_labs.alert.eventlog.event_data |  | flattened |
| harfang_labs.alert.eventlog.event_id |  | keyword |
| harfang_labs.alert.eventlog.source_name |  | keyword |
| harfang_labs.alert.execution_mode |  | long |
| harfang_labs.alert.id |  | keyword |
| harfang_labs.alert.level |  | keyword |
| harfang_labs.alert.logonid |  | long |
| harfang_labs.alert.matched_rules.namespace |  | keyword |
| harfang_labs.alert.matched_rules.rulename |  | keyword |
| harfang_labs.alert.matched_rules.source |  | keyword |
| harfang_labs.alert.maturity |  | keyword |
| harfang_labs.alert.payload_type |  | keyword |
| harfang_labs.alert.process.integrity_level |  | keyword |
| harfang_labs.alert.process.issuer_name |  | keyword |
| harfang_labs.alert.process.product_version |  | keyword |
| harfang_labs.alert.process.size |  | long |
| harfang_labs.alert.score |  | long |
| harfang_labs.alert.session_id |  | long |
| harfang_labs.alert.status |  | keyword |
| harfang_labs.alert.subtype |  | keyword |
| harfang_labs.alert.threat_key |  | long |
| harfang_labs.alert.type |  | keyword |
| harfang_labs.event.auditlog.response_status_text |  | keyword |
| harfang_labs.event.connectionlog.connection_count |  | long |
| harfang_labs.event.connectionlog.investigation_data_sum |  | long |
| harfang_labs.event.connectionlog.object_type |  | keyword |
| harfang_labs.event.connectionlog.telemetry_sum |  | long |
| harfang_labs.event.driverload.image_base |  | keyword |
| harfang_labs.event.driverload.image_size |  | long |
| harfang_labs.event.eventlog.type |  | keyword |
| harfang_labs.event.file.event_id |  | keyword |
| harfang_labs.event.file.operation |  | keyword |
| harfang_labs.event.file.thread_id |  | long |
| harfang_labs.event.injectedthread.region_protect |  | long |
| harfang_labs.event.injectedthread.region_size |  | long |
| harfang_labs.event.injectedthread.region_state |  | long |
| harfang_labs.event.injectedthread.region_type |  | long |
| harfang_labs.event.injectedthread.start_address |  | long |
| harfang_labs.event.injectedthread.start_address_string |  | keyword |
| harfang_labs.event.injectedthread.thread_id |  | long |
| harfang_labs.event.injectedthread.thread_sha256 |  | keyword |
| harfang_labs.event.injectedthread.thread_uid |  | keyword |
| harfang_labs.event.investigation.binaryinfo |  | flattened |
| harfang_labs.event.investigation.job_id |  | keyword |
| harfang_labs.event.investigation.job_instance_id |  | keyword |
| harfang_labs.event.investigation.object_type |  | keyword |
| harfang_labs.event.investigation.status |  | keyword |
| harfang_labs.event.log_type | Source log_type value from the JSON body. | keyword |
| harfang_labs.event.network.conn_type |  | long |
| harfang_labs.event.network.event_id |  | keyword |
| harfang_labs.event.network.initiated |  | keyword |
| harfang_labs.event.network.is_ipv6 |  | keyword |
| harfang_labs.event.network.query_results |  | flattened |
| harfang_labs.event.process.integrity_level |  | keyword |
| harfang_labs.event.process.logonid |  | long |
| harfang_labs.event.process.session_id |  | long |
| harfang_labs.event.process.size |  | long |
| harfang_labs.threat.agent_count |  | long |
| harfang_labs.threat.agents.agent_hostname |  | keyword |
| harfang_labs.threat.agents.agent_id |  | keyword |
| harfang_labs.threat.agents.agent_ostype |  | keyword |
| harfang_labs.threat.agents.domainname |  | keyword |
| harfang_labs.threat.agents.hostname |  | keyword |
| harfang_labs.threat.agents.id |  | keyword |
| harfang_labs.threat.agents.ipaddress |  | ip |
| harfang_labs.threat.agents.osproducttype |  | keyword |
| harfang_labs.threat.agents.ostype |  | keyword |
| harfang_labs.threat.agents.security_event_count |  | long |
| harfang_labs.threat.alert_count |  | long |
| harfang_labs.threat.alert_count_by_level |  | object |
| harfang_labs.threat.first_seen |  | date |
| harfang_labs.threat.id |  | long |
| harfang_labs.threat.impacted_users.full_name |  | keyword |
| harfang_labs.threat.impacted_users.user_sid |  | keyword |
| harfang_labs.threat.last_seen |  | date |
| harfang_labs.threat.last_update |  | date |
| harfang_labs.threat.level |  | keyword |
| harfang_labs.threat.note.content |  | text |
| harfang_labs.threat.note.creation_date |  | date |
| harfang_labs.threat.note.last_update |  | date |
| harfang_labs.threat.note.title |  | keyword |
| harfang_labs.threat.rule_count |  | long |
| harfang_labs.threat.rules.rule_id |  | keyword |
| harfang_labs.threat.rules.rule_level |  | keyword |
| harfang_labs.threat.rules.rule_name |  | keyword |
| harfang_labs.threat.rules.security_event_count |  | long |
| harfang_labs.threat.slug |  | keyword |
| harfang_labs.threat.status |  | keyword |
| harfang_labs.threat.top_agents.agent_hostname |  | keyword |
| harfang_labs.threat.top_agents.agent_id |  | keyword |
| harfang_labs.threat.top_impacted_users.user_name |  | keyword |
| harfang_labs.threat.top_rules.rule_name |  | keyword |
| host.architecture | Operating system architecture. | keyword |
| host.domain | Name of the domain of which the host is a member. For example, on Windows this could be the host's Active Directory domain or NetBIOS domain name. For Linux this could be the domain of the host's LDAP provider. | keyword |
| host.hostname | Hostname of the host. It normally contains what the `hostname` command returns on the host machine. | keyword |
| host.id | Unique host id. As hostname is not always unique, use values that are meaningful in your environment. Example: The current usage of `beat.name`. | keyword |
| host.ip | Host ip addresses. | ip |
| host.name | Name of the host. It can contain what hostname returns on Unix systems, the fully qualified domain name (FQDN), or a name specified by the user. The recommended value is the lowercase FQDN of the host. | keyword |
| host.os.full | Operating system name, including the version or code name. | keyword |
| host.os.full.text | Multi-field of `host.os.full`. | match_only_text |
| host.os.name | Operating system name, without the version. | keyword |
| host.os.name.text | Multi-field of `host.os.name`. | match_only_text |
| host.os.platform | Operating system platform (such centos, ubuntu, windows). | keyword |
| host.os.type | Use the `os.type` field to categorize the operating system into one of the broad commercial families. If the OS you're dealing with is not listed as an expected value, the field should not be populated. Please let us know by opening an issue with ECS, to propose its addition. | keyword |
| host.os.version | Operating system version as a raw string. | keyword |
| host.user.domain | Logged-in user domain on the endpoint. | keyword |
| host.user.name | Logged-in user name on the endpoint. | keyword |
| http.request.body.content | The full HTTP request body. | wildcard |
| http.request.body.content.text | Multi-field of `http.request.body.content`. | match_only_text |
| http.request.method | HTTP request method. The value should retain its casing from the original event. For example, `GET`, `get`, and `GeT` are all considered valid values for this field. | keyword |
| http.response.body.content | The full HTTP response body. | wildcard |
| http.response.body.content.text | Multi-field of `http.response.body.content`. | match_only_text |
| http.response.status_code | HTTP response status code. | long |
| input.type | Input type. | keyword |
| log.source.address | Source address from which the log event was read or received. | keyword |
| log.syslog.appname |  | keyword |
| log.syslog.facility |  | long |
| log.syslog.hostname |  | keyword |
| log.syslog.msgid |  | keyword |
| log.syslog.priority |  | long |
| log.syslog.procid |  | keyword |
| log.syslog.severity |  | long |
| log.syslog.version |  | keyword |
| message | For log events the message field contains the log message, optimized for viewing in a log viewer. For structured logs without an original message field, other fields can be concatenated to form a human-readable summary of the event. If multiple messages exist, they can be combined into one message. | match_only_text |
| network.direction | Direction of the network traffic. When mapping events from a host-based monitoring context, populate this field from the host's point of view, using the values "ingress" or "egress". When mapping events from a network or perimeter-based monitoring context, populate this field from the point of view of the network perimeter, using the values "inbound", "outbound", "internal" or "external". Note that "internal" is not crossing perimeter boundaries, and is meant to describe communication between two hosts within the perimeter. Note also that "external" is meant to describe traffic between two hosts that are external to the perimeter. This could for example be useful for ISPs or VPN service providers. | keyword |
| observer.type | The type of the observer the data is coming from. There is no predefined list of observer types. Some examples are `forwarder`, `firewall`, `ids`, `ips`, `proxy`, `poller`, `sensor`, `APM server`. | keyword |
| organization.id | Unique identifier for the organization. | keyword |
| process.code_signature.exists | Boolean to capture if a signature is present. | boolean |
| process.code_signature.subject_name | Subject name of the code signer | keyword |
| process.code_signature.valid | Boolean to capture if the digital signature is verified against the binary content. Leave unpopulated if a certificate was unchecked. | boolean |
| process.command_line | Full command line that started the process, including the absolute path to the executable, and all arguments. Some arguments may be filtered to protect sensitive information. | wildcard |
| process.command_line.text | Multi-field of `process.command_line`. | match_only_text |
| process.entity_id | Unique identifier for the process. The implementation of this is specified by the data source, but some examples of what could be used here are a process-generated UUID, Sysmon Process GUIDs, or a hash of some uniquely identifying components of a process. Constructing a globally unique identifier is a common practice to mitigate PID reuse as well as to identify a specific process over time, across multiple monitored hosts. | keyword |
| process.executable | Absolute path to the process executable. | keyword |
| process.executable.text | Multi-field of `process.executable`. | match_only_text |
| process.hash.md5 | MD5 hash. | keyword |
| process.hash.sha1 | SHA1 hash. | keyword |
| process.hash.sha256 | SHA256 hash. | keyword |
| process.name | Process name. Sometimes called program name or similar. | keyword |
| process.name.text | Multi-field of `process.name`. | match_only_text |
| process.parent.command_line | Full command line that started the process, including the absolute path to the executable, and all arguments. Some arguments may be filtered to protect sensitive information. | wildcard |
| process.parent.command_line.text | Multi-field of `process.parent.command_line`. | match_only_text |
| process.parent.entity_id | Unique identifier for the process. The implementation of this is specified by the data source, but some examples of what could be used here are a process-generated UUID, Sysmon Process GUIDs, or a hash of some uniquely identifying components of a process. Constructing a globally unique identifier is a common practice to mitigate PID reuse as well as to identify a specific process over time, across multiple monitored hosts. | keyword |
| process.parent.executable | Absolute path to the process executable. | keyword |
| process.parent.executable.text | Multi-field of `process.parent.executable`. | match_only_text |
| process.parent.pid | Process id. | long |
| process.pe.company | Internal company name of the file, provided at compile-time. | keyword |
| process.pe.description | Internal description of the file, provided at compile-time. | keyword |
| process.pe.file_version | Internal version of the file, provided at compile-time. | keyword |
| process.pe.imphash | A hash of the imports in a PE file. An imphash -- or import hash -- can be used to fingerprint binaries even after recompilation or other code-level transformations have occurred, which would change more traditional hash values. Learn more at https://www.fireeye.com/blog/threat-research/2014/01/tracking-malware-import-hashing.html. | keyword |
| process.pe.original_file_name | Internal name of the file, provided at compile-time. | keyword |
| process.pe.product | Internal product name of the file, provided at compile-time. | keyword |
| process.pid | Process id. | long |
| process.start | The time the process started. | date |
| process.working_directory | The working directory of the process. | keyword |
| process.working_directory.text | Multi-field of `process.working_directory`. | match_only_text |
| related.hash | All the hashes seen on your event. Populating this field, then using it to search for hashes can help in situations where you're unsure what the hash algorithm is (and therefore which key name to search). | keyword |
| related.hosts | All hostnames or other host identifiers seen on your event. Example identifiers include FQDNs, domain names, workstation names, or aliases. | keyword |
| related.ip | All of the IPs seen on your event. | ip |
| related.user | All the user names or other user identifiers seen on the event. | keyword |
| rule.category | A categorization value keyword used by the entity using the rule for detection of this event. | keyword |
| rule.id | A rule ID that is unique within the scope of an agent, observer, or other entity using the rule for detection of this event. | keyword |
| rule.name | The name of the rule or signature generating the event. | keyword |
| rule.version | The version / revision of the rule being used for analysis. | keyword |
| source.ip | IP address of the source (IPv4 or IPv6). | ip |
| source.port | Port of the source. | long |
| tags | List of keywords used to tag each event. | keyword |
| url.path | Path of the request, such as "/search". | wildcard |
| user.domain | Name of the directory the user is a member of. For example, an LDAP or Active Directory domain name. | keyword |
| user.id | Unique identifier of the user. | keyword |
| user.name | Short name or login of the user. | keyword |
| user.name.text | Multi-field of `user.name`. | match_only_text |
| user_agent.original | Unparsed user_agent string. | keyword |
| user_agent.original.text | Multi-field of `user_agent.original`. | match_only_text |
| winlog.channel |  | keyword |
| winlog.computer_name |  | keyword |
| winlog.event_data |  | flattened |
| winlog.event_id |  | keyword |
| winlog.level |  | keyword |
| winlog.opcode |  | keyword |
| winlog.process.pid |  | long |
| winlog.process.thread.id |  | long |
| winlog.provider_guid |  | keyword |
| winlog.provider_name |  | keyword |
| winlog.record_id |  | long |
| winlog.task |  | keyword |
| winlog.user_data |  | flattened |


##### event sample event

An example event for `event` looks as following:

```json
{
    "@timestamp": "2021-04-30T13:21:20.704Z",
    "_conf": {
        "tz_offset": "Local"
    },
    "agent": {
        "ephemeral_id": "9fc7f1c8-f741-4f8e-8a7e-5d9fee565f06",
        "id": "11111111-2222-3333-4444-555555555555",
        "name": "elastic-agent-22663",
        "type": "filebeat",
        "version": "9.4.1"
    },
    "data_stream": {
        "dataset": "harfang_labs.event",
        "namespace": "72430",
        "type": "logs"
    },
    "ecs": {
        "version": "9.3.0"
    },
    "elastic_agent": {
        "id": "90ac9727-f05c-4952-8084-f075b5fcf685",
        "snapshot": false,
        "version": "9.4.1"
    },
    "event": {
        "agent_id_status": "mismatch",
        "category": [
            "malware",
            "intrusion_detection",
            "process"
        ],
        "dataset": "harfang_labs.event",
        "id": "11111111-2222-3333-4444-555555555555",
        "ingested": "2026-05-25T11:11:04Z",
        "kind": "alert",
        "module": "harfang_labs",
        "original": "<134>1 2021-04-30T13:21:20.704+00:00 firewall-01.example.com EDR - - - {\"aggregation_key\":\"40fd5973a79d4e0f21e689938e5f269d20a3be780949eb6f408d5cb65c6974d1\",\"type\":\"rtlogs\",\"agent\":{\"osversion\":\"10.0.19041\",\"domainname\":\"example.com\",\"agentid\":\"11111111-2222-3333-4444-555555555555\",\"osproducttype\":\"Windows 10 Pro\",\"hostname\":\"workstation-01.example.com\",\"domain\":null},\"level\":\"high\",\"msg\":\"Binary was found malicious by 2 rule(s) with a score of 120\",\"tags\":[],\"rule_name\":\"YARA binary check\",\"process\":{\"signature_info\":{\"root_info\":{\"display_name\":\"\",\"thumbprint\":\"\",\"serial_number\":\"\",\"issuer_name\":\"\"},\"signed_catalog\":false,\"signer_info\":{\"display_name\":\"\",\"thumbprint\":\"\",\"serial_number\":\"\",\"issuer_name\":\"\"},\"signed_authenticode\":false},\"commandline\":\"ANONYMIZED\",\"matched_rules_count\":2,\"fake_parent_commandline\":null,\"dont_create_process\":true,\"session\":2,\"pid\":5736,\"process_name\":\"mimikatz.exe\",\"score\":120,\"pe_info\":{\"product_version\":\"203.0.113.10\",\"file_description\":\"mimikatz for Windows\",\"original_filename\":\"mimikatz.exe\",\"internal_name\":\"mimikatz\",\"file_version\":\"203.0.113.10\",\"company_name\":\"Braveheart\",\"legal_copyright\":\"Copyright (c) 2007 - 2020 Braveheart\",\"product_name\":\"mimikatz\"},\"fake_parent_image\":null,\"username\":\"workstation-01.example.com\\\\user1\",\"pe_timestamp_int\":1619768223,\"image_name\":\"ANONYMIZED\",\"parent_integrity_level\":\"High\",\"fake_parent_unique_id\":null,\"status_msg\":\"Binary was found potentially malicious on the endpoint by Yara with a score of 120 , but it is running in alert only mode\",\"log_type\":\"process\",\"logonid\":1121594,\"process_unique_id\":\"11111111-2222-3333-4444-555555555555\",\"usersid\":\"S-1-5-21-794873159-2603523847-3490392889-500\",\"error_msg\":\"\",\"parent_image\":\"C:\\\\Windows\\\\System32\\\\WindowsPowerShell\\\\v1.0\\\\powershell.exe\",\"fake_ppid\":null,\"integrity_level\":\"High\",\"current_directory\":\"ANONYMIZED\",\"pe_timestamp\":\"2021-04-30T07:37:03.000Z\",\"parent_unique_id\":\"11111111-2222-3333-4444-555555555555\",\"pe_imphash\":\"B6BC868F5046CE7764D0627266AF0200\",\"hashes\":{\"md5\":\"68b329da9893e34099c7d8ad5cb9c940\",\"sha256\":\"01ba4719c80b6fe911b091a7c05124b64eeece964e09c058ef8f9805daca546b\",\"sha1\":\"adc83b19e793491b1c6ea0fd8b46cd9f32e592fc\"},\"size\":1430016,\"matched_rules\":[{\"namespace\":\"mimikatz.yar\",\"rulename\":\"mimikatz_clear_string_markers\",\"source\":\"default\"},{\"namespace\":\"mimikatz.yar\",\"rulename\":\"mimikatz_patches_x64\",\"source\":\"default\"}],\"create_time\":\"2021/04/30 13:20:54.315189\",\"parent_commandline\":\"\\\"C:\\\\Windows\\\\System32\\\\WindowsPowerShell\\\\v1.0\\\\powershell.exe\\\" \",\"rule_revision\":81,\"signed\":false,\"ppid\":1700,\"status\":0},\"alert_time\":\"2021-04-30T13:21:20.704+00:00\",\"alert_subtype\":\"process\",\"execution\":0,\"alert_type\":\"yara\",\"rule_id\":\"YARA binary check\",\"@version\":\"1\",\"@timestamp\":\"2021-04-30T13:21:20.717Z\",\"@event_create_date\":\"2021-04-30T13:21:20.704Z\",\"log_type\":\"alert\",\"status\":\"new\",\"alert_unique_id\":\"11111111-2222-3333-4444-555555555555\"}",
        "outcome": "unknown",
        "severity": 75,
        "start": "2021-04-30T13:21:20.704Z",
        "timezone": "Local",
        "type": [
            "info",
            "start"
        ]
    },
    "harfang_labs": {
        "alert": {
            "aggregation_key": "40fd5973a79d4e0f21e689938e5f269d20a3be780949eb6f408d5cb65c6974d1",
            "execution_mode": 0,
            "level": "high",
            "logonid": 1121594,
            "matched_rules": {
                "namespace": "mimikatz.yar",
                "rulename": [
                    "mimikatz_clear_string_markers",
                    "mimikatz_patches_x64"
                ],
                "source": "default"
            },
            "payload_type": "rtlogs",
            "process": {
                "integrity_level": "High",
                "issuer_name": "",
                "product_version": "203.0.113.10",
                "size": 1430016
            },
            "score": 120,
            "session_id": 2,
            "status": "new",
            "subtype": "process",
            "type": "yara"
        },
        "event": {
            "log_type": "alert"
        }
    },
    "host": {
        "domain": "example.com",
        "hostname": "workstation-01.example.com",
        "name": "workstation-01.example.com",
        "os": {
            "full": "Windows 10 Pro",
            "version": "10.0.19041"
        }
    },
    "input": {
        "type": "udp"
    },
    "log": {
        "source": {
            "address": "198.51.100.50:51514"
        },
        "syslog": {
            "appname": "EDR",
            "facility": {
                "code": 16,
                "name": "local0"
            },
            "hostname": "firewall-01.example.com",
            "priority": 134,
            "severity": {
                "code": 6,
                "name": "Informational"
            },
            "version": "1"
        }
    },
    "message": "Binary was found malicious by 2 rule(s) with a score of 120",
    "process": {
        "code_signature": {
            "subject_name": "",
            "valid": false
        },
        "command_line": "ANONYMIZED",
        "entity_id": "11111111-2222-3333-4444-555555555555",
        "executable": "ANONYMIZED",
        "hash": {
            "md5": "68b329da9893e34099c7d8ad5cb9c940",
            "sha1": "adc83b19e793491b1c6ea0fd8b46cd9f32e592fc",
            "sha256": "01ba4719c80b6fe911b091a7c05124b64eeece964e09c058ef8f9805daca546b"
        },
        "name": "mimikatz.exe",
        "parent": {
            "command_line": "\"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe\" ",
            "entity_id": "11111111-2222-3333-4444-555555555555",
            "executable": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe",
            "pid": 1700
        },
        "pe": {
            "company": "Braveheart",
            "description": "mimikatz for Windows",
            "file_version": "203.0.113.10",
            "imphash": "B6BC868F5046CE7764D0627266AF0200",
            "original_file_name": "mimikatz.exe",
            "product": "mimikatz"
        },
        "pid": 5736,
        "start": "2021-04-30T13:20:54.315Z",
        "working_directory": "ANONYMIZED"
    },
    "related": {
        "hash": [
            "68b329da9893e34099c7d8ad5cb9c940",
            "adc83b19e793491b1c6ea0fd8b46cd9f32e592fc",
            "01ba4719c80b6fe911b091a7c05124b64eeece964e09c058ef8f9805daca546b"
        ],
        "hosts": [
            "workstation-01.example.com"
        ],
        "user": [
            "user1"
        ]
    },
    "rule": {
        "category": "yara",
        "id": "YARA binary check",
        "name": "YARA binary check"
    },
    "tags": [
        "preserve_original_event",
        "forwarded",
        "harfang_labs-event"
    ],
    "user": {
        "domain": "workstation-01.example.com",
        "id": "S-1-5-21-794873159-2603523847-3490392889-500",
        "name": "user1"
    }
}
```


### Data streams using ILM policies



