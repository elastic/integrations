fields:
  tags:
    - preserve_original_event
dynamic_fields:
  # The API (CEL) path sets @timestamp to the ingest time (non-deterministic),
  # since the people inventory snapshot has no per-record modified-at field.
  "@timestamp": "^.*$"
