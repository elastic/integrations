version: '2.3'
services:
  kolide-deprovisioned-person:
    image: docker.elastic.co/observability/stream:v0.20.0
    hostname: kolide-deprovisioned-person
    ports:
      - 8090
    volumes:
      - ./files:/files:ro
    environment:
      PORT: '8090'
    command:
      - http-server
      - --addr=:8090
      - --config=/files/config-deprovisioned-person.yml
