service: threatstream-integrator-test
service_notify_signal: SIGHUP
input: webhook
data_stream:
  vars:
    listen_address: 0.0.0.0
    listen_port: 9080
    secret: TheSecret
    preserve_original_event: true
