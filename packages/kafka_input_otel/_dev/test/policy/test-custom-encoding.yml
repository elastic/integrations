vars:
  logs_topics:
    - app_logs
  logs_encoding: otlp_json
  logs_exclude_topics:
    - excluded_logs
  metrics_topics:
    - app_metrics
  metrics_encoding: otlp_json
  metrics_exclude_topics:
    - excluded_metrics
  traces_topics:
    - app_traces
  traces_encoding: otlp_json
  traces_exclude_topics:
    - excluded_traces
