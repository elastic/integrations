# validation.yml
docs_structure_enforced:
  enabled: true
  version: 1
errors:
  # SVR00002 is intentionally excluded because this package uses a shared vendor namespace pending maintainer guidance.
  exclude_checks: [ "SVR00002" ]
