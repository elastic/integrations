title: Amazon Security Lake Discovery Events
dataset: amazon_security_lake.discovery
type: logs
elasticsearch:
  index_template:
    settings:
      index:
        mapping:
          total_fields:
            limit: 5000
