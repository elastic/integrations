include_matches:
{{#each units}}
  - _SYSTEMD_UNIT={{this}}
{{/each}}
{{#if tags}}
tags:
{{#each tags as |tag|}}
  - {{tag}}
{{/each}}
{{#if preserve_original_event}}
  - preserve_original_event
{{/if}}
{{else}}
{{#if preserve_original_event}}
tags:
  - preserve_original_event
{{/if}}
{{/if}}
{{#if cluster_name}}
{{#if processors}}
processors:
  - add_fields:
      target: proxmox.cluster
      fields:
        name: '{{cluster_name}}'
{{processors}}
{{else}}
processors:
  - add_fields:
      target: proxmox.cluster
      fields:
        name: '{{cluster_name}}'
{{/if}}
{{else}}
{{#if processors}}
processors:
{{processors}}
{{/if}}
{{/if}}
