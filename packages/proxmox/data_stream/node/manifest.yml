title: Proxmox VE node metrics
type: metrics
streams:
  - input: cel
    vars:
      - name: metrics_interval
        type: text
        title: Metrics Collection Interval
        description: How often to poll the Proxmox API for node metrics.
        required: true
        show_user: true
        default: 60s
      - name: tags
        type: text
        title: Tags
        multi: true
        required: false
        show_user: false
        default:
          - proxmox-node
      - name: processors
        type: yaml
        title: Processors
        required: false
        show_user: false
    title: Proxmox VE node metrics
    description: Collect per-node metrics from the Proxmox REST API
elasticsearch:
  index_mode: time_series
