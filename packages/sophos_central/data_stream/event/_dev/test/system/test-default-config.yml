input: httpjson
service: sophos_central
vars:
  request_url: http://{{Hostname}}:{{Port}}
  token_url: http://{{Hostname}}:{{Port}}
  client_id: xxxx
  client_secret: xxxx
  tenant_id: xxxx
data_stream:
  vars:
    preserve_original_event: true
    preserve_duplicate_custom_fields: true
