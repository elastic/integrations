input: cel
service: nextron_thor
data_stream:
  vars:
    url: http://{{Hostname}}:{{Port}}
    api_key: xxxx
    interval: 5m
    initial_interval: 24h
    batch_size: 10
    preserve_original_event: false
assert:
  hit_count: 10
