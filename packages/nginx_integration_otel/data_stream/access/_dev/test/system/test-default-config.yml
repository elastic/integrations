service: nginx
data_stream:
  vars:
    include:
      - "{{SERVICE_LOGS_DIR}}/access.log*"
    start_at: beginning
assert:
  min_count: 30
  fields_present:
    - body.text
    - observed_timestamp
    - attributes.log.file.name