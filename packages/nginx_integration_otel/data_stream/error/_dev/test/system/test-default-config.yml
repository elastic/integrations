service: nginx
service_notify_signal: SIGHUP
data_stream:
  vars:
    include:
      - "{{SERVICE_LOGS_DIR}}/error.log*"
    start_at: beginning
assert:
  min_count: 5
  fields_present:
    - body.text
    - observed_timestamp
    - attributes.log.file.name
