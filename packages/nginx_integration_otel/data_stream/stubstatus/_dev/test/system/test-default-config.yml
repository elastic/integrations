service: nginx
data_stream:
  vars:
    endpoint: "http://{{Hostname}}:{{Port}}/server-status"
assert:
  min_count: 30
  fields_present:
    - metrics.nginx.connections_accepted
    - metrics.nginx.connections_handled
    - metrics.nginx.connections_current
    - metrics.nginx.requests