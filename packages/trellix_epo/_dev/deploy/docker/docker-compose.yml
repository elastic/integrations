version: '2.3'
services:
  trellix_epo:
    image: docker.elastic.co/observability/stream:v0.10.0
    hostname: trellix_epo
    ports:
      - 8090
    volumes:
      - ./files:/files:ro
    environment:
      PORT: '8090'
    command:
      - http-server
      - --addr=:8090
      - --config=/files/config.yml
