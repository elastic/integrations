service: cisco-logfile
input: logfile
data_stream:
  vars:
    paths:
      - "{{SERVICE_LOGS_DIR}}/*ftd*.log"
    preserve_original_event: true
    consider_private_internal: true
    tz_offset: "+0500"
