# newer versions go on top
- version: "0.2.0"
  changes:
    - description: Handle geometry request network errors gracefully using `is_error` so that a timed-out polygon fetch no longer fails the entire evaluation. Add configurable `request_timeout` variable.
      type: enhancement
      link: https://github.com/elastic/integrations/pull/21119
- version: "0.1.0"
  changes:
    - description: Initial draft of the package
      type: enhancement
      link: https://github.com/elastic/integrations/pull/19290
