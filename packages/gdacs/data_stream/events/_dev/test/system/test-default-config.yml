input: cel
service: gdacs
vars:
  logging:
    level: debug
data_stream:
  vars:
    interval: 10s
    lookback_hours: 168
    event_types: "EQ;TC;FL;VO;DR;WF"
    alert_levels: "red;orange;green"
    page_size: 1
    url: http://{{Hostname}}:{{Port}}/gdacsapi/api
assert:
  hit_count: 3
