input: cel
service: island_browser
vars:
  url: http://{{Hostname}}:{{Port}}
  api_key: xxxx
data_stream:
  vars:
    preserve_original_event: true
    preserve_duplicate_custom_fields: true
    batch_size: 2
    initial_interval: 24h
assert:
  hit_count: 5
