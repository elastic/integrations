rules:
  - path: /
    methods: ["GET"]
    responses:
      - status_code: 200
        headers:
          Content-Type:
            - "application/json"
        body: |-
          {"service.name":"package-registry","service.version":"1.0.0"}
