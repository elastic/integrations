---
description: Pipeline for processing Elastic Managed Hello World generic logs.
processors:
  - set:
      field: ecs.version
      value: '9.1.0'
  - remove:
      field:
        - organization
        - division
        - team
      ignore_missing: true
      if: ctx.organization instanceof String && ctx.division instanceof String && ctx.team instanceof String
      tag: remove_agentless_tags
      description: >-
        Removes the fields added by Elastic Managed Integration as metadata, as they can collide with ECS fields.
  - json:
      tag: parse_message_json
      field: message
      target_field: _parsed
      if: ctx.message instanceof String && ctx.message.trim().startsWith('{')
      ignore_failure: true
  - set:
      tag: extract_cursor_from_message
      if: ctx._parsed?.cursor != null
      field: cursor
      copy_from: _parsed.cursor
  - set:
      tag: extract_http_from_message
      if: ctx._parsed?.http != null
      field: http
      copy_from: _parsed.http
  - remove:
      tag: cleanup_parsed
      field:
        - _parsed
        - message
      ignore_missing: true
  - terminate:
      tag: data_collection_error
      if: ctx.error?.message != null && ctx.message == null && ctx.event?.original == null
  - set:
      field: event.kind
      value: event
  - set:
      field: event.type
      value: [info]
  - set:
      field: event.category
      value: [web]
  - remove:
      field: event.original
      if: ctx.tags == null || !(ctx.tags.contains('preserve_original_event'))
      ignore_failure: true
      ignore_missing: true
  - script:
      lang: painless
      description: This script processor iterates over the whole document to remove fields with null values.
      source: |
        void handleMap(Map map) {
          for (def x : map.values()) {
            if (x instanceof Map) {
                handleMap(x);
            } else if (x instanceof List) {
                handleList(x);
            }
          }
          map.values().removeIf(v -> v == null);
        }
        void handleList(List list) {
          for (def x : list) {
              if (x instanceof Map) {
                  handleMap(x);
              } else if (x instanceof List) {
                  handleList(x);
              }
          }
        }
        handleMap(ctx);
on_failure:
  - set:
      field: event.kind
      value: pipeline_error
  - append:
      field: error.message
      value: >-
        Processor '{{{ _ingest.on_failure_processor_type }}}'
        {{#_ingest.on_failure_processor_tag}}with tag '{{{ _ingest.on_failure_processor_tag }}}'
        {{/_ingest.on_failure_processor_tag}}failed with message '{{{ _ingest.on_failure_message }}}'
