config_version: 2
interval: 20s
resource.timeout: 15s
resource.url: "{{url}}"
state:
  url: "{{url}}"
program: |
  request("GET", state.url)
    .do_request()
    .as(resp, {
        "events": [{
            "message": {
                "status_code": resp.StatusCode
            }.encode_json()
        }]
    })
tags:
  - agentless-hello-world
