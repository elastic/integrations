title: "alerts"
type: logs
#ilm_policy: logs-cloud_defend.alerts-default_policy TODO retention policy
elasticsearch:
  index_template:
    mappings:
      dynamic: false
streams:
  - input: cloud_defend/control
    template_path: stream.yml.hbs
    title: BPF/LSM controls
    description: Enables BPF/LSM control mechanisms, for use with FIM and container drift prevention.
