process_context_matchers:
{{#each process_context_matchers }}
  - {{this}}
{{/each}}

matchers_response_bindings:
{{#each matchers_response_bindings}}
  - {{this}}
{{/each}}

responses:
{{#each responses}}
  - {{this}}
{{/each}}
