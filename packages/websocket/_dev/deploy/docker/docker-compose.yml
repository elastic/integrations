version: "2.3"
services:
  websocket-mock-service:
    image: golang:1.21-alpine
    hostname: websocket
    working_dir: /app
    volumes:
      - ./websocket-mock-service:/app
    ports:
      - "3000:3000"
    command: ["go", "run", "main.go"]
