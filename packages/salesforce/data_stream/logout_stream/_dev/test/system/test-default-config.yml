service: salesforce
vars:
  client_id: temp_client_id_1
  client_secret: forty_characters_long_secret_key
  username: temp_user
  password: temp_password
  token_url: http://{{Hostname}}:{{Port}}/services/oauth2/token
input: cometd
data_stream:
  vars:
    preserve_original_event: true
