service: apm-app
service_notify_signal: SIGHUP
vars:
  endpoint: "0.0.0.0:8200"
# the test service sends all signals, this test only checks for metrics
signal_types:
  - metrics
assert:
  min_count: 50
