# App blocks on SIGHUP until agent is ready; elastic-package sends SIGHUP when policy is applied.
wait_for_data_timeout: 2m
service: apm-app
service_notify_signal: SIGHUP
vars:
  endpoint: "0.0.0.0:8200"
assert:
  min_count: 5
