service: apm-app
service_notify_signal: SIGHUP
vars:
  endpoint: "0.0.0.0:8200"
# the test service sends all signals, this test checks for traces (with span events routed to logs)
signal_types:
  - traces
  - logs
assert:
  min_count: 50
