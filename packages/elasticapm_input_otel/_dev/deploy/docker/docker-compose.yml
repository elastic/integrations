version: '2.3'
services:
  # Simple web service for testing basic connectivity
  # APM agents would connect to the collector endpoint, not this service
  web:
    image: nginx:1.29.0-alpine-slim
    ports:
      - 80
