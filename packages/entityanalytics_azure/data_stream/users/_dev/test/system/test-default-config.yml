input: entity-analytics
service: azuread
data_stream:
  vars:
    tenant_id: TENANT_ID
    client_id: CLIENT_ID
    secret: foobar
    login_endpoint: http://{{Hostname}}:{{Port}}
    api_endpoint: http://{{Hostname}}:{{Port}}/v1.0
