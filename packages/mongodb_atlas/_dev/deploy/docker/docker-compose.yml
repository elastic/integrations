version: '2.3'
services:
  mongodb_atlas:
    image: docker.elastic.co/observability/stream:v0.13.0
    hostname: mongodb_atlas
    ports:
      - 8010
    volumes:
      - ./files:/files:ro
    environment:
      PORT: 8010
    command:
      - http-server
      - --addr=:8010
      - --config=/files/config.yml
