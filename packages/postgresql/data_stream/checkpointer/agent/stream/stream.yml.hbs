metricsets: ["query"]
period: {{period}}
hosts:
{{#each hosts}}
  - {{this}}
{{/each}}
tags:
{{#each tags as |tag|}}
  - {{tag}}
{{/each}}
{{#if processors}}
processors:
{{processors}}
{{/if}}
{{#if ssl}}
{{ssl}}
{{/if}}
driver: "postgres"
sql_queries:
  - query: "SELECT * FROM pg_stat_checkpointer;"
    response_format: "table"