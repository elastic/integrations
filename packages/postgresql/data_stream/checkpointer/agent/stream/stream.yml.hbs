metricsets: ["query"]
hosts:
  - "postgres://postgres:postgres@postgres-17:5432/postgres?sslmode=disable"
{{#if condition}}
condition: {{ condition }}
{{/if}}
driver: "postgres"
sql_queries:
  - query: "SELECT * FROM pg_stat_checkpointer;"
    response_format: table
raw_data.enabled: true
period: {{period}}
{{#if ssl}}
ssl: {{ssl}}
{{/if}}
{{#if processors}}
processors:
{{processors}}
{{/if}}
