metricsets: ["query"]
hosts:
{{#each hosts}}
  - {{this}}
{{/each}}
driver: {{driver}}
sql_queries: {{sql_queries}}
raw_data.enabled: true
period: {{period}}
merge_results: {{merge_results}}
data_stream:
  dataset: {{data_stream.dataset}}
