metricsets: ["query"]
hosts:
{{#each hosts}}
  - {{this}}
{{/each}}
driver: {{driver}}
sql_queries: {{sql_queries}}
raw_data.enabled: true
period: {{period}}
data_stream:
  dataset: {{data_stream.dataset}}
