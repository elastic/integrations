service: mysql_replica
vars:
  hosts:
    - tcp({{Hostname}}:{{Port}})/
  username: mydb_replica_user
  password: mydb_replica_pwd
  replication_status_query: "SHOW REPLICA STATUS;"
data_stream:
  vars: ~
