paths:
{{#each paths as |path|}}
  - {{path}}
{{/each}}


tags:
{{#each tags as |tag|}}
  - {{tag}}
{{/each}}
{{#if experimental_conversion}}
  - experimental_ecs_conversion
{{/if}}
{{#if preserve_original_event}}
  - preserve_original_event
{{/if}}

{{#if processors}}
processors:
{{processors}}
{{/if}}