{
  "id": "airflow_otel-dag-file-queue-large",
  "type": "alerting_rule_template",
  "managed": true,
  "attributes": {
    "name": "[Airflow OTel] DAG file processing queue large",
    "ruleTypeId": ".es-query",
    "tags": [
      "observability",
      "airflow",
      "dag-processing"
    ],
    "schedule": {
      "interval": "5m"
    },
    "alertDelay": {
      "active": 2
    },
    "flapping": {
      "lookBackWindow": 10,
      "statusChangeThreshold": 4
    },
    "params": {
      "searchType": "esqlQuery",
      "esqlQuery": {
        "esql": "FROM metrics-airflow.otel-default*\n| WHERE data_stream.dataset == \"airflow.otel\"\n| WHERE airflow.dag_processing.file_path_queue_size IS NOT NULL\n// Gauge: DAG files pending processing; growing queue = processor falling behind\n| STATS max_queue = MAX(airflow.dag_processing.file_path_queue_size)\n// Adjust threshold (20) for your DAG count\n| WHERE max_queue > 20"
      },
      "size": 0,
      "threshold": [0],
      "thresholdComparator": ">",
      "timeField": "@timestamp",
      "timeWindowSize": 15,
      "timeWindowUnit": "m"
    }
  }
}
