{
    "id": "kubernetes-node-freediskspacefailed-error",
    "type": "alerting_rule_template",
    "attributes": {
        "name": "[Kubernetes] Node FreeDiskSpaceFailed Error",
        "tags": [
            "Kubernetes",
            "Node",
            "Disk"
        ],
        "ruleTypeId": ".es-query",
        "schedule": {
            "interval": "5m"
        },
        "params": {
            "searchType": "esqlQuery",
            "timeWindowSize": 10,
            "timeWindowUnit": "m",
            "esqlQuery": {
                "esql": "// Alert on Kubernetes Node FreeDiskSpaceFailed events.\n//\n// What triggers this alert:\n// Node cannot reclaim disk space \u2014 critical disk exhaustion persists after evictions.\n//\n// Common causes:\n// Large system logs, orphaned volumes, container image buildup, filesystem issues.\n//\n// Action needed:\n// Manual disk cleanup, remove unused images/volumes, expand disk, check filesystem errors.\n//\n// To scope to a cluster, add: AND `orchestrator.cluster.name` == \"YOUR_CLUSTER\"\nFROM metrics-kubernetes.event-*\n| WHERE `kubernetes.event.involved_object.kind` == \"Node\"\n  AND `kubernetes.event.source.component` == \"kubelet\"\n  AND `kubernetes.event.reason` == \"FreeDiskSpaceFailed\"\n| STATS doc_count = COUNT(*)\n  BY `orchestrator.cluster.name`, `kubernetes.event.involved_object.name`,\n     `kubernetes.event.involved_object.kind`, `kubernetes.event.reason`\n| WHERE doc_count > 0"
            },
            "groupBy": "row",
            "timeField": "@timestamp"
        },
        "alertDelay": {
            "active": 3
        }
    },
    "managed": true,
    "coreMigrationVersion": "8.8.0",
    "typeMigrationVersion": "10.1.0"
}
