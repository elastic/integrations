{
    "id": "kubernetes-job-error",
    "type": "alerting_rule_template",
    "attributes": {
        "name": "[Kubernetes] Job Error",
        "tags": [
            "Kubernetes",
            "Job",
            "Availability"
        ],
        "ruleTypeId": ".es-query",
        "schedule": {
            "interval": "5m"
        },
        "params": {
            "searchType": "esqlQuery",
            "timeWindowSize": 10,
            "timeWindowUnit": "m",
            "esqlQuery": {
                "esql": "// Alert when Kubernetes jobs have failed status or failed pods.\n//\n// What triggers this alert:\n// Job reporting failed status or having one or more failed pods.\n//\n// What it means:\n// Job unable to complete \u2014 may impact dependent workflows.\n//\n// Common causes:\n// Bad configuration, missing dependencies, insufficient resources, application bugs.\n//\n// Grouped by cluster, job name, and namespace.\nTS metrics-kubernetes.state_job-*\n| WHERE `kubernetes.job.status.failed` == \"true\"\n  OR `kubernetes.job.pods.failed` > 0\n| STATS doc_count = COUNT(*)\n  BY `orchestrator.cluster.name`, `kubernetes.job.name`, `kubernetes.namespace`\n| WHERE doc_count > 0"
            },
            "groupBy": "row",
            "timeField": "@timestamp"
        },
        "alertDelay": {
            "active": 3
        }
    },
    "managed": true,
    "coreMigrationVersion": "8.8.0",
    "typeMigrationVersion": "10.1.0"
}
