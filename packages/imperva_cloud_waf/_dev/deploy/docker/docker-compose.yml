version: '3.8'
services:
  imperva_cloud_waf:
    image: docker.elastic.co/observability/stream:v0.20.0
    hostname: imperva_cloud_waf
    ports:
      - 8090
    volumes:
      - ./files:/files:ro
    environment:
      PORT: '8090'
    command:
      - http-server
      - --addr=:8090
      - --config=/files/config.yml
