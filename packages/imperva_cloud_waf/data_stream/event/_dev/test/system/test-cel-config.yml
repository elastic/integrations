input: cel
deployer: docker
service: imperva_cloud_waf
data_stream:
  vars:
    url: http://{{Hostname}}:{{Port}}
    api_id: abcd
    api_key: xxxx
    interval: 5m
    preserve_original_event: true
    preserve_duplicate_custom_fields: true
assert:
  hit_count: 2
