---
apiVersion: v1
kind: Service
metadata:
  name: syslog-service
  namespace: kube-system
spec:
  ports:
    - port: 9001
      protocol: TCP
      targetPort: 9001
  selector:
    app: elastic-agent
