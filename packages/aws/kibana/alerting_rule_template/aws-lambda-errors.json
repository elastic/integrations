{
  "id": "aws-lambda-errors",
  "type": "alerting_rule_template",
  "attributes": {
    "name": "[AWS Lambda] Errors High",
    "tags": ["AWS Lambda"],
    "ruleTypeId": ".es-query",
    "schedule": {
      "interval": "5m"
    },
    "params": {
      "searchType": "esqlQuery",
      "timeWindowSize": 5,
      "timeWindowUnit": "m",
      "esqlQuery": {
        "esql": "// Alert triggers when the errors exceeds the recommended threshold value {0} within the look back time window.\n// The alert is grouped by cloud account id, cloud region and function name.\n// You can adjust the threshold value by modifying the errors in the WHERE clause.\nFROM metrics-aws.lambda-default\n| STATS errors=sum(aws.lambda.Errors.avg) by cloud.account.id, cloud.region, aws.dimensions.FunctionName\n| WHERE errors > 0"
      },
      "groupBy": "row",
      "timeField": "event.ingested"
    },
    "alertDelay": {
      "active": 1
    }
  },
  "managed": true,
  "coreMigrationVersion": "8.8.0",
  "typeMigrationVersion": "10.1.0"
}