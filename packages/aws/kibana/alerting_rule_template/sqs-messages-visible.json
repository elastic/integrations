{
  "id": "sqs-messages-visible",
  "type": "alerting_rule_template",
  "attributes": {
    "name": "[AWS SQS] Messages Visible High",
    "tags": ["AWS SQS"],
    "ruleTypeId": ".es-query",
    "schedule": {
      "interval": "5m"
    },
    "params": {
      "searchType": "esqlQuery",
      "timeWindowSize": 5,
      "timeWindowUnit": "m",
      "esqlQuery": {
        "esql": "// Alert triggers when the msgsvisible exceeds the recommended threshold value {1000} within the look back time window.\n// The alert is grouped by cloud account id, cloud region and queue name.\n// You can adjust the threshold value by modifying the msgsvisible in the WHERE clause.\nFROM metrics-aws.sqs-default\n| STATS msgsvisible=max(aws.sqs.messages.visible) by cloud.account.id, cloud.region, aws.dimensions.QueueName\n| WHERE msgsvisible >= 1000"
      },
      "groupBy": "row",
      "timeField": "event.ingested"
    },
    "alertDelay": {
      "active": 1
    }
  },
  "managed": true,
  "coreMigrationVersion": "8.8.0",
  "typeMigrationVersion": "10.1.0"
}