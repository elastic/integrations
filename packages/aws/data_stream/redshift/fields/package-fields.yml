- name: aws
  type: group
  fields:
    - name: tags.*
      type: object
      description: |
        Tag key value pairs from aws resources.
    - name: dimensions.*
      type: object
      description: |
        Metric dimensions.
    - name: '*.metrics.*.*'
      type: object
      description: |
        Metrics that returned from Cloudwatch API query.
