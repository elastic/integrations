- name: aws
  type: group
  fields:
    - name: tags.*
      type: object
      description: |
        Tag key value pairs from aws resources.
    - name: '*.metrics.*.*'
      type: object
      description: |
        Metrics that returned from Cloudwatch API query.
