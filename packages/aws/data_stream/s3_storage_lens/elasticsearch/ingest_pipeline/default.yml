---
processors:
  - fingerprint:
      fields: ["aws.dimensions"]
      target_field: "aws.s3_storage_lens.dimensions_id"
      ignore_missing: true
on_failure:
  - append:
      field: error.message
      value: '{{{ _ingest.on_failure_message }}}'