title: "AWS EKS logs"
type: logs
dataset: aws.eks_logs
# This integration includes predefined rules that automatically route AWS EKS logs to the respective integrations.
# Adding these permissions to ensure the agents have permissions to write data to `logs-*-*`.
elasticsearch:
  dynamic_dataset: true
  dynamic_namespace: true
