---
description: Pipeline for processing tenable OT security cloud assets
processors:

  - rename:
      tag: rename_message_to_tenable_ot_security_assets_934bcc8a
      field: message
      target_field: tenable_ot_security.assets

  - fingerprint:
      fields:
        - tenable_ot_security.assets.id
        - tenable_ot_security.assets.firstSeen
        - tenable_ot_security.assets.lastSeen
        - tenable_ot_security.assets.lastHit
        - tenable_ot_security.assets.lastSnapshot
        - tenable_ot_security.assets.lastUpdate
        - tenable_ot_security.assets.runStatusTime
      tag: fingerprinting_24a5e1f5
      target_field: "_id"
      ignore_missing: true
      on_failure:
        - append:
            tag: append_error_message_8f3586ca
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'


  ##################### Scripts for processing input #####################
  - script:
      tag: script_drops_null_empty_values_recursively_82f51bf1
      description: Drops null/empty values recursively.
      lang: painless
      source: |
        boolean dropEmptyFields(Object object) {
          if (object == null || object == "") {
            return true;
          } else if (object instanceof Map) {
            ((Map) object).values().removeIf(value -> dropEmptyFields(value));
            return (((Map) object).size() == 0);
          } else if (object instanceof List) {
            ((List) object).removeIf(value -> dropEmptyFields(value));
            return (((List) object).size() == 0);
          }
          return false;
        }
        dropEmptyFields(ctx);

  - script:
      description: Convert Additional Info keys to snake case.
      tag: additional-info-keys-to-snake-case_2c62275b
      lang: painless
      source: |
        Map keysToSnakeCase(Map m) {
          def regex = /_?([a-z])([A-Z]+)/;
          def snakeCaseMap = [:];
          for (entry in m.entrySet()) {
            def k = entry.getKey();
            def v = entry.getValue();
            if (v instanceof Map) {
              v = keysToSnakeCase(v);
            } else if (v instanceof List) {
              for (int i = 0; i < v.size(); i++) {
                def item = v.get(i);
                if (item instanceof Map) {
                  v.set(i, keysToSnakeCase(item));
                }
              }
            }
            k = regex.matcher(k).replaceAll('$1_$2').toLowerCase();
            snakeCaseMap.put(k, v);
          }
          return snakeCaseMap;
        }
        if (ctx.tenable_ot_security.assets != null) {
          ctx.tenable_ot_security.assets = keysToSnakeCase(ctx.tenable_ot_security.assets);
        }

  - script:
      tag: script_recursively_normalize_every_occurrence_nodes_5384ed97
      description: Recursively normalize every occurrence of 'nodes'.
      lang: painless
      source: |
        def normalize(def obj) {
          if (obj instanceof Map) {
                Map newObj = new HashMap();
                for (entry in obj.entrySet()) {
                    def key = entry.getKey();
                    def value = entry.getValue();

                    // If the value is a map with a single key "nodes", replace it with its contents
                    if (value instanceof Map && value.containsKey("nodes")) {
                        value = value.get("nodes");
                    }

                    // Recursively process if value is a map or list
                    newObj.put(key, normalize(value));
                }
                return newObj;
            } else if (obj instanceof List) {
                List newList = new ArrayList();
                for (item in obj) {
                    newList.add(normalize(item));
                }
                return newList;
            } else {
                return obj;
            }
           }
        // Apply normalization to every occurrence of "nodes" in assets.
        if (ctx.containsKey("tenable_ot_security") && ctx.tenable_ot_security.containsKey("assets")) {
          ctx.tenable_ot_security.assets = normalize(ctx.tenable_ot_security.assets);
        }

  - convert:
      tag: convert_total_risk_to_double_2f9570db
      field: tenable_ot_security.assets.risk.total_risk
      type: double
      if: ctx.tenable_ot_security?.assets?.risk?.total_risk != null
      on_failure:
        - append:
            tag: append_error_message_b7efbbba
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
        - remove:
            tag: remove_tenable_ot_security_assets_risk_total_risk_9eb63d3f
            field: tenable_ot_security.assets.risk.total_risk


  ##################### ECS Fields #####################
  - set:
      tag: set_event_kind_d452ef2c
      field: event.kind
      value: event

  - set:
      tag: set_ecs_version_dbf680ba
      field: ecs.version
      value: "8.17.0"

  - append:
      tag: append_event_category_0eb47497
      field: event.category
      value: iam

  - set:
      tag: set_event_module_c6a78e99
      field: event.module
      value: tenable_ot_security

  - set:
      tag: set_event_type_8bf4a5be
      field: event.type
      value: ["info"]

  - set:
      tag: set_event_dataset_6e0d15b2
      field: event.dataset
      value: tenable_ot_security.assets

  - set:
      tag: set_event_id_a75231ca
      field: event.id
      copy_from: tenable_ot_security.assets.id
      ignore_empty_value: true

  - foreach:
      tag: foreach_tenable_ot_security_assets_direct_ips_9156b652
      field: tenable_ot_security.assets.direct_ips
      if: ctx.tenable_ot_security.assets.direct_ips instanceof List && ctx.tenable_ot_security.assets.direct_ips.length > 0
      processor:
        append:
          tag: append_related_hosts_bcdd2e90
          field: related.hosts
          value: "{{{_ingest._value}}}"
          allow_duplicates: false

  - foreach:
      tag: foreach_tenable_ot_security_assets_direct_macs_186b3e09
      field: tenable_ot_security.assets.direct_macs
      if: ctx.tenable_ot_security.assets.direct_macs instanceof List && ctx.tenable_ot_security.assets.direct_macs.length > 0
      processor:
        append:
          tag: append_related_hosts_cb07e598
          field: related.hosts
          value: "{{{_ingest._value}}}"
          allow_duplicates: false


  #################### Override host.ip ####################
  - foreach:
      tag: foreach_tenable_ot_security_assets_direct_ips_03e4edb8
      field: tenable_ot_security.assets.direct_ips
      if: ctx.tenable_ot_security.assets.direct_ips instanceof List && ctx.tenable_ot_security.assets.direct_ips.length > 0
      processor:
        convert:
          tag: convert_ingest_value_b6438ab7
          field: _ingest._value
          type: ip
          ignore_missing: true

  - foreach:
      tag: foreach_tenable_ot_security_assets_direct_ips_cc5d7a4a
      field: tenable_ot_security.assets.direct_ips
      if: ctx.tenable_ot_security.assets.direct_ips instanceof List && ctx.tenable_ot_security.assets.direct_ips.length > 0
      processor:
        append:
          tag: append_host_ip_accc7843
          field: host.ip
          value: "{{{_ingest._value}}}"
          allow_duplicates: false

  - geoip:
      tag: geoip_host_ip_to_host_geo_165841ea
      field: host.ip
      target_field: host.geo
      ignore_missing: true
      ignore_failure: true

#################### Error Log fields ####################

on_failure:
  - append:
      tag: append_error_message_9b078312
      field: error.message
      value: '{{{_ingest.on_failure_message}}}'

  - set:
      tag: set_event_kind_72b1902f
      field: event.kind
      value: pipeline_error
  - append:
      tag: append_tags_279d5a5c
      field: tags
      value: preserve_original_event
      allow_duplicates: false
