protocol.udp:
  host: "{{syslog_host}}:{{syslog_port}}"
  max_message_size: {{max_message_size}}
  timeout: {{rw_timeout}}
  {{#if read_buffer_size}}
  read_buffer: {{read_buffer_size}}
  {{/if}}
processors:
  - add_locale: ~
