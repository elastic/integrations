- name: prometheus
  type: group
  fields:
    - name: labels.*
      type: object
      description: |
        Prometheus metric labels
    - name: query.*
      type: object
      description: |
        Prometheus value resulted from PromQL

