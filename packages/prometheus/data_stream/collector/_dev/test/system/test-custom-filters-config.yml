# Regression test: metrics_filters.include set through custom yaml.
vars: ~
data_stream:
  vars:
    hosts:
      - "{{Hostname}}:9090"
    custom: |
      metrics_filters.include:
        - "^prometheus_"
