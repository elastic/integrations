version: '2.3'
services:
  nginx:
    build:
      context: .
      args:
        NGINX_SERVICE_VERSION: ${SERVICE_VERSION}
    ports:
      - 80
    volumes:
      - ${SERVICE_LOGS_DIR}:/var/log/nginx
