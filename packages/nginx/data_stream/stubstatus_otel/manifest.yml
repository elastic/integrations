title: Nginx OTEL metrics
type: metrics
dataset: nginx_aliasing_otel
elasticsearch:
  index_template:
    mappings:
      dynamic : true
      dynamic_templates:
        - double:
            mapping:
              type: double
