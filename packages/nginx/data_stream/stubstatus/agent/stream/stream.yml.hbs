metricsets: ["stubstatus"]
hosts:
{{#each hosts}}
  - {{this}}
{{/each}}
tags:
{{#each tags as |tag i|}}
  - {{tag}}
{{/each}}
period: {{period}}
{{#if server_status_path}}
server_status_path: {{server_status_path}}
{{/if}}
{{#if processors}}
processors:
{{processors}}
{{/if}}
