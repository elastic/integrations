errors:
  exclude_checks:
    - JSE00001 # rename "message" to "event.original" pattern check; pipeline pre-dates this requirement.
    - SVR00006 # ingest pipeline processors missing required tag; pipeline pre-dates this requirement.
    - SVR00008 # on_failure handler must set event.kind to "pipeline_error"; pipeline pre-dates this requirement.
    - SVR00009 # on_failure error.message must include processor context fields; pipeline pre-dates this requirement.
