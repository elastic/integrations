type: metrics
title: Elasticsearch index_recovery metrics
release: experimental
streams:
  - input: elasticsearch/metrics
    title: Elasticsearch index_recovery metrics
    description: Collect Elasticsearch metrics of index in recovery state
    vars:
      - name: active.only
        type: bool
        title: Fetch active only indices
        multi: false
        required: false
        show_user: true
        default: true
