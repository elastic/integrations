type: elasticsearch/metrics
dataset: elasticsearch.stack_monitoring.index_recovery
vars:
  hosts:
    - "http://{{Hostname}}:9200"
  username: elastic
  password: changeme
data_stream:
  vars:
    active.only: false
