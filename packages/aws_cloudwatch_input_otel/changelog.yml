# newer versions go on top
- version: "0.5.0"
  changes:
    - description: Initial AWS CloudWatch OpenTelemetry input package using the `awscloudwatchmetrics` receiver. Curated service toggles for AWS/EC2, AWS/RDS, AWS/SQS, AWS/ApplicationELB, AWS/Lambda, and AWS/ECS (Fargate). Agent and agentless deployment modes enabled. NOTE -  `awscloudwatchmetrics` receiver is not yet bundled in EDOT; the package compiles and lints but cannot run end-to-end until the receiver is added to EDOT.
      type: enhancement
      link: https://github.com/elastic/integrations/pull/1
