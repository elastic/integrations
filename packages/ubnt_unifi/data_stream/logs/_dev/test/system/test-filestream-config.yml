service: test-filestream
input: filestream
data_stream:
  vars:
    paths:
      - "{{SERVICE_LOGS_DIR}}/logs-filestream-*.log"
    preserve_original_event: true
    preserve_duplicate_custom_fields: true
assert:
  hit_count: 28
