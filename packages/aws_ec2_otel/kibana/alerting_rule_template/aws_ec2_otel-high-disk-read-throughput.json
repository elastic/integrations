{
    "id": "aws_ec2_otel-high-disk-read-throughput",
    "type": "alerting_rule_template",
    "managed": true,
    "attributes": {
        "description": "Alerts when disk read bytes exceed a threshold over the lookback window, indicating high instance-store or hypervisor-view read I/O demand.",
        "name": "[AWS EC2 OTel] High disk read throughput",
        "ruleTypeId": ".es-query",
        "tags": [
            "observability",
            "aws",
            "ec2",
            "otel"
        ],
        "schedule": {
            "interval": "5m"
        },
        "alertDelay": {
            "active": 2
        },
        "params": {
            "searchType": "esqlQuery",
            "esqlQuery": {
                "esql": "FROM metrics-aws.ec2.otel-*\n| WHERE attributes.Namespace == \"AWS/EC2\"\n    AND attributes.MetricName == \"DiskReadBytes\"\n    AND attributes.stat == \"Sum\"\n| EVAL ec2_instance = CONCAT(attributes.InstanceId, \" (\", resource.attributes.cloud.region, \")\")\n| STATS total_read_bytes = SUM(`metrics.amazonaws.com/AWS/EC2/DiskReadBytes`) BY ec2_instance\n// Bytes threshold: tune per instance size and workload — Sum stat is bytes per CloudWatch period\n| WHERE total_read_bytes > 536870912\n| SORT total_read_bytes DESC\n| LIMIT 100"
            },
            "size": 0,
            "threshold": [0],
            "thresholdComparator": ">",
            "timeField": "@timestamp",
            "timeWindowSize": 15,
            "timeWindowUnit": "m",
            "groupBy": "row",
            "termField": "ec2_instance",
            "termSize": 100,
            "excludeHitsFromPreviousRun": true
        }
    }
}
