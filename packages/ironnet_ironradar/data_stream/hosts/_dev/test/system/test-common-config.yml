input: httpjson
service: ironradar
vars:
  logging.level: debug
data_stream:
  vars:
    request_url: http://{{Hostname}}:{{Port}}/integrations/all/1d/json
    api_key: password
    tags:
      - forwarded
      - ironradar
      - preserve_original_event
