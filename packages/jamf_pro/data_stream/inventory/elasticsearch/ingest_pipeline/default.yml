---
description: Pipeline for processing sample logs
# processors:
# - fingerprint:
#     if: ctx.udid != null
#     fields:
#     - udid
#     target_field: "_id"
#     ignore_missing: true
on_failure:
- set:          
    field: error.message
    value: '{{ _ingest.on_failure_message }}'
