---
description: Process inventory data from the Jamf Pro API
processors:

- rename:
    tag: rename_message_to_jamf_pro_inventory_d5de13ec
    field: message
    target_field: jamf_pro.inventory

- script:
    tag: script_a3eb5add
    description: Drops null/empty values recursively.
    lang: painless
    source: |
      boolean dropEmptyFields(Object object) {
        if (object == null || object == "") {
          return true;
        } else if (object instanceof Map) {
          ((Map) object).values().removeIf(value -> dropEmptyFields(value));
          return (((Map) object).size() == 0);
        } else if (object instanceof List) {
          ((List) object).removeIf(value -> dropEmptyFields(value));
          return (((List) object).length == 0);
        }
        return false;
      }
      dropEmptyFields(ctx);

- script:
    description: Convert Additional Info keys to snake case.
    tag: additional-info-keys-to-snake-case
    lang: painless
    source: |
      Map keysToSnakeCase(Map m) {
        def regex = /_?([a-z])([A-Z]+)/;
        def snakeCaseMap = [:];

        for (entry in m.entrySet()) {
          def k = entry.getKey();
          def v = entry.getValue();

          if (v instanceof Map) {
            v = keysToSnakeCase(v);
          } else if (v instanceof List) {
            for (int i = 0; i < v.size(); i++) {
              def item = v.get(i);
              if (item instanceof Map) {
                v.set(i, keysToSnakeCase(item));
              }
            }
          }

          k = regex.matcher(k).replaceAll('$1_$2').toLowerCase();
          snakeCaseMap.put(k, v);
        }
        return snakeCaseMap;
      }

      if (ctx.jamf_pro.inventory != null) {
        ctx.jamf_pro.inventory = keysToSnakeCase(ctx.jamf_pro.inventory);
      }

- fingerprint:
    tag: fingerprint_a6dde3e1
    fields:
      - jamf_pro.inventory.udid
      - jamf_pro.inventory.general.report_date
    target_field: "_id"

- geoip:
    tag: geoip_jamf_pro_inventory_general_last_ip_address_to_jamf_pro_inventory_general_last_ip_address_geo_51eed0b8
    field: jamf_pro.inventory.general.last_ip_address
    target_field: jamf_pro.inventory.general.last_ip_address_geo
    ignore_missing: true
- set:
    tag: set_ecs_version_f5923549
    field: ecs.version
    value: '8.17.0'

##############
# ECS compat #
##############

- append:
    tag: append_host_ip_036ddbf2
    if: "ctx.jamf_pro.inventory.general != null && ctx.jamf_pro.inventory.general.last_ip_address != ''"
    field: host.ip
    value: '{{{jamf_pro.inventory.general.last_ip_address}}}'
    ignore_failure: true
- set:
    tag: set_host_id_d9f48262
    field: host.id
    copy_from: jamf_pro.inventory.udid
    ignore_empty_value: true
- set:
    tag: set_host_name_3e068ef3
    field: host.name
    copy_from: jamf_pro.inventory.general.name
    ignore_empty_value: true
- set:
    tag: set_host_address_8760bf68
    field: host.address
    copy_from: host.ip
    ignore_empty_value: true
- append:
    tag: append_host_mac_bcfc0ce8
    if: "ctx.jamf_pro.inventory.hardware != null && ctx.jamf_pro.inventory.hardware.mac_address != ''"
    field: host.mac
    value: '{{{jamf_pro.inventory.hardware.mac_address}}}'
    ignore_failure: true
- gsub:
    field: host.mac
    pattern: '[:.]'
    replacement: '-'
    tag: gsub_host_mac
    ignore_missing: true
    on_failure:
      - remove:
          tag: remove_host_mac_27cf896f
          field: host.mac
          ignore_missing: true
      - append:
          tag: append_error_message_1031db47
          field: error.message
          value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
- uppercase:
    field: host.mac
    tag: uppercase_host_mac
    ignore_missing: true
    if: ctx.host?.mac != ''
    on_failure:
      - remove:
          tag: remove_host_mac_762f8874
          field: host.mac
          ignore_missing: true
      - append:
          tag: append_error_message_6b5e084c
          field: error.message
          value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
- set:
    tag: set_host_geo_9f1f0bd4
    field: host.geo
    copy_from: jamf_pro.inventory.general.last_ip_address_geo
    ignore_empty_value: true
    ignore_failure: true
- set:
    tag: set_host_entity_attributes_managed_bfbdd343
    field: host.entity.attributes.managed
    copy_from: jamf_pro.inventory.general.remote_management.managed
    ignore_empty_value: true
- set:
    tag: set_host_entity_lifecycle_last_activity_6e6e376f
    field: host.entity.lifecycle.last_activity
    copy_from: jamf_pro.inventory.general.last_contact_time
    ignore_empty_value: true
- set:
    tag: set_host_os_name_382c3eba
    field: host.os.name
    copy_from: jamf_pro.inventory.operating_system.name
    ignore_empty_value: true
- script:
    tag: script_normalize_operating_system_version
    lang: painless
    if: ctx.jamf_pro?.inventory?.operating_system?.version != null && ctx.jamf_pro.inventory.operating_system.version != ''
    source: |-
      String normalize(String s) {
        int n = 0;
        for (int i = 0; i < s.length(); i++){
            char c = s.charAt(i);
            if (c == (char)'.') {
              n++;
              continue;
            }
            if (c < (char)'0' || (char)'9' < c) {
              // If we have non-numeric parts, bail.
              return s;
            }
        }
        if (n >= 2) {
            return s;
        }
        if (n == 1) {
            return s + ".0";
        }
        return s + ".0.0";
      }
      String full_name(String s) {
        if (s.startsWith('15.')) {
          return 'sequoia';
        }
        if (s.startsWith('14.')) {
          return 'sonoma';
        }
        if (s.startsWith('13.')) {
          return 'ventura';
        }
        if (s.startsWith('12.')) {
          return 'monterey';
        }
        if (s.startsWith('11.')) {
          return 'big sur';
        }
        if (s.startsWith('10.15.')) {
          return 'catalina';
        }
        if (s.startsWith('10.14.')) {
          return 'mojave';
        }
        if (s.startsWith('10.13.')) {
          return 'high sierra';
        }
        if (s.startsWith('10.12.')) {
          return 'sierra';
        }
        if (s.startsWith('10.11.')) {
          return 'el capitan';
        }
        if (s.startsWith('10.10.')) {
          return 'yosemite';
        }
        if (s.startsWith('10.9.')) {
          return 'mavericks';
        }
        return '';
      }
      ctx.jamf_pro.inventory.operating_system.version = normalize(ctx.jamf_pro.inventory.operating_system.version);
      String name = full_name(ctx.jamf_pro.inventory.operating_system.version);
      if (name != '') {
        ctx.host = ctx.host ?: [:];
        ctx.host.os = ctx.host.os ?: [:];
        ctx.host.os.full = name;
      }
- set:
    tag: set_host_os_version_167e76e0
    field: host.os.version
    copy_from: jamf_pro.inventory.operating_system.version
    ignore_empty_value: true
- set:
    tag: set_host_os_platform_f077c83b
    field: host.os.platform
    copy_from: jamf_pro.inventory.general.platform
    ignore_empty_value: true
- set:
    tag: set_user_name_78d20b57
    field: user.name
    copy_from: jamf_pro.inventory.user_and_location.name
    ignore_empty_value: true
- set:
    tag: set_user_email_41e85901
    field: user.email
    copy_from: jamf_pro.inventory.user_and_location.email
    ignore_empty_value: true
- set:
    tag: set_user_full_name_4beacbed
    field: user.full_name
    copy_from: jamf_pro.inventory.user_and_location.realname
    ignore_empty_value: true
- foreach:
    tag: foreach_jamf_pro_inventory_group_memberships_b5a5a3a2
    field: jamf_pro.inventory.group_memberships
    if: ctx.jamf_pro.inventory?.group_memberships != null
    processor:
      append:
        field: user.group.name
        value: '{{{_ingest._value.group_name}}}'
        allow_duplicates: false
    ignore_missing: true
    ignore_failure: true
- foreach:
    tag: foreach_jamf_pro_inventory_group_memberships_3a061922
    field: jamf_pro.inventory.group_memberships
    if: ctx.jamf_pro.inventory?.group_memberships != null
    processor:
      append:
        field: user.group.id
        value: '{{{_ingest._value.group_id}}}'
        allow_duplicates: false
    ignore_missing: true
    ignore_failure: true
- append:
    tag: append_related_user_93408443
    field: related.user
    value: '{{{user.email}}}'
    if: ctx.user?.email != null && ctx.user.email != ""
    allow_duplicates: false
- set:
    tag: set_related_ip_3008f8de
    field: related.ip
    copy_from: host.ip
    ignore_empty_value: true

####################
# Asset definition #
####################

- set:
    tag: set_event_kind_18c57bf4
    field: event.kind
    value: asset

- append:
    tag: append_event_type_7a1a4ee6
    field: event.type
    value: user
    if: ctx.jamf_pro.inventory.user_and_location != null
    allow_duplicates: false
- append:
    tag: append_event_type_2e1f55b2
    field: event.type
    value: info
    if: ctx.jamf_pro.inventory.hardware != null
    allow_duplicates: false

##################
# Error handling #
##################

on_failure:
- set:
    field: event.kind
    value: pipeline_error
- append:
    field: tags
    value: preserve_original_event
    allow_duplicates: false
- append:
    field: error.message
    value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
