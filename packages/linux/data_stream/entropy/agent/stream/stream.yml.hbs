metricsets: ["entropy"]
condition: ${host.platform} == 'linux'
period: {{period}}
{{#if system.hostfs}}
system.hostfs: {{system.hostfs}}
{{/if}}
processors:
  - drop_fields:
      fields: event.module