- source_dataset: generic.otel
  rules:
    - target_dataset: azure.events
      if: 'ctx.resource?.attributes?.event?.name == "az.resource.log" || ctx.body?.structured != null'
      namespace:
        - "{{data_stream.namespace}}"
        - default  
