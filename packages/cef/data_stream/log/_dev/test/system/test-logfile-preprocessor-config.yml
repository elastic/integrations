service: cef-log-logfile
input: logfile
data_stream:
  vars:
    preprocessors: |
      - set:
          field: "foo"
          value: "bar"
    decode_cef_timezone: UTC
    paths:
      - '{{SERVICE_LOGS_DIR}}/*.log'
