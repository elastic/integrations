name: cef
title: Common Event Format (CEF)
version: "2.6.1"
release: ga
description: Collect logs from CEF Logs with Elastic Agent.
type: integration
format_version: 1.0.0
license: basic
categories:
  - network
  - security
conditions:
  kibana.version: ^8.1.0
policy_templates:
  - name: cef
    title: CEF logs
    description: Collect logs from CEF instances
    inputs:
      - type: filestream
        title: "Collect CEF application logs (input: filestream)"
        description: "Collecting application logs from CEF instances (input: filestream)"
      - type: udp
        title: "Collect CEF application logs (input: udp)"
        description: "Collecting application logs from CEF instances (input: udp)"
owner:
  github: elastic/security-external-integrations
