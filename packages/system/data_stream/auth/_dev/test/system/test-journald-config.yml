service: system-auth-journald
input: journald
data_stream:
  vars:
    paths:
      - "/run/service_logs/system-auth.journal"
    condition: ""
agent:
  base_image: systemd # Related issue: https://github.com/elastic/integrations/issues/10998
  runtime: docker
assert:
  hit_count: 9
