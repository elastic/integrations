metricsets: ["network"]
period: {{period}}
network.interfaces:
{{#each network.interfaces}}
- {{this}}
{{/each}}
{{#if processors.length}}
processors:
{{processors}}
{{/if}}
{{#if tags.length}}
tags:
{{#each tags as |tag i|}}
- {{tag}}
{{/each}}
{{/if}}