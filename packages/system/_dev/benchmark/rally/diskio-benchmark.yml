---
description: Benchmark 20000 system.diskio events ingested
data_stream:
  name: diskio
corpora:
  generator:
    total_events: 20000
    template:
      type: gotext
      path: ./diskio-benchmark/template.ndjson
    config:
      path: ./diskio-benchmark/config.yml
    fields:
      path: ./diskio-benchmark/fields.yml
