metricsets: ["uptime"]
core.metrics:
{{#each core.metrics}}
  - {{this}}
{{/each}}
cpu.metrics:
{{#each cpu.metrics}}
  - {{this}}
{{/each}}
period: {{period}}
processes:
{{#each processes}}
  - {{this}}
{{/each}}
