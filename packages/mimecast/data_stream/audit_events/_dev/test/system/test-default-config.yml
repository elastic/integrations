input: httpjson
service: mimecast
vars: ~
request.method: "POST"
data_stream:
  vars:
    preserve_original_event: true
    api_key: test
    api_url: http://{{Hostname}}:{{Port}}/api/audit/get-audit-events
