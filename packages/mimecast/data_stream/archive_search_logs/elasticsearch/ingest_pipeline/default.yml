---
description: Pipeline for processing Mimecast archive_search_logs.
processors:
  # Generic event/ecs fields we always want to populate.
  - set:
      tag: set_ecs_version_0f7334bb
      field: ecs.version
      value: '8.11.0'
  - remove:
      field:
        - organization
        - division
        - team
      ignore_missing: true
      if: ctx.organization instanceof String && ctx.division instanceof String && ctx.team instanceof String
      tag: remove_agentless_tags
      description: >-
        Removes the fields added by Agentless as metadata,
        as they can collide with ECS fields.
  - rename:
      field: message
      tag: rename_message_to_event_original
      target_field: event.original
      ignore_missing: true
      description: Renames the original `message` field to `event.original` to store a copy of the original message. The `event.original` field is not touched if the document already has one; it may happen when Logstash sends the document.
      if: ctx.event?.original == null
  - remove:
      field: message
      tag: remove_message
      ignore_missing: true
      description: The `message` field is no longer required if the document has an `event.original` field.
      if: ctx.event?.original != null
  - json:
      tag: json_event_original_to_mimecast_d72eb09c
      description: Parse 'message' JSON contents into a 'mimecast' object.
      field: event.original
      target_field: mimecast
  - drop:
      tag: drop_752a620f
      if: ctx.mimecast?.createTime == null || (ctx.mimecast?.data != null && ctx.mimecast.data.isEmpty())
  - fingerprint:
      tag: fingerprint_b0c83cf3
      fields:
        - mimecast.id
      target_field: "_id"
      ignore_missing: true

  - date:
      tag: date_mimecast_createTime_9e9563ec
      description: Use 'mimecast.createTime' as the '@timestamp'
      field: mimecast.createTime
      timezone: UTC
      formats:
        - "yyyy-MM-dd'T'HH:mm:ssz"
        - "yyyy-MM-dd'T'HH:mm:ssZ"
        - "yyyy-MM-dd'T'HH:mm:ss.Sz"
        - "yyyy-MM-dd'T'HH:mm:ss.SZ"
        - "yyyy-MM-dd'T'HH:mm:ss.SSz"
        - "yyyy-MM-dd'T'HH:mm:ss.SSZ"
        - "yyyy-MM-dd'T'HH:mm:ss.SSSz"
        - "yyyy-MM-dd'T'HH:mm:ss.SSSZ"
        - "yyyy-MM-dd'T'HH:mm:ss.SSSSSSSSSz"
        - "yyyy-MM-dd'T'HH:mm:ss.SSSSSSSSSZ"
        - "yyyy-MM-dd'T'HH:mm:ss z"

  ###

  # Search related fields
  - rename:
      tag: rename_mimecast_searchText_to_mimecast_search_details_text_60a182f7
      field: mimecast.searchText
      target_field: mimecast.search_details.text
      ignore_missing: true
  - rename:
      tag: rename_mimecast_description_to_mimecast_search_details_description_e0295549
      field: mimecast.description
      target_field: mimecast.search_details.description
      ignore_missing: true
  - rename:
      tag: rename_mimecast_source_to_mimecast_search_details_source_6016c9db
      field: mimecast.source
      target_field: mimecast.search_details.source
      ignore_missing: true
  - rename:
      tag: rename_mimecast_searchPath_to_mimecast_search_details_path_3ecd656f
      field: mimecast.searchPath
      target_field: mimecast.search_details.path
      ignore_missing: true
  - rename:
      tag: rename_mimecast_searchReason_to_mimecast_search_details_reason_4867c79b
      field: mimecast.searchReason
      target_field: mimecast.search_details.reason
      ignore_missing: true

  ###

  # User fields
  - remove:
      tag: remove_mimecast_emailAddr_55db77c8
      field: mimecast.emailAddr
      if: ctx.mimecast?.emailAddr == "<>"
  - rename:
      tag: rename_mimecast_emailAddr_to_user_email_b6f2ff29
      field: mimecast.emailAddr
      target_field: user.email
      ignore_missing: true
  - dissect:
      tag: dissect_user_email_00f1db91
      field: user.email
      pattern: "<%{user.email}>"
      ignore_missing: true
      ignore_failure: true
  - split:
      tag: split_user_email_to_user_parts_5e89f261
      field: user.email
      separator: "@"
      target_field: user.parts
      if: 'ctx.user?.email != null'
  - set:
      tag: set_user_name_607743de
      field: user.name
      copy_from: user.parts.0
      if: ctx.user?.parts?.length != null && ctx.user.parts.length > 1
  - set:
      tag: set_user_domain_49f3b1be
      field: user.domain
      copy_from: user.parts.1
      if: ctx.user?.parts?.length != null && ctx.user.parts.length > 1

  - append:
      tag: append_related_user_039a5a91
      field: related.user
      value: "{{{user.name}}}"
      allow_duplicates: false
      if: 'ctx.user?.name !=null'
  - append:
      tag: append_related_user_34fcf415
      field: related.user
      value: "{{{user.email}}}"
      allow_duplicates: false
      if: ctx.user?.email != null

  # Event related fields
  # Sets event.created from the @timestamp field generated by filebeat before being overwritten further down
  - set:
      tag: set_event_created_e3f09e3b
      field: event.created
      copy_from: "@timestamp"
  - set:
      tag: set_event_kind_de80643c
      field: event.kind
      value: event
  - append:
      tag: append_event_category_4520c8f4
      field: event.category
      value: [api]
  - append:
      tag: append_event_type_9d7bc353
      field: event.type
      value: [admin]

  # Cleanup
  - script:
      description: Drops null/empty values recursively
      tag: script_to_drop_empty_values
      lang: painless
      ignore_failure: true
      source: |
        void handleMap(Map map) {
          map.values().removeIf(v -> {
            if (v instanceof Map) {
                handleMap(v);
            } else if (v instanceof List) {
                handleList(v);
            }
            return v == null || v == '' || (v instanceof Map && v.size() == 0) || (v instanceof List && v.size() == 0)
          });
        }
        void handleList(List list) {
          list.removeIf(v -> {
            if (v instanceof Map) {
                handleMap(v);
            } else if (v instanceof List) {
                handleList(v);
            }
            return v == null || v == '' || (v instanceof Map && v.size() == 0) || (v instanceof List && v.size() == 0)
          });
        }
        handleMap(ctx);
  - remove:
      tag: remove_4b706cc6
      description: Cleanup of repeated/unwanted/temporary fields.
      field:
        - mimecast.createTime
        - mimecast.searchPath
        - mimecast.searchText
        - mimecast.description
        - mimecast.searchReason
        - mimecast.source
        - user.parts
      ignore_missing: true

# Error handling
on_failure:
  - set:
      tag: set_event_kind
      field: event.kind
      value: pipeline_error
  - append:
      tag: append_tags
      field: tags
      value: preserve_original_event
      allow_duplicates: false
  - append:
      tag: append_error_message
      field: error.message
      value: >-
        Processor '{{{ _ingest.on_failure_processor_type }}}'
        {{{#_ingest.on_failure_processor_tag}}}with tag '{{{ _ingest.on_failure_processor_tag }}}'
        {{{/_ingest.on_failure_processor_tag}}}failed with message '{{{ _ingest.on_failure_message }}}'
