paths:
{{#each paths as |path i|}}
 - {{path}}
{{/each}}
exclude_files: [".gz$"]
processors:
  - decode_json_fields:
      target: "json"
      fields: ["message"]
      overwrite_keys: true
      add_error_key: true
  - add_fields:
      target: ""
      fields:
        ecs.version: 1.9.0
