version: '3.0'
services:
  vectra_cloud:
    image: docker.elastic.co/observability/stream:v0.17.1
    hostname: vectra_cloud
    ports:
      - 8090
    volumes:
      - ./files:/files:ro
    environment:
      PORT: '8090'
    command:
      - http-server
      - --addr=:8090
      - --config=/files/config.yml