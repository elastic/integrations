title: Alerts V2
type: logs
dataset: netskope.alerts_v2
streams:
  - input: cel
    title: Netskope alerts via REST API v2 dataexport
    description: Collect Netskope alert records from the REST API v2 dataexport iterator (`/api/v2/events/dataexport/alerts/{type}`).
    template_path: cel.yml.hbs
    enabled: false
    vars:
      - name: initial_interval
        type: text
        title: Initial Interval
        description: How far back to pull the Alert logs from the Netskope API. Supported units for this parameter are h/m/s.
        multi: false
        required: true
        show_user: true
        default: 24h
      - name: interval
        type: text
        title: Interval
        description: Duration between requests to the Netskope API. Supported units for this parameter are h/m/s.
        default: 5m
        multi: false
        required: true
        show_user: true
      - name: alert_types
        type: text
        title: Alert export types
        description: >-
          Alert export types to collect. Each value is the `{type}` segment in `/api/v2/events/dataexport/alerts/{type}`. Allowed values: `policy`, `dlp`, `malware`, `malsite`, `uba`, `quarantine`, `remediation`, `compromisedcredential`, `ctep`, `securityassessment`, `watchlist`. Enable only types your token can read. Prefer this stream over Events V2 `alert` when both are enabled (same records in two datasets).
        multi: true
        required: true
        show_user: true
        default:
          - uba
          - securityassessment
          - quarantine
          - remediation
          - policy
          - malware
          - malsite
          - compromisedcredential
          - ctep
          - dlp
          - watchlist
      - name: tags
        type: text
        title: Tags
        multi: true
        required: true
        show_user: false
        default:
          - forwarded
          - netskope-alerts_v2
          - netskope-api
      - name: preserve_original_event
        required: true
        show_user: true
        title: Preserve original event
        description: Preserves a raw copy of the original event, added to the field `event.original`.
        type: bool
        multi: false
        default: false
      - name: enable_request_tracer
        type: bool
        title: Enable request tracing
        default: false
        multi: false
        required: false
        show_user: false
        description: The request tracer logs requests and responses to the agent's local file-system for debugging configurations. Enabling this request tracing compromises security and should only be used for debugging. See [documentation](https://www.elastic.co/guide/en/beats/filebeat/current/filebeat-input-cel.html#_resource_tracer_filename) for details.
      - name: processors
        type: yaml
        title: Processors
        multi: false
        required: false
        show_user: false
        description: >-
          Processors are used to reduce the number of fields in the exported event or to enhance the event with metadata. This executes in the agent before the logs are parsed. See [Processors](https://www.elastic.co/guide/en/beats/filebeat/current/filtering-and-enhancing-data.html) for details.
