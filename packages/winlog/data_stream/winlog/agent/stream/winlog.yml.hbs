condition: ${host.platform} == 'windows'
data_stream:
  dataset: {{data_stream.dataset}}
name: {{channel}}
{{#if event_id}}
event_id: {{event_id}}
{{/if}}
{{#if language}}
language: {{language}}
{{/if}}
tags:
{{#each tags}}
  - {{this}}
{{/each}}
{{custom}}