{
  "id": "quota_error_rates",
  "type": "alerting_rule_template",
  "attributes": {
    "name": "[Azure OpenAI] Quota Error Rates above threshold",
    "tags": ["Azure OpenAI"],
    "ruleTypeId": ".es-query",
    "schedule": {
      "interval": "1m"
    },
    "params": {
      "searchType": "esqlQuery",
      "timeWindowSize": 15,
      "timeWindowUnit": "m",
      "esqlQuery": {
        "esql": "// Alert triggers when the quota_error is greater than recommended threshold value {0} within the look back time window.\n// The alert is grouped by Model Name.\n// You can adjust the threshold value by modifying the quota_error count in the WHERE clause. \n FROM logs-azure_openai.logs-default\n| STATS quota_error = COUNT(http.response.status_code == 429) by azure.dimensions.model_deployment_name\n| WHERE quota_error > 0"
      },
      "groupBy": "row",
      "timeField": "@timestamp"
    },
    "alertDelay": {
      "active": 1
    }
  },
  "managed": true,
  "coreMigrationVersion": "8.8.0",
  "typeMigrationVersion": "10.1.0"
}