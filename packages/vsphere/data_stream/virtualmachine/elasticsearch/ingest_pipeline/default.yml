---
description: Pipeline for virtualmachine
processors:
- set:
    field: ecs.version
    value: '8.8.0'
on_failure:
  - set:
      field: event.kind
      value: pipeline_error
  - append:
      field: error.message
      value: '{{{ _ingest.on_failure_message }}}'
