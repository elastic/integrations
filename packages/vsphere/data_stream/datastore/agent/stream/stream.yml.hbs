metricsets: ["datastore"]
hosts:
{{#each hosts}}
  - {{this}}
{{/each}}
period: {{period}}
username: {{username}}
password: {{password}}
# If insecure is true, don't verify the server's certificate chain
insecure: {{insecure}}
{{#if processors}}
processors:
{{processors}}
{{/if}}