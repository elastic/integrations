service: vsphere-metrics
vars:
  hosts:
    - "{{Hostname}}:8989/sdk"
  period: 10s
  username: "user"
  password: "password"
  # If insecure is true, don't verify the server's certificate chain
  insecure: true
