title: Jolokia Metrics
type: metrics
release: beta
streams:
  - input: jolokia/metrics
    template_path: stream.yml.hbs
    title: Spring Boot Endpoints Jolokia metrics
    description: Collect Spring Boot Endpoints Jolokia metrics.
    vars:
      - name: path
        type: text
        title: Path
        multi: false
        required: true
        show_user: false
        default: /actuator/jolokia/?ignoreErrors=true&canonicalNaming=false
      - name: period
        type: text
        title: Period
        default: 60s
      - name: username
        type: text
        title: Username
        multi: false
        required: false
        show_user: false
        default: actuator
      - name: password
        type: password
        title: Password
        multi: false
        required: false
        show_user: false
        default: actuator
      - name: hosts
        type: text
        title: Hosts
        multi: false
        required: true
        show_user: true
        description: "Host for Spring Boot Endpoints Jolokia metrics. (example: http://localhost:8090/actuator/jolokia)."
      - name: processors
        type: yaml
        title: Processors
        multi: false
        required: false
        show_user: false
        description: >
          Processors are used to reduce the number of fields in the exported event or to enhance the event with metadata. This executes in the agent before the logs are parsed. See [Processors](https://www.elastic.co/guide/en/beats/filebeat/current/filtering-and-enhancing-data.html) for details.

      - name: ssl
        type: yaml
        title: SSL Configuration
        description: i.e. certificate_authorities, supported_protocols, verification_mode etc.
        multi: false
        required: false
        show_user: false
        default: |-
          #certificate_authorities:
          #  - |
          #    -----BEGIN CERTIFICATE-----
          #    MIIDCjCCAfKgAwIBAgITJ706Mu2wJlKckpIvkWxEHvEyijANBgkqhkiG9w0BAQsF
          #    -----END CERTIFICATE-----
