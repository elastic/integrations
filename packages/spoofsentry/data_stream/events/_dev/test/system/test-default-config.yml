input: http_endpoint
skip:
  reason: >-
    System test requires a live SpoofSentry webhook source (or a faithful payload
    emitter) reachable from the test Agent, which is not available in CI yet.
  link: https://github.com/netallion/domainseal-labs/issues/3
