- source_dataset: aws.cloudtrail.otel
  rules:
    # Route to aws.cloudtrail dataset if event parsing is successful
    - target_dataset: aws.cloudtrail
      if: ctx.pre_extracted == true
      namespace:
        - "{{data_stream.namespace}}"
        - default
