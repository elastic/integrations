title: "Appgate Syslog"
type: logs
streams:
  - input: logfile
    title: Appgate syslog input
    description: Collect Appgate syslog data
    vars:
      - name: paths
        type: text
        title: Log path
        multi: false
        default:
          - /var/log/syslog
