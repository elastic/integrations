service: prometheus_input_otel
service_notify_signal: SIGHUP
vars:
  targets:
    - "prometheus_input_otel:9090/metrics"
  scrape_interval: "10s"
data_stream:
  vars:
    data_stream.dataset: prometheus_input_otel.prometheusreceiver
assert:
  hit_count: 1
