service: prometheus_input_otel
policy_template: prometheusreceiver
vars:
  targets:
    - "{{Hostname}}:{{Port}}"
  scrape_interval: "10s"
  data_stream.dataset: prometheus_input_otel.prometheusreceiver
