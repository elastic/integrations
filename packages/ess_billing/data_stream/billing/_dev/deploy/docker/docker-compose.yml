services:
  ess_billing:
    image: docker.elastic.co/observability/stream:v0.19.0
    ports:
      - 8080
    volumes:
      - ./files:/files:ro
    environment:
      PORT: '8080'
      STREAM_ADDR: ":8080"
    command:
      - http-server
      - --config=/files/stream-config.yml
