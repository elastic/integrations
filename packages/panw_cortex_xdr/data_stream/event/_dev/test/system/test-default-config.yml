input: gcs
service: panw_cortex_xdr-emulator
wait_for_data_timeout: 1m
vars:
  project_id: testproject
  alternative_host: "http://{{Hostname}}:{{Port}}"
  data_stream.dataset: panw_cortex_xdr.event
  buckets: |
    - name: testbucket
      poll: true
      poll_interval: 15s
assert:
  hit_count: 1