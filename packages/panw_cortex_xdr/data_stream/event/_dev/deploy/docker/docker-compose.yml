version: '2.3'
services:
  panw_cortex_xdr-emulator:
    image: fsouza/fake-gcs-server:latest
    command: -host=0.0.0.0 -public-host=elastic-package-service_panw_cortex_xdr-emulator_1 -port=4443 -scheme=http
    volumes:
      - ./sample_logs:/data
    ports:
      - 4443/tcp
