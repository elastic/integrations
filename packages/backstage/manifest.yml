format_version: 3.6.2
name: backstage
title: "Backstage"
version: 0.0.1
source:
  license: "Elastic-2.0"
description: "Fetches audit logs from spotify backstage for security analysis"
type: integration
categories:
  - observability
  - security
  - web
conditions:
  kibana:
    version: "^9.3.3"
policy_templates:
  - name: backstage
    title: Backstage logs
    description: Collect audit logs that document user activity across backstage's platform and plugins
    inputs:
      - type: filestream
        title: Backstage container logs
        description: Collecting logs from backstage's docker container or k8s pod
owner:
  github: elastic/integration-experience
  type: elastic
