---
description: Pipeline for processing alarm events
processors:
  - drop:
      tag: data_collection_error
      if: ctx.error?.message != null && ctx.message == null && ctx.event?.original == null
      description: Skip pipeline processing for CEL input collection errors.
  - rename:
      field: message
      target_field: event.original
      tag: rename_message_to_event_original
      ignore_missing: true
      if: ctx.event?.original == null
      description: Renames the original message to event.original to store a copy of the raw event.
  - remove:
      field: message
      tag: remove_message
      ignore_missing: true
      if: ctx.event?.original != null
      description: The message field is no longer required once event.original is set.
  - json:
      field: event.original
      target_field: alarm
      tag: json_decode_event_original
      ignore_failure: true
  - set:
      field: ecs.version
      tag: set_ecs_version
      value: 9.3.0
  - set:
      field: event.kind
      tag: set_event_kind
      value: alert
  - append:
      field: event.category
      tag: append_event_category
      value: threat
      allow_duplicates: false
  - append:
      field: event.type
      tag: append_event_type
      value: indicator
      allow_duplicates: false
  - date:
      field: alarm.date
      target_field: '@timestamp'
      tag: date_alarm_date
      formats:
        - "yyyy-MM-dd HH:mm:ss"
      timezone: UTC
      ignore_failure: false
  - set:
      field: alarm.alarm_sub_type
      copy_from: alarm.alarm_type_details.alarm_sub_type
      tag: set_alarm_sub_type
      ignore_empty_value: true
      ignore_failure: true
  - set:
      field: alarm.alarm_main_type
      copy_from: alarm.alarm_type_details.alarm_main_type
      tag: set_alarm_main_type
      ignore_empty_value: true
      ignore_failure: true
  - set:
      field: alarm.alarm_generic_title
      copy_from: alarm.alarm_type_details.alarm_generic_title
      tag: set_alarm_generic_title
      ignore_empty_value: true
      ignore_failure: true
  - set:
      field: alarm.alarm_default_risk_level
      copy_from: alarm.alarm_type_details.alarm_default_risk_level
      tag: set_alarm_default_risk_level
      ignore_empty_value: true
      ignore_failure: true
  - remove:
      field: event.original
      tag: remove_event_original
      if: ctx.tags == null || !(ctx.tags.contains('preserve_original_event'))
      ignore_failure: true
      ignore_missing: true
on_failure:
  - append:
      field: error.message
      value: >-
        Processor '{{{ _ingest.on_failure_processor_type }}}'
        {{#_ingest.on_failure_processor_tag}}with tag '{{{ _ingest.on_failure_processor_tag }}}'
        {{/_ingest.on_failure_processor_tag}}failed with message '{{{ _ingest.on_failure_message }}}'
  - set:
      field: event.kind
      value: pipeline_error
  - append:
      field: tags
      value: preserve_original_event
      allow_duplicates: false
