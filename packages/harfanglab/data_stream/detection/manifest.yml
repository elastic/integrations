title: "HarfangLab EDR detections"
type: logs
dataset: harfanglab.detection
# This data stream has no agent inputs of its own: it only receives detection alerts that the
# harfanglab.generic ingest pipeline reroutes here (events with event.kind: alert).
