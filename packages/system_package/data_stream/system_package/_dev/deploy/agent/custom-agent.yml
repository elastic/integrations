version: "2.3"
services:
  custom-agent:
    image: alpine
    pid: host
    cap_add:
      - AUDIT_CONTROL
      - AUDIT_READ
    user: root
    # volumes:
    #   - ./sample_logs:/sample_logs:ro
    #   - ${SERVICE_LOGS_DIR}:/var/log
    # command: /bin/sh -c "cp /sample_logs/* /var/log/"


