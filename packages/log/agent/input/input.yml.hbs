paths:
{{#each paths}}
  - {{this}}
{{/each}}

{{#if exclude_files}}
exclude_files:
{{#each exclude_files}}
  - {{this}}
{{/each}}
{{/if}}
{{#if ignore_older}}
ignore_older: {{ignore_older}}
{{/if}}
data_stream:
  dataset: {{data_stream.dataset}}
{{#if processors.length}}
processors:
{{processors}}
{{/if}}
{{#if tags.length}}
tags:
{{#each tags as |tag i|}}
- {{tag}}
{{/each}}
{{/if}}

{{custom}}
