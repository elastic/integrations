- name: lyve_cloud.audit
  type: group
  description: >
    Fields for lyve cloud audit.

  # fields:
# name, type, description as keys for all of them  

#requestId is just example of a field not located in ecs ( even though this one is and needed to be changed later !!!)
  fields:
  - name: auditEntry.requestID
    type: keyword 
    description: Id of the request