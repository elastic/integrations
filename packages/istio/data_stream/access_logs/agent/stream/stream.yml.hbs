paths:
{{#each paths}}
  - {{this}}
{{/each}}
prospector.scanner.symlinks: {{ symlinks }}
parsers:
- container:
    stream: stdout
    format: cri
tags:
{{#if preserve_original_event}}
  - preserve_original_event
{{/if}}