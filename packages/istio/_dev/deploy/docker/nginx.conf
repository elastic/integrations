worker_processes 1;

events { worker_connections 1024; }

http {
    sendfile on;

    server {
        listen 8080;

        root /www/data;

        location /metrics {
            autoindex on;
        }
    }
}