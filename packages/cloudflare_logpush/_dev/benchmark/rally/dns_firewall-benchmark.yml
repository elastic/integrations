---
description: Benchmark 10000 cloudflare_logpush.dns_firewall events ingested
data_stream:
  name: dns_firewall
corpora:
  generator:
    total_events: 10000
    template:
      type: gotext
      path: ./dns_firewall-benchmark/template.ndjson
    config:
      path: ./dns_firewall-benchmark/config.yml
    fields:
      path: ./dns_firewall-benchmark/fields.yml
