---
description: Benchmark 10000 cloudflare_logpush.firewall_event events ingested
data_stream:
  name: firewall_event
corpora:
  generator:
    total_events: 10000
    template:
      type: gotext
      path: ./firewall_event-benchmark/template.ndjson
    config:
      path: ./firewall_event-benchmark/config.yml
    fields:
      path: ./firewall_event-benchmark/fields.yml
