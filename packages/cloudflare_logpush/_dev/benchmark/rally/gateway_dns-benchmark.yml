---
description: Benchmark 10000 cloudflare_logpush.gateway_dns events ingested
data_stream:
  name: gateway_dns
corpora:
  generator:
    total_events: 10000
    template:
      type: gotext
      path: ./gateway_dns-benchmark/template.ndjson
    config:
      path: ./gateway_dns-benchmark/config.yml
    fields:
      path: ./gateway_dns-benchmark/fields.yml
