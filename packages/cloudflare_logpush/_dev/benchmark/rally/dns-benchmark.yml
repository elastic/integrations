---
description: Benchmark 10000 cloudflare_logpush.dns events ingested
data_stream:
  name: dns
corpora:
  generator:
    total_events: 10000
    template:
      type: gotext
      path: ./dns-benchmark/template.ndjson
    config:
      path: ./dns-benchmark/config.yml
    fields:
      path: ./dns-benchmark/fields.yml
