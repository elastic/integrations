input: cel
service: ti_cyware_threat_intelligence
data_stream:
  vars:
    access_id: test_username
    secret_key: test_secret_key
    url: http://{{Hostname}}:{{Port}}
    batch_size: 2
    preserve_original_event: true
assert:
  hit_count: 5
