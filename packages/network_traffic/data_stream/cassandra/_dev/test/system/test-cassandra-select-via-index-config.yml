input: packet
data_stream:
  vars: null
numeric_keyword_fields:
  - cassandra.request.headers.stream
  - cassandra.response.headers.stream
vars:
  interface: '{{SERVICE_LOGS_DIR}}/cassandra_select_via_index.pcap'
