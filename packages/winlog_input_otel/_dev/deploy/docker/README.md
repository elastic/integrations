# Docker deploy for Windows Event Log OTEL Input

The Windows Event Log receiver runs only on Windows. There is no Linux container image for the receiver itself. Use a Windows host or Windows container host for integration testing.
