# Windows Event Log receiver runs only on Windows hosts.
# This Dockerfile is a placeholder to match the nginx_input_otel package structure.
# For testing, use a Windows host with the OpenTelemetry Collector installed.
FROM scratch
