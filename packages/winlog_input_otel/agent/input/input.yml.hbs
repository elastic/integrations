receivers:
  windowseventlog:
    channel: {{channel}}
processors:
  resourcedetection/system:
    detectors: ["system"]
service:
  pipelines:
    logs:
      receivers: [windowseventlog]
      processors: [resourcedetection/system]
