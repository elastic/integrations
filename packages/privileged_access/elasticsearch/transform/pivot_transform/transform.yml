# TODO: placeholder
source:
  index: "logs-*"
  query:
    bool:
      must_not:
        term:
          'user.name':
            value: system
      filter:
        - exists:
            field: 
        
  runtime_mappings:
        
dest:
  index: ""
  # TODO: Placeholder
description: This transform runs hourly.
frequency: 1h
pivot:
  aggregations:
    # TODO: Placeholder
  group_by:
    'host.name':
      terms:
        field: host.name

settings:
  # This is required to prevent the transform from clobbering the Fleet-managed mappings.
  deduce_mappings: false
  unattended: true
sync:
  time:
    delay: 60s
    field: '@timestamp'
_meta:
  fleet_transform_version: # TODO
  run_as_kibana_system: false
