- source_dataset: entityanalytics_ad.entity
  rules:
    - target_dataset: entityanalytics_ad.device
      if: ctx.device?.id != null
      namespace:
        - "{{data_stream.namespace}}"
        - default
    - target_dataset: entityanalytics_ad.user
      if: ctx.user?.id != null
      namespace:
        - "{{data_stream.namespace}}"
        - default
