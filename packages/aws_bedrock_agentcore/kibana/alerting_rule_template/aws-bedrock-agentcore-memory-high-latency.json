{
    "id": "aws-bedrock-agentcore-memory-high-latency",
    "type": "alerting_rule_template",
    "attributes": {
      "name": "[AWS Bedrock AgentCore] Memory high latency",
      "tags": ["AWS", "Amazon Bedrock AgentCore"],
      "ruleTypeId": ".es-query",
      "schedule": {
        "interval": "5m"
      },
      "params": {
        "searchType": "esqlQuery",
        "timeWindowSize": 5,
        "timeWindowUnit": "m",
        "esqlQuery": {
          "esql": "// Alert triggers when high latency is detected during AWS Bedrock AgentCore Memory operations.\n//\n// Memory latency directly impacts agent response times, especially for operations\n// that require retrieving contextual information before responding.\n//\n// The alert is grouped by cloud account, region, and operation to pinpoint\n// specific memory operations experiencing high latency.\n//\n// To adjust sensitivity, change the threshold in the WHERE clause (default: 3000ms = 3 seconds).\n// For more details, see: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/memory.html\n\nFROM metrics-aws_bedrock_agentcore.metrics-*\n| WHERE aws.dimensions.Operation IN (\"CreateEvent\", \"RetrieveMemoryRecords\", \"ListEvents\", \"ListMemory\")\n| STATS avg_latency_ms = avg(aws.bedrock_agentcore.metrics.Latency.avg) BY cloud.account.id, cloud.region, aws.dimensions.Operation\n| WHERE avg_latency_ms > 3000"
        },
        "groupBy": "row",
        "timeField": "event.ingested"
      },
      "alertDelay": {
        "active": 1
      }
    },
    "managed": true,
    "coreMigrationVersion": "8.8.0",
    "typeMigrationVersion": "10.1.0"
  }






