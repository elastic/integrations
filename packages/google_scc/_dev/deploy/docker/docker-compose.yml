version: '2.3'
services:
  google_scc:
    image: docker.elastic.co/observability/stream:v0.10.0
    hostname: google_scc
    ports:
      - 8090
    volumes:
      - ./files:/files:ro
    environment:
      PORT: '8090'
    command:
      - http-server
      - --addr=:8090
      - --config=/files/config.yml
