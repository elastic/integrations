format_version: 1.0.0
name: cel
title: CEL Custom API
description: Collect custom events from an API with Elastic agent
type: integration
version: "0.1.0"
release: experimental
conditions:
  kibana.version: "^8.6.1"
license: basic
categories:
  - custom
policy_templates:
  - name: cel
    title: CEL Custom API Input
    description: Collect custom data from HTTP and file-system APIs.
    inputs:
      - type: cel
        title: Collect custom data from HTTP and file-system APIs.
        description: Collect custom data from HTTP and file-system APIs using the Common Expression Language.
owner:
  github: elastic/security-external-integrations
