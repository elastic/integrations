---
description: Benchmark 100000 trend_micro_vision_one.telemetry events ingested
data_stream:
   name: telemetry
corpora:
   generator:
      total_events: 100000
      template:
         type: gotext
         path: ./telemetry-benchmark/template.ndjson
      config:
         path: ./telemetry-benchmark/config.yml
      fields:
         path: ./telemetry-benchmark/fields.yml
