type: metrics
title: HAProxy stat metrics
streams:
  - input: haproxy/metrics
    vars:
      - name: period
        type: text
        title: Period
        multi: false
        required: true
        show_user: true
        default: 10s
    title: HAProxy stat metrics
    description: Collect HAProxy stat metrics
