---
description: Pipeline for URL destination indices created by transform `latest_url`.
processors:
- remove:
    field: labels.is_ioc_transform_source
    ignore_missing: true
    on_failure:
      - append:
          field: error.message
          value: '{{ _ingest.on_failure_message }}'
