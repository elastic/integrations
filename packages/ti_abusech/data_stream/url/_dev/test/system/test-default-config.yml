input: httpjson
service: abusech
data_stream:
  vars:
    ioc_expiry_url: http://{{Hostname}}:{{Port}}/downloads/json
    preserve_original_event: true
    enable_request_tracer: true
    ioc_expiry: true
vars:
  enable_request_tracer: true
assert:
  hit_count: 2
wait_for_data_timeout: 2m