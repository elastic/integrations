input: httpjson
service: netbox_mock
vars:
  url: http://{{Hostname}}:{{Port}}
  token: --token--
  enable_request_tracer: true
data_stream:
  vars:
    preserve_original_event: true
    limit: 2
assert:
  hit_count: 3
