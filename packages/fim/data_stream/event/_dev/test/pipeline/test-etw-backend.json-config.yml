# ECS documents below mirror the shapes produced by the auditbeat file_integrity ETW
# backend golden samples:
# https://github.com/elastic/beats/tree/main/auditbeat/module/file_integrity/testdata/samples
fields:
  event.module: file_integrity
  event.dataset: fim.event
  service.type: file_integrity
  data_stream.type: logs
  data_stream.dataset: fim.event
  data_stream.namespace: test
