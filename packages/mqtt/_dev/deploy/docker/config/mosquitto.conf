# Test broker configuration. Anonymous access is enabled deliberately: the
# system test exercises the input, not broker authentication.
listener 1883 0.0.0.0
allow_anonymous true
persistence false
log_dest stdout
log_type all
