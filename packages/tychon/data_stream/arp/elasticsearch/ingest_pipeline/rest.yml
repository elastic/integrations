---
description: Pipeline for TYCHON ARP Tables
processors:

  - uppercase:
      field: tychon.destination.mac

  # ECS fields

  - set:
      field: event.kind
      value: state
  - set:
      field: event.category
      value: [network]
  - set:
      field: event.type
      value: [info]
