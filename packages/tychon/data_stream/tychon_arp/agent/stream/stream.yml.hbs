paths:
{{#each paths as |path|}}
 - {{path}}
{{/each}}
tags:
{{#if preserve_original_event}}
  - preserve_original_event
{{/if}}
{{#each tags as |tag|}}
  - {{tag}}
{{/each}}
{{#contains "forwarded" tags}}
{{/contains}}
processors:
{{#if processors}}
{{processors}}
{{/if}}
