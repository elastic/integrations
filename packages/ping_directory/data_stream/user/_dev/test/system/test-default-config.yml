input: cel
service: ping_directory
vars:
  url: http://{{Hostname}}:{{Port}}
  bind_dn: "xxxx"
  password: "xxxx"
data_stream:
  vars:
    preserve_original_event: true
    batch_size: 2
assert:
  hit_count: 5
