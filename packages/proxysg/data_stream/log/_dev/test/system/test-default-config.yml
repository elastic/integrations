service: proxysg-access-logs
input: filestream
data_stream:
  vars:
    paths: ["{{SERVICE_LOGS_DIR}}/proxysg.log"]
    preserve_original_event: true
    keep_message: true
    config: onprem
assert:
  hit_count: 8
