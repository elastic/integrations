service: radware-defensepro-logfile
skip:
  reason: "No sample logs for radware"
  link: https://github.com/elastic/integrations/issues/698
input: logfile
data_stream:
  vars:
    paths:
      - "{{SERVICE_LOGS_DIR}}/radware-defensepro-*.log"
