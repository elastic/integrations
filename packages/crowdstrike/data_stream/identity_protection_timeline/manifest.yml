title: Collect identity protection timeline events from CrowdStrike.
type: logs
streams:
  - input: cel
    title: Identity Protection timeline
    description: Collect identity protection activity timeline events from CrowdStrike Falcon.
    enabled: false
    template_path: cel.yml.hbs
    vars:
      - name: initial_interval
        type: text
        title: Initial interval
        description: How far back to pull the first timeline page from CrowdStrike. Supported units for this parameter are h/m/s.
        multi: false
        required: true
        show_user: true
        default: 24h
      - name: interval
        type: text
        title: Interval
        description: Duration between poller runs. Supported units for this parameter are h/m/s.
        default: 1h
        multi: false
        required: true
        show_user: true
      - name: batch_size
        type: integer
        title: Page size
        description: Max timeline events per request.
        default: 100
        required: true
        show_user: true
      - name: http_client_timeout
        type: text
        title: HTTP Client timeout
        description: Duration before declaring that the HTTP client connection has timed out. Valid time units are ns, us, ms, s, m, h.
        multi: false
        required: true
        show_user: false
        default: 60s
      - name: rate_limit
        type: text
        title: HTTP request rate limit
        description: Max requests per second to the API (e.g. `0.2`). Leave empty to use the CEL default.
        required: false
        show_user: false
        multi: false
      - name: enable_request_tracer
        type: bool
        title: Enable request tracing
        default: false
        multi: false
        required: false
        show_user: false
        description: >-
          The request tracer logs requests and responses to the agent's local file-system for debugging configurations. Enabling this request tracing compromises security and should only be used for debugging. Disabling the request tracer will delete any stored traces. See [documentation](https://www.elastic.co/guide/en/beats/filebeat/current/filebeat-input-cel.html#_resource_tracer_enable) for details.
      - name: max_executions
        type: integer
        title: Maximum Executions
        description: >-
          Maximum number of CEL program re-evaluations per collection interval. High-volume environments with large result sets may need a higher value to complete pagination within a single interval.
        default: 1000
        multi: false
        required: true
        show_user: false
      - name: tags
        type: text
        title: Tags
        multi: true
        required: true
        show_user: false
        default:
          - forwarded
          - crowdstrike-identity_protection_timeline
      - name: preserve_original_event
        required: true
        show_user: true
        title: Preserve original event
        description: Preserves a raw copy of the original event, added to the field `event.original`.
        type: bool
        multi: false
        default: false
      - name: preserve_duplicate_custom_fields
        type: bool
        title: Preserve duplicate custom fields
        description: Use this option to allow duplicate custom fields in events.
        default: false
        multi: false
        required: true
        show_user: true
      - name: processors
        type: yaml
        title: Processors
        multi: false
        required: false
        show_user: false
        description: >-
          Processors are used to reduce the number of fields in the exported event or to enhance the event with metadata. This executes in the agent before the logs are parsed. See [Processors](https://www.elastic.co/guide/en/beats/filebeat/current/filtering-and-enhancing-data.html) for details.
