# Only the join key used by LOOKUP JOIN. Other ECS fields are not stored in the
# lookup index (pipeline keeps only host.id and crowdstrike.info). Defining
# only host.id here avoids the destination index exposing @timestamp, event.*,
# etc., which would shadow the main stream's columns in ESQL LOOKUP JOIN results.
- name: host.id
  external: ecs
