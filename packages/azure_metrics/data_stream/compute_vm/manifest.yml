type: metrics
title: Compute VM metrics
release: beta
streams:
  - input: azure/metrics
    vars:
      - name: period
        type: text
        title: Period
        multi: false
        required: true
        show_user: true
        default: 300s
      - name: resources
        type: yaml
        title: Resources
        multi: false
        required: true
        show_user: true
        default: |
          - resource_group: "dev"
    title: Compute VM metrics
    description: Collect Compute VM metrics
