services:
  whoisfreaks-mock:
    image: nginx:1.25-alpine
    volumes:
      - ./files/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./files/gtld:/usr/share/nginx/html/gtld:ro
    ports:
      - "8080"

