pipeline:
  skip:
    reason: >-
      Pipeline test field validation does not support otelcol input type. The pipeline test runner lacks WithOTelValidation, so fields under attributes.* (passthrough containers) are reported as undefined. System tests validate the full integration with OTel-aware validation.
    link: https://github.com/elastic/elastic-package/issues/3711
