input: httpjson
skip:
  reason: "The fleet health status changes to DEGRADED when an array of strings is returned in the response."
  link: https://github.com/elastic/beats/issues/48254
service: ti_rapid7_threat_command
vars:
  url: http://{{Hostname}}:{{Port}}
  account_id: xxxx
  api_key: xxxx
  enable_request_tracer: true
data_stream:
  vars:
    preserve_original_event: true
