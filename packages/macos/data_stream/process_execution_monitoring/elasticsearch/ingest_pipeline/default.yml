---
description: Pipeline for processing process_execution_monitoring logs.
processors:
  - append:
      field: event.category
      tag: set_event_category
      value: process
  - append:
      field: event.type
      tag: append_info_into_event_type_in_process
      value: info
  - pipeline:
      name: '{{ IngestPipeline "common-pipeline" }}'
      tag: run-common-pipeline
