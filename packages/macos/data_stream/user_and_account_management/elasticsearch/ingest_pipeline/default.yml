---
description: Pipeline for processing user and account management logs.
processors:
  - append:
      field: event.category
      tag: set_event_category
      value: iam
  - append:
      field: event.type
      tag: append_info_into_event_type_in_user_account_management
      value: info
  - pipeline:
      name: '{{ IngestPipeline "common-pipeline" }}'
