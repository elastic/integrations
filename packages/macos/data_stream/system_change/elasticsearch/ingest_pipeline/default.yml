---
description: Pipeline for processing system change logs.
processors:
  - append:
      field: event.category
      tag: set_event_category_configuration
      value: configuration
  - pipeline:
      name: '{{ IngestPipeline "common-pipeline" }}'
      tag: run-common-pipeline
  - pipeline:
      name: '{{ IngestPipeline "common-network-pipeline" }}'
      tag: run-common-network-pipeline
