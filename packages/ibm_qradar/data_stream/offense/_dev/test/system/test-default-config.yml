input: cel
service: ibm_qradar
vars:
  url: http://{{Hostname}}:{{Port}}
  auth_token: xxxx
data_stream:
  vars:
    preserve_original_event: true
    preserve_duplicate_custom_fields: true
    batch_size: 2
assert:
  hit_count: 6
