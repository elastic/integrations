version: '3.8'
services:
  ibm_qradar:
    image: docker.elastic.co/observability/stream:v0.20.0
    hostname: ibm_qradar
    ports:
      - 8090
    volumes:
      - ./files:/files:ro
    environment:
      PORT: '8090'
    command:
      - http-server
      - --addr=:8090
      - --config=/files/config.yml
