---
description: Pipeline for usage
processors:
  - set:
      field: ecs.version
      value: "8.16.0"
on_failure:
  - set:
      field: error.message
      value: "{{ _ingest.on_failure_message }}"
