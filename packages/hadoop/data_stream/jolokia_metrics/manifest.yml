title: Metrics
type: metrics
release: beta
streams:
  - input: jolokia/metrics
    title: Hadoop metrics
    description: Collect Hadoop metrics using Jolokia agent.
    vars:
      - name: period
        type: text
        title: Period
        multi: false
        required: true
        show_user: true
        default: 60s
    template_path: "stream.yml.hbs"
