---
description: Pipeline for normalizing Zeek smb_files.log
processors:
  - rename:
      field: message
      target_field: event.original
  - json:
      field: event.original
      target_field: _temp_
  - pipeline:
      if: ctx?._temp_?.result != null
      name: '{{ IngestPipeline "third-party" }}'
  - drop:
      description: Drop if no timestamp (invalid json)
      if: 'ctx?._temp_?.ts == null'
  - rename:
      field: _temp_
      target_field: zeek.smb_files

# Sets event.created from the @timestamp field generated by filebeat before being overwritten further down
  - set:
      field: event.created
      copy_from: "@timestamp"
  - set:
      field: event.kind
      value: event
  - set:
      field: ecs.version
      value: '8.4.0'
  - append:
      field: event.category
      value: network
  - append:
      field: event.category
      value: file
  - append:
      field: event.type
      value: connection
  - append:
      field: event.type
      value: protocol
  - set:
      field: network.transport
      value: tcp
  - set:
      field: network.protocol
      value: smb
  - dot_expander:
      path: zeek.smb_files
      field: id.orig_p
      ignore_failure: true
  - dot_expander:
      path: zeek.smb_files
      field: id.orig_h
      ignore_failure: true
  - dot_expander:
      path: zeek.smb_files
      field: id.resp_h
      ignore_failure: true
  - dot_expander:
      path: zeek.smb_files
      field: id.resp_p
      ignore_failure: true
  - rename:
      field: zeek.smb_files.id.orig_h
      target_field: source.address
      ignore_missing: true
  - rename:
      field: zeek.smb_files.id.orig_p
      target_field: source.port
      ignore_missing: true
  - rename:
      field: zeek.smb_files.id.resp_h
      target_field: destination.address
      ignore_missing: true
  - rename:
      field: zeek.smb_files.id.resp_p
      target_field: destination.port
      ignore_missing: true
  - rename:
      field: zeek.smb_files.uid
      target_field: zeek.session_id
      ignore_missing: true
  - set:
      field: event.id
      copy_from: zeek.session_id
      if: ctx?.zeek?.session_id != null
  - set:
      field: source.ip
      copy_from: source.address
      if: ctx?.source?.address != null
  - set:
      field: destination.ip
      copy_from: destination.address
      if: ctx?.destination?.address != null
  - set:
      field: event.action
      copy_from: zeek.smb_files.action
      if: ctx?.zeek?.smb_files?.action != null
  - set:
      field: file.name
      copy_from: zeek.smb_files.name
      if: ctx?.zeek?.smb_files?.name != null
  - set:
      field: file.size
      copy_from: zeek.smb_files.size
      if: ctx?.zeek?.smb_files?.size != null
  - date:
      field: zeek.smb_files.ts
      formats:
        - UNIX
        - ISO8601
  - remove:
      field: zeek.smb_files.ts
  - dot_expander:
      field: times.accessed
      path: zeek.smb_files
  - dot_expander:
      field: times.changed
      path: zeek.smb_files
  - dot_expander:
      field: times.created
      path: zeek.smb_files
  - dot_expander:
      field: times.modified
      path: zeek.smb_files
  - date:
      field: zeek.smb_files.times.accessed
      target_field: zeek.smb_files.times.accessed
      formats:
        - UNIX
        - ISO8601
      if: ctx.zeek.smb_files.times?.accessed != null
  - set:
      field: file.accessed
      value: "{{zeek.smb_files.times.accessed}}"
      if: "ctx?.zeek?.smb_files?.times?.accessed != null"
  - date:
      field: zeek.smb_files.times.changed
      target_field: zeek.smb_files.times.changed
      formats:
        - UNIX
        - ISO8601
      if: ctx.zeek.smb_files.times?.accessed != null
  - set:
      field: file.ctime
      value: "{{zeek.smb_files.times.changed}}"
      if: "ctx?.zeek?.smb_files?.times?.changed != null"
  - date:
      field: zeek.smb_files.times.created
      target_field: zeek.smb_files.times.created
      formats:
        - UNIX
        - ISO8601
      if: ctx.zeek.smb_files.times?.accessed != null
  - set:
      field: file.created
      value: "{{zeek.smb_files.times.created}}"
      if: "ctx?.zeek?.smb_files?.times?.created != null"
  - date:
      field: zeek.smb_files.times.modified
      target_field: zeek.smb_files.times.modified
      formats:
        - UNIX
        - ISO8601
      if: ctx.zeek.smb_files.times?.accessed != null
  - set:
      field: file.mtime
      value: "{{zeek.smb_files.times.modified}}"
      if: "ctx?.zeek?.smb_files?.times?.modified != null"
  - geoip:
      field: destination.ip
      target_field: destination.geo
      ignore_missing: true
  - geoip:
      field: source.ip
      target_field: source.geo
      ignore_missing: true
  - geoip:
      database_file: GeoLite2-ASN.mmdb
      field: source.ip
      target_field: source.as
      properties:
        - asn
        - organization_name
      ignore_missing: true
  - geoip:
      database_file: GeoLite2-ASN.mmdb
      field: destination.ip
      target_field: destination.as
      properties:
        - asn
        - organization_name
      ignore_missing: true
  - rename:
      field: source.as.asn
      target_field: source.as.number
      ignore_missing: true
  - rename:
      field: source.as.organization_name
      target_field: source.as.organization.name
      ignore_missing: true
  - rename:
      field: destination.as.asn
      target_field: destination.as.number
      ignore_missing: true
  - rename:
      field: destination.as.organization_name
      target_field: destination.as.organization.name
      ignore_missing: true
  - append:
      field: related.ip
      value: "{{source.ip}}"
      if: "ctx?.source?.ip != null"
      allow_duplicates: false
  - append:
      field: related.ip
      value: "{{destination.ip}}"
      if: "ctx?.destination?.ip != null"
      allow_duplicates: false
  - append:
      field: related.user
      value: "{{user.name}}"
      if: "ctx?.user?.name != null"
      allow_duplicates: false
  - set:
      field: file.path
      value: "{{zeek.smb_files.path}}\\{{zeek.smb_files.name}}"
      if: "ctx?.zeek?.smb_files?.path != null && ctx?.zeek?.smb_files?.name != null"
  - append:
      field: event.type
      value: deletion
      if: "ctx?.zeek?.smb_files?.action == 'SMB::FILE_DELETE'"
  - append:
      field: event.type
      value: change
      if: "ctx?.zeek?.smb_files?.action == 'SMB::FILE_RENAME' || ctx?.zeek?.smb_files?.action == 'SMB::FILE_SET_ATTRIBUTE'"
  - append:
      field: event.type
      value: info
      if: "ctx?.zeek?.smb_files?.action != null && ctx.zeek.smb_files != 'SMB::FILE_DELETE' && ctx.zeek.smb_files != 'SMB::FILE_RENAME' && ctx.zeek.smb_files != 'SMB::FILE_SET_ATTRIBUTE'"
  - community_id:
      target_field: network.community_id
  - remove:
      field: 
        - zeek.smb_files.id
      ignore_missing: true
  - remove:
      field: event.original
      if: "ctx?.tags == null || !(ctx.tags.contains('preserve_original_event'))"
      ignore_failure: true
      ignore_missing: true
on_failure:
  - set:
      field: error.message
      value: "{{ _ingest.on_failure_message }}"
