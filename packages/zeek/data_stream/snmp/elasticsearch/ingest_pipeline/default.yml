---
description: Pipeline for normalizing Zeek snmp.log
processors:
  - set:
      field: event.ingested
      value: "{{_ingest.timestamp}}"
# Sets event.created from the @timestamp field generated by filebeat before being overwritten further down
  - set:
      field: event.created
      copy_from: "@timestamp"
  - set:
      field: event.kind
      value: event
  - set:
      field: ecs.version
      value: '1.9.0'
  - append:
      field: event.category
      value: network
  - append:
      field: event.type
      value: connection
  - append:
      field: event.type
      value: protocol
  - set:
      field: network.transport
      value: udp
  - set:
      field: network.protocol
      value: snmp
  - json:
      field: message
      target_field: json
      ignore_failure: true
      if: ctx?.message != null
  - drop:
      if: 'ctx?.json?.result == null && ctx?.json?.ts == null'
  - rename:
      field: json
      target_field: zeek.snmp
      if: ctx?.json?.ts != null
  - fingerprint:
      fields: 
        - json.result._cd
        - json.result._indextime
        - json.result._raw
        - json.result._time
        - json.result.host
        - json.result.source
      target_field: '_id'
      if: 'ctx?.json?.result != null && ctx?.json?.ts == null'
  - rename:
      field: json.result._raw
      target_field: event.original
      ignore_missing: true
  - rename:
      field: json.result.host
      target_field: host.name
      ignore_missing: true
  - rename:
      field: json.result.source
      target_field: log.file.path
      ignore_missing: true
  - json:
      field: event.original
      target_field: zeek.snmp
      ignore_failure: true
      if: ctx?.event?.original != null
  - dot_expander:
      path: zeek.snmp
      field: id.orig_p
      ignore_failure: true
  - dot_expander:
      path: zeek.snmp
      field: id.orig_h
      ignore_failure: true
  - dot_expander:
      path: zeek.snmp
      field: id.resp_h
      ignore_failure: true
  - dot_expander:
      path: zeek.snmp
      field: id.resp_p
      ignore_failure: true
  - rename:
      field: zeek.snmp.id.orig_h
      target_field: source.address
      ignore_missing: true
  - rename:
      field: zeek.snmp.id.orig_p
      target_field: source.port
      ignore_missing: true
  - rename:
      field: zeek.snmp.id.resp_h
      target_field: destination.address
      ignore_missing: true
  - rename:
      field: zeek.snmp.id.resp_p
      target_field: destination.port
      ignore_missing: true
  - rename:
      field: zeek.snmp.uid
      target_field: zeek.session_id
      ignore_missing: true
  - set:
      field: event.id
      copy_from: zeek.session_id
      if: ctx?.zeek?.session_id != null
  - set:
      field: source.ip
      copy_from: source.address
      if: ctx?.source?.address != null
  - set:
      field: destination.ip
      copy_from: destination.address
      if: ctx?.destination?.address != null
  - rename:
      field: zeek.snmp.get_requests
      target_field: zeek.snmp.get.requests
      ignore_missing: true
  - rename:
      field: zeek.snmp.get_bulk_requests
      target_field: zeek.snmp.get.bulk_requests
      ignore_missing: true
  - rename:
      field: zeek.snmp.get_responses
      target_field: zeek.snmp.get.responses
      ignore_missing: true
  - rename:
      field: zeek.snmp.set_requests
      target_field: zeek.snmp.set.requests
      ignore_missing: true
  - date:
      field: zeek.snmp.ts
      formats:
        - UNIX
  - remove:
      field: zeek.snmp.ts
  - date:
      field: zeek.snmp.up_since
      target_field: zeek.snmp.up_since
      formats:
        - UNIX
      if: ctx.zeek.snmp.up_since != null
  - geoip:
      field: destination.ip
      target_field: destination.geo
      ignore_missing: true
  - geoip:
      field: source.ip
      target_field: source.geo
      ignore_missing: true
  - geoip:
      database_file: GeoLite2-ASN.mmdb
      field: source.ip
      target_field: source.as
      properties:
        - asn
        - organization_name
      ignore_missing: true
  - geoip:
      database_file: GeoLite2-ASN.mmdb
      field: destination.ip
      target_field: destination.as
      properties:
        - asn
        - organization_name
      ignore_missing: true
  - rename:
      field: source.as.asn
      target_field: source.as.number
      ignore_missing: true
  - rename:
      field: source.as.organization_name
      target_field: source.as.organization.name
      ignore_missing: true
  - rename:
      field: destination.as.asn
      target_field: destination.as.number
      ignore_missing: true
  - rename:
      field: destination.as.organization_name
      target_field: destination.as.organization.name
      ignore_missing: true
  - append:
      field: related.ip
      value: "{{source.ip}}"
      if: "ctx?.source?.ip != null"
      allow_duplicates: false
  - append:
      field: related.ip
      value: "{{destination.ip}}"
      if: "ctx?.destination?.ip != null"
      allow_duplicates: false
  - community_id:
      target_field: network.community_id
  - remove:
      field: 
        - message
        - json
        - zeek.snmp.id
      ignore_missing: true
on_failure:
  - set:
      field: error.message
      value: "{{ _ingest.on_failure_message }}"
