input: logfile
data_stream:
  vars:
    paths:
      - "{{SERVICE_LOGS_DIR}}/kibana.log"
skip:
  link: https://github.com/elastic/integrations/issues/6294
  reason: "This integration produces events that cannot be indexed"
