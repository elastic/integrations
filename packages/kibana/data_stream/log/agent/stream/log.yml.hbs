paths:
{{#each paths as |path i|}}
 - {{path}}
{{/each}}
exclude_files: [".gz$"]
json.keys_under_root: false
json.add_error_key: true
processors:
  - add_fields:
      target: ''
      fields:
        ecs.version: 1.10.0