paths:
{{#each paths as |path i|}}
 - {{path}}
{{/each}}
exclude_files: [".gz$"]
processors:
  - add_locale: ~
  - add_fields:
      target: ''
      fields:
        ecs.version: 1.10.0
  - decode_json_fields:
      fields: [message]
      target: kibana._audit_temp