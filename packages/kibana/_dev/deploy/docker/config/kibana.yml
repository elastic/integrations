server.host: "0.0.0.0"
elasticsearch.hosts: ["http://elasticsearch:9200"]

logging:
  root:
    level: all
    appenders: [default, file]
  appenders:
    file:
      type: file
      fileName: ./logs/kibana.log
      layout:
        type: json
