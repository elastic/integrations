rules:
  # We’re unable to test the pagination functionality with the current rule since a fixed batch size of
  # 10,000 is being used in datacollection
  - path: /v1/logging/svractivities
    methods: ['POST']
    request_body: '{}'
    responses:
      - status_code: 200
        headers:
          Content-Type:
            - application/json
          X-ApiKey:
            - my-secret-api-key
        body: |
          {{ minify_json `
          {
            "error": "Success",
            "response": {
              "svractivities": [
                {
                  "checkpoint": "firstcheckpoint",
                  "datetime": "2024-01-25T05:23:31.77Z",
                  "task": "Repository Add",
                  "user": "SYSTEM",
                  "description": "a.out added to repository"
                },
                {
                  "checkpoint": "secondcheckpoint",
                  "datetime": "2024-01-25T05:23:31.88Z",
                  "task": "Repository Add",
                  "user": "SYSTEM",
                  "description": "file.exe added to repository"
                },
                {
                  "checkpoint": "thirdcheckpoint",
                  "datetime": "2024-01-25T05:23:30.88Z",
                  "task": "Client Checkin",
                  "user": "SYSTEM",
                  "description": "DESKTOP-GG4CEJM checks in"
                }
              ]
            }
          }
          ` }}
