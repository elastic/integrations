type: metrics
title: Stan stats metrics
streams:
  - input: stan/metrics
    vars:
      - name: period
        type: text
        title: Period
        multi: false
        required: true
        show_user: true
        default: 60s
    title: Stan stats metrics
    description: Collect Stan stats metrics
elasticsearch:
  index_mode: "time_series"
