type: metrics
title: Stan channels metrics
release: experimental
streams:
  - input: stan/metrics
    vars:
      - name: period
        type: text
        title: Period
        multi: false
        required: true
        show_user: true
        default: 60s
    title: Stan channels metrics
    description: Collect Stan channels metrics
