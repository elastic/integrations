type: metrics
title: Stan subscriptions metrics
release: experimental
streams:
  - input: stan/metrics
    vars:
      - name: period
        type: text
        title: Period
        multi: false
        required: true
        show_user: true
        default: 60s
    title: Stan subscriptions metrics
    description: Collect Stan subscriptions metrics
