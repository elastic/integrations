service: citrix-adc-custom-date
input: logfile
data_stream:
  vars:
    paths:
      - "{{SERVICE_LOGS_DIR}}/citrix-adc-custom-date.log"
    preserve_original_event: true
    custom_date_format: "dd/MM/yyyy:HH:mm:ss"
