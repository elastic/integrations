config_version: 2
interval: {{interval}}
request.method: GET
request.url: {{hostname}}/nagiosxi/api/v1/objects/servicestatus?apikey={{api_key}}
{{#if proxy_url }}
request.proxy_url: {{proxy_url}}
{{/if}}
{{#if ssl}}
request.ssl: {{ssl}}
{{/if}}
{{#if query}}
request.transforms:
  - set:
      target: url.params.display_name
      value: in:{{#each query as |q i|}}{{q}},{{/each}}
      default: in:{{#each query as |q i|}}{{q}},{{/each}}
{{/if}}
response.split:
  target: body.servicestatus
  type: array

tags:
{{#if preserve_original_event}}
  - preserve_original_event
{{/if}}
{{#each tags as |tag i|}}
  - {{tag}}
{{/each}}
{{#contains "forwarded" tags}}
publisher_pipeline.disable_host: true
{{/contains}}
{{#if processors}}
processors:
{{processors}}
{{/if}}
