vars:
  scrape_configs: |
    - job_name: secure_prometheus
      scheme: https
      scrape_interval: 60s
      tls_config:
        insecure_skip_verify: true
      basic_auth:
        username: admin
        password: secret
      static_configs:
        - targets:
            - localhost:9090
