service: prometheus_basic_auth
policy_template: prometheusreceiver
vars:
  scrape_configs: |
    - job_name: prometheus_raw_auth
      scrape_interval: 10s
      basic_auth:
        username: prometheus_user
        password: test_password
      static_configs:
        - targets:
            - "{{Hostname}}:{{Port}}"
