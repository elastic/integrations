service: prometheus_input_otel_raw
policy_template: prometheusreceiver_raw
vars:
  scrape_configs: |
    - job_name: prometheus_raw
      scrape_interval: 10s
      static_configs:
        - targets:
            - "{{Hostname}}:{{Port}}"
