wait_for_data_timeout: 1m
service: mosquitto-log-filestream
input: filestream
data_stream:
  vars:
    paths:
      - "{{SERVICE_LOGS_DIR}}/mosquitto-iso.log"
    preserve_original_event: true
    tz_offset: "UTC"
