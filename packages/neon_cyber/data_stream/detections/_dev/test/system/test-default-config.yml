input: cel
service: neon_cyber
vars:
    url: http://{{Hostname}}:{{Port}}
    api_token: xxxx
data_stream:
    vars:
        enable_request_tracer: true
        preserve_original_event: true
assert:
    hit_count: 1
