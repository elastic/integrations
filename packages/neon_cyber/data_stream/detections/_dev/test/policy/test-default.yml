vars:
    api_token: test_api_token
data_stream:
    vars:
        resource_url: http://host.tld
        resource_interval: 1m
        enable_request_tracer: false
        preserve_original_event: false
        http_client_timeout: 30s
        tags:
            - forwarded
            - neon_cyber-detections
