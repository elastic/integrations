input: cel
service: neon_cyber
vars:
    api_token: xxxx
    enable_request_tracer: true
data_stream:
    vars:
        resource_url: http://{{Hostname}}:{{Port}}/v1/events
        preserve_original_event: true
assert:
    hit_count: 1
