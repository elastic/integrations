title: ActiveMQ Artemis broker metrics
type: metrics
streams:
  - input: jolokia/metrics
    vars:
      - name: period
        type: text
        title: Period
        multi: false
        required: true
        show_user: true
        default: 30s
    title: ActiveMQ Artemis broker metrics
    description: Collect broker metrics from ActiveMQ Artemis
    template_path: stream.yml.hbs
