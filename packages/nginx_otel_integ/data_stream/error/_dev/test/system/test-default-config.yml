service: logfile
data_stream:
  vars:
    include:
      - "{{SERVICE_LOGS_DIR}}/error.log"
    start_at: beginning
    include_file_path: true
    multiline_line_start_pattern: '^\d{4}/\d{2}/\d{2} \d{2}:\d{2}:\d{2}'
wait_for_data_timeout: 1m
assert:
  hit_count: 3
