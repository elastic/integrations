service: shodan-downloaded-file
input: logfile
data_stream:
  vars:
    paths:
      - "{{SERVICE_LOGS_DIR}}/shodan-*.ndjson"
    tags:
      - forwarded
    preserve_original_event: true
    preserve_duplicate_custom_fields: true