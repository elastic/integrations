input: cel
policy_template: domain
service: shodan
data_stream:
  vars:
    url: http://{{Hostname}}:{{Port}}
    domain: example.com
    interval: 1h
    preserve_original_event: true
assert:
  hit_count: 2
