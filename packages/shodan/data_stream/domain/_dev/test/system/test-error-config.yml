input: cel
policy_template: domain
service: shodan
data_stream:
  vars:
    url: http://{{Hostname}}:{{Port}}
    domain: error.example.com
    interval: 1h
    preserve_original_event: true
assert:
  hit_count: 1
