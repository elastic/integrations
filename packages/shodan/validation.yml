errors:
  exclude_checks:
    - SVR00004 # References in dashboards: the dashboard embeds Lens visualizations
    #           by value, but keeps its saved search by reference (same pattern
    #           used by packages/ti_greynoise).
