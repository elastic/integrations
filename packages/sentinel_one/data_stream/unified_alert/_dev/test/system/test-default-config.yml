input: cel
service: sentinel_one-unified_alert
vars:
  url: http://{{Hostname}}:{{Port}}
  api_token: xxxx
data_stream:
  vars:
    initial_interval: 24h
    interval: 1m
    batch_size: 2
    preserve_original_event: true
assert:
  hit_count: 5
