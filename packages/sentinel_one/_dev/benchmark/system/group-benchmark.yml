---
description: Benchmark 100000 group events ingested
input: httpjson
vars:
  url: http://{{Hostname}}:{{Port}}
  api_token: xxxx
  enable_request_tracer: true
data_stream:
  name: group
  vars:
    preserve_original_event: true
warmup_time_period: 2s
corpora:
  input_service:
    name: sentinel_one
  generator:
    total_events: 10000
    template:
      path: ./group-benchmark/template.ndjson
      type: gotext
    config:
      path: ./group-benchmark/config.yml
    fields:
      path: ./group-benchmark/fields.yml
