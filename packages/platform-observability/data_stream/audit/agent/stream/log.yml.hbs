paths:
{{#each paths}}
  - {{this}}
{{/each}}
processors:
- rename:
    fields:
      - from: "message"
        to: "event.original"
- decode_json_fields:
    fields: [event.original]
    target: ""
    add_error_key: true
    overwrite_keys: true
{{#if processors}}
{{processors}}  
{{/if}}