fields:
  # This test checks the temporary workaround for invalid field names
  # coming from the Azure Native ISV after the ES .NET client 8.x
  # update.
  azureLogForwarder:
    serviceProvider: "Microsoft.Web"
    category: "FunctionAppLogs"
    resourceType: "Microsoft.Web/sites/functions"
  dataStream:
    namespace: "invalid"
    type: "logs"
    dataset: "azure.platformlogs"
  # The second test document does not containt the `time` field,
  # so it should use the value from the `timestamp` field instead.
  timestamp: "2025-08-13T22:05:57.908Z"
  tags:
    - preserve_original_event
