paths:
  {{#each paths as |path i|}}
- {{path}}
  {{/each}}
exclude_files: [".gz$"]
tags: {{tags}}
publisher_pipeline.disable_host: {{nList tags forwarded"}}

processors:
  - add_fields:
      target: ''
      fields:
        ecs.version: 1.5.0