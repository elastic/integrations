# IIS is a Windows service and typically runs on Windows Server
# This Dockerfile is a placeholder for consistency with package structure
# IIS cannot run in standard Linux containers
ARG SERVICE_VERSION=${SERVICE_VERSION:-10.0}
FROM mcr.microsoft.com/windows/servercore:${SERVICE_VERSION}
# Note: IIS requires Windows containers, not Linux containers
# This is a placeholder file for package structure consistency
