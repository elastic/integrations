---
description: Pipeline for processing Entro audit logs
processors:
  - set:
      field: ecs.version
      value: '8.17.0'

on_failure:
  - set:
      field: error.message
      value: '{{ _ingest.on_failure_message }}'
