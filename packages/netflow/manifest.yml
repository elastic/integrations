format_version: 1.0.0
name: netflow
title: NetFlow
version: 1.2.2
license: basic
description: Collect logs from NetFlow with Elastic Agent.
type: integration
categories:
  - network
  - security
release: ga
conditions:
  kibana.version: "^7.14.0"
policy_templates:
  - name: netflow
    title: NetFlow logs
    description: Collect Netflow logs from networks via UDP
    inputs:
      - type: netflow
        title: Collect NetFlow logs
        description: Collecting NetFlow logs using the netflow input
owner:
  github: elastic/security-external-integrations
