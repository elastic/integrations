interval: {{interval}}
resource.tracer:
  enabled: {{enable_request_tracer}}
  filename: "../../logs/cel/http-request-trace-*.ndjson"
  maxbackups: 5
{{#if proxy_url}}
resource.proxy_url: {{proxy_url}}
{{/if}}
{{#if ssl}}
resource.ssl: {{ssl}}
{{/if}}
{{#if http_client_timeout}}
resource.timeout: {{http_client_timeout}}
{{/if}}
{{#if max_executions}}
max_executions: {{max_executions}}
{{/if}}
resource.url: {{url}}
auth.basic:
  user: {{username}}
  password: {{password}}
state:
  initial_interval: {{initial_interval}}
  page_size: {{page_size}}
  columns: >-
    EpoComplianceHistory.AutoId
    EpoComplianceHistory.ChartName
    EpoComplianceHistory.TaskName
    EpoComplianceHistory.CountComputers
    EpoComplianceHistory.CountNonCompliant
    EpoComplianceHistory.PercentNonCompliant
    EpoComplianceHistory.PercentCompliant
    EpoComplianceHistory.CountCompliant
    EpoComplianceHistory.TheTimestamp
    EpoComplianceHistory.TenantId
redact:
  fields:
    - password
program: |-
  (
    state.?want_more.orValue(false)
    ?
      state
    :
      state.with(
        {
          "the_timestamp": state.?cursor.last_timestamp.orValue(
            (now - duration(state.initial_interval)).format("2006-01-02T15:04:05")
          ),
        }
      )
  ).as(state,
    state.with(
      get(
        state.url.trim_right("/") + "/remote/core.executeQuery?" + {
          "target": ["EpoComplianceHistory"],
          "select": [
            "(select (top " + string(state.page_size) + ") " + state.columns + ")",
          ],
          // The inclusive time cursor intentionally overlaps the boundary
          // timestamp so downstream fingerprinting can remove duplicates.
          "where": [
            "(where (ge EpoComplianceHistory.TheTimestamp \"" +
              state.the_timestamp + "\"))",
          ],
          "order": [
            "(order (asc EpoComplianceHistory.TheTimestamp))",
          ],
        }.format_query() + "&:output=json"
      ).as(resp,
        (resp.StatusCode == 200)
        ?
          string(resp.Body).trim_space().trim_prefix("OK:").trim_space().decode_json().as(rows,
            (
              (size(rows) > 0)
              ?
                timestamp(
                  string(
                    rows[size(rows) - 1]["EpoComplianceHistory.TheTimestamp"]
                  )
                ).format("2006-01-02T15:04:05")
              :
                state.the_timestamp
            ).as(new_timestamp,
              {
                "events": rows.map(e, {"message": e.encode_json()}),
                "the_timestamp": new_timestamp,
                "cursor": {
                  "last_timestamp": new_timestamp,
                },
                "want_more": size(rows) == int(state.page_size),
              }
            )
          )
        :
          {
            "events": {
              "error": {
                "code": string(resp.StatusCode),
                "id": string(resp.Status),
                "message": "GET /remote/core.executeQuery: " + (
                  (size(resp.Body) != 0) ? string(resp.Body) : string(resp.Status)
                ),
              },
            },
            "want_more": false,
          }
      )
    )
  )
tags:
{{#if preserve_original_event}}
  - preserve_original_event
{{/if}}
{{#each tags as |tag|}}
  - {{tag}}
{{/each}}
{{#contains "forwarded" tags}}
publisher_pipeline.disable_host: true
{{/contains}}
{{#if processors}}
processors:
{{processors}}
{{/if}}
