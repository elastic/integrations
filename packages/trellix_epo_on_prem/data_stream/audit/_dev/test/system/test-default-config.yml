deployer: docker
input: cel
service: trellix_epo_on_prem
vars:
  url: http://{{Hostname}}:{{Port}}
  username: temp_user
  password: temp_password
data_stream:
  vars:
    page_size: 2
    tz_offset: "+05:30"
    preserve_original_event: true
