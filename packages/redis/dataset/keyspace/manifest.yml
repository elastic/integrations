title: Redis keyspace metrics
type: metrics
release: beta
streams:
- input: redis/metrics
  vars:
  - name: period
    type: text
    title: Period
    multi: false
    required: true
    show_user: true
    default: 10s
  title: Redis keyspace metrics
  description: Collect Redis keyspace metrics
