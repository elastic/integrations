vars:
  hosts:
    - "{{Hostname}}:6379"
data_stream:
  vars: ~
skip:
  reason: Test is skipped for now since it will fail on Redis 7.x and 8.x due to the issue #43887
  link: https://github.com/elastic/beats/issues/43887