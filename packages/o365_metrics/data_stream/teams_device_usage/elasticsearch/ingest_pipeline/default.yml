---
description: Pipeline for renaming object
processors:
  - set:
      field: ecs.version
      value: "8.16.0"
  - set:
      copy_from: teamsdeviceusage
      field: event.original
      ignore_empty_value: true
      if: ctx.event?.original == null && (ctx.tags != null && (ctx.tags.contains('preserve_original_event')))
  - json:
      field: teamsdeviceusage
      target_field: o365.metrics.teams.device.usage
      tag: json_decoding
      on_failure:
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - script:
      lang: painless
      description: Replace spaces, dashes and bom character in field names under o365.metrics.teams.device.usage.
      tag: painless_purge_spaces_and_dashes
      if: ctx.o365.metrics?.teams.device.usage instanceof Map
      source: |
        String underscore(String s) {
          String result = /[ -]/.matcher(s).replaceAll('_').toLowerCase();
          return /[\ufeff]/.matcher(result).replaceAll('')
        }
        
        def out = [:];
        for (def item : ctx.o365.metrics.teams.device.usage.entrySet()) {
          out[underscore(item.getKey())] = item.getValue();
        }
        ctx.o365.metrics.teams.device.usage = out;

  - remove:
      if: ctx.teamsdeviceusage != null
      field: teamsdeviceusage
      ignore_missing: true
  - convert:
      field: o365.metrics.teams.device.usage.android_phone
      tag: convert_o365.metrics.teams.device.usage.android_phone
      type: long
      if: ctx.o365?.metrics?.teams?.device?.usage?.android_phone != null && ctx.o365?.metrics?.teams?.device?.usage?.android_phone != ''
      on_failure:
        - remove:
            field: o365.metrics.teams.device.usage.android_phone
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - rename:
      field: o365.metrics.teams.device.usage.android_phone
      target_field: o365.metrics.teams.device.usage.android_phone.count
      ignore_missing: true
  - convert:
      field: o365.metrics.teams.device.usage.ios
      tag: convert_o365.metrics.teams.device.usage.ios
      type: long
      if: ctx.o365?.metrics?.teams?.device?.usage?.ios != null && ctx.o365?.metrics?.teams?.device?.usage?.ios != ''
      on_failure:
        - remove:
            field: o365.metrics.teams.device.usage.ios
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - rename:
      field: o365.metrics.teams.device.usage.ios
      target_field: o365.metrics.teams.device.usage.ios.count
      ignore_missing: true
  - convert:
      field: o365.metrics.teams.device.usage.mac
      tag: convert_o365.metrics.teams.device.usage.mac
      type: long
      if: ctx.o365?.metrics?.teams?.device?.usage?.mac != null && ctx.o365?.metrics?.teams?.device?.usage?.mac != ''
      on_failure:
        - remove:
            field: o365.metrics.teams.device.usage.mac
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - rename:
      field: o365.metrics.teams.device.usage.mac
      target_field: o365.metrics.teams.device.usage.mac.count
      ignore_missing: true
  - convert:
      field: o365.metrics.teams.device.usage.web
      tag: convert_o365.metrics.teams.device.usage.web
      type: long
      if: ctx.o365?.metrics?.teams?.device?.usage?.web != null && ctx.o365?.metrics?.teams?.device?.usage?.web != ''
      on_failure:
        - remove:
            field: o365.metrics.teams.device.usage.web
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - rename:
      field: o365.metrics.teams.device.usage.web
      target_field: o365.metrics.teams.device.usage.web.count
      ignore_missing: true
  - convert:
      field: o365.metrics.teams.device.usage.windows
      tag: convert_o365.metrics.teams.device.usage.windows
      type: long
      if: ctx.o365?.metrics?.teams?.device?.usage?.windows != null && ctx.o365?.metrics?.teams?.device?.usage?.windows != ''
      on_failure:
        - remove:
            field: o365.metrics.teams.device.usage.windows
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - rename:
      field: o365.metrics.teams.device.usage.windows
      target_field: o365.metrics.teams.device.usage.windows.count
      ignore_missing: true
  - convert:
      field: o365.metrics.teams.device.usage.windows_phone
      tag: convert_o365.metrics.teams.device.usage.windows_phone
      type: long
      if: ctx.o365?.metrics?.teams?.device?.usage?.windows_phone != null && ctx.o365?.metrics?.teams?.device?.usage?.windows_phone != ''
      on_failure:
        - remove:
            field: o365.metrics.teams.device.usage.windows_phone
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - rename:
      field: o365.metrics.teams.device.usage.windows_phone
      target_field: o365.metrics.teams.device.usage.windows_phone.count
      ignore_missing: true      
  - convert:
      field: o365.metrics.teams.device.usage.linux
      tag: convert_o365.metrics.teams.device.usage.linux
      type: long
      if: ctx.o365?.metrics?.teams?.device?.usage?.linux != null && ctx.o365?.metrics?.teams?.device?.usage?.linux != ''
      on_failure:
        - remove:
            field: o365.metrics.teams.device.usage.linux
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - rename:
      field: o365.metrics.teams.device.usage.linux
      target_field: o365.metrics.teams.device.usage.linux.count
      ignore_missing: true
  - convert:
      field: o365.metrics.teams.device.usage.chrome_os
      tag: convert_o365.metrics.teams.device.usage.chrome_os
      type: long
      if: ctx.o365?.metrics?.teams?.device?.usage?.chrome_os != null && ctx.o365?.metrics?.teams?.device?.usage?.chrome_os != ''
      on_failure:
        - remove:
            field: o365.metrics.teams.device.usage.chrome_os
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - rename:
      field: o365.metrics.teams.device.usage.chrome_os
      target_field: o365.metrics.teams.device.usage.chrome_os.count
      ignore_missing: true
  - date:
      field: o365.metrics.teams.device.usage.report_date
      tag: date_o365.metrics.teams.device.usage.report_date
      if: ctx.o365.metrics?.teams?.device?.usage?.report_date != null && ctx.o365.metrics?.teams?.device?.usage?.report_date != ''
      timezone: UTC
      formats:
        - yyyy-MM-dd
      on_failure:
        - remove:
            field: o365.metrics.teams.device.usage.report_date
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - rename:
      field: o365.metrics.teams.device.usage.report_date
      target_field: o365.metrics.teams.device.usage.report.date
      ignore_missing: true
  - set:
      field: '@timestamp'
      tag: set_timestamp_from_report_date
      copy_from: o365.metrics.teams.device.usage.report.date
      ignore_empty_value: true
  - fingerprint:
      fields:
        - o365.metrics.teams.device.usage.report.date
      target_field: _id
  - rename:
      field: o365.metrics.teams.device.usage.report_period
      target_field: o365.metrics.teams.device.usage.report.period.day
      ignore_missing: true
  - rename:
      field: o365.metrics.teams.device.usage.report_refresh_date
      target_field: o365.metrics.teams.device.usage.report.refresh_date
      ignore_missing: true

on_failure:
  - set:
      field: error.message
      value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - append:
      field: tags
      value: preserve_original_event
      allow_duplicates: false
  - append:
      field: event.kind
      value: pipeline_error
      allow_duplicates: false
  - append:
      field: event.type
      value: error
      if: ctx.error?.message != null
