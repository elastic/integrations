---
description: Pipeline for renaming object
processors:
  - set:
      field: ecs.version
      value: "8.11.0"             
  - json:
      field: sharepoint_siteusagedetails
      target_field: o365metrics.sharepoint_siteusagedetails   
  - script:
      lang: painless
      description: Replace spaces and dashes in field names under o365metrics.sharepoint_siteusagedetails.
      tag: painless_purge_spaces_and_dashes
      if: ctx.o365metrics?.sharepoint_siteusagedetails instanceof Map
      source: |
        String underscore(String s) {
          return /[ -]/.matcher(s).replaceAll('');
        }
        
        def out = [:];
        for (def item : ctx.o365metrics.sharepoint_siteusagedetails.entrySet()) {
          out[underscore(item.getKey())] = item.getValue();
        }
        ctx.o365metrics.sharepoint_siteusagedetails = out;
  - remove:
      if: ctx.sharepoint_siteusagedetails != null
      field: sharepoint_siteusagedetails
      ignore_missing: true
  - rename:
      field: o365metrics.sharepoint_siteusagedetails.StorageUsed(Byte)
      target_field: o365metrics.sharepoint_siteusagedetails.StorageUsedByte
      ignore_missing: true

on_failure:
  - append:
      field: error.message
      value: "{{{_ingest.on_failure_message}}}"
  - append:
      field: event.kind
      value: pipeline_error
      allow_duplicates: false