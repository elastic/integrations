---
description: Pipeline for processing Microsoft 365 mailbox usage quota status mailbox counts metrics.
processors:
  - set:
      field: ecs.version
      value: "8.16.0"   
  - set:
      copy_from: mailboxquotastatus
      field: event.original
      ignore_empty_value: true
      if: ctx.event?.original == null && (ctx.tags != null && (ctx.tags.contains('preserve_original_event')))   
  - json:
      field: mailboxquotastatus
      target_field: o365.metrics.mailbox.quota.status.mailbox.counts
      tag: json_decoding
      on_failure:
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'             
  - script:
      lang: painless
      description: Replace spaces, dashes and bom character in field names under o365.metrics.mailbox.quota.status.mailbox.counts.
      tag: painless_purge_spaces_and_dashes
      ignore_failure: true
      if: ctx.o365?.metrics?.mailbox?.quota?.status instanceof Map
      source: |
        String underscore(String s) {
          String result = /[ -]/.matcher(s).replaceAll('_').replace("/", "_").toLowerCase();
          return /[\ufeff]/.matcher(result).replaceAll('')
        }
        
        def out = [:];
        for (def item : ctx.o365.metrics.mailbox.quota.status.mailbox.counts.entrySet()) {
          out[underscore(item.getKey())] = item.getValue();
        }
        ctx.o365.metrics.mailbox.quota.status.mailbox.counts = out;
  - remove:
      if: ctx.mailboxquotastatus != null
      field: mailboxquotastatus
      ignore_missing: true
  - convert:
      field: o365.metrics.mailbox.quota.status.mailbox.counts.indeterminate
      tag: convert_o365.metrics.mailbox.quota.status.mailbox.counts.indeterminate
      type: long
      if: ctx.o365?.metrics?.mailbox?.quota?.status?.mailbox?.counts?.indeterminate != null && ctx.o365?.metrics?.mailbox?.quota?.status?.mailbox?.counts?.indeterminate != ''
      on_failure:
        - remove:
            field: o365.metrics.mailbox.quota.status.mailbox.counts.indeterminate
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - rename:
      field: o365.metrics.mailbox.quota.status.mailbox.counts.indeterminate
      target_field: o365.metrics.mailbox.quota.status.mailbox.counts.indeterminate.count
      ignore_missing: true 
  - convert:
      field: o365.metrics.mailbox.quota.status.mailbox.counts.send_prohibited
      tag: convert_o365.metrics.mailbox.quota.status.mailbox.counts.send_prohibited
      type: long
      if: ctx.o365?.metrics?.mailbox?.quota?.status?.mailbox?.counts?.send_prohibited != null && ctx.o365?.metrics?.mailbox?.quota?.status?.mailbox?.counts?.send_prohibited != ''
      on_failure:
        - remove:
            field: o365.metrics.mailbox.quota.status.mailbox.counts.send_prohibited
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'  
  - rename:
      field: o365.metrics.mailbox.quota.status.mailbox.counts.send_prohibited
      target_field: o365.metrics.mailbox.quota.status.mailbox.counts.send_prohibited.count
      ignore_missing: true
  - convert:
      field: o365.metrics.mailbox.quota.status.mailbox.counts.send_receive_prohibited
      tag: convert_o365.metrics.mailbox.quota.status.mailbox.counts.send_receive_prohibited
      type: long
      if: ctx.o365?.metrics?.mailbox?.quota?.status?.mailbox?.counts?.send_receive_prohibited != null && ctx.o365?.metrics?.mailbox?.quota?.status?.mailbox?.counts?.send_receive_prohibited != ''
      on_failure:
        - remove:
            field: o365.metrics.mailbox.quota.status.mailbox.counts.send_receive_prohibited
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'  
  - rename:
      field: o365.metrics.mailbox.quota.status.mailbox.counts.send_receive_prohibited
      target_field: o365.metrics.mailbox.quota.status.mailbox.counts.send_receive_prohibited.count
      ignore_missing: true
  - convert:
      field: o365.metrics.mailbox.quota.status.mailbox.counts.under_limit
      tag: convert_o365.metrics.mailbox.quota.status.mailbox.counts.under_limit
      type: long
      if: ctx.o365?.metrics?.mailbox?.quota?.status?.mailbox?.counts?.under_limit != null && ctx.o365?.metrics?.mailbox?.quota?.status?.mailbox?.counts?.under_limit != ''
      on_failure:
        - remove:
            field: o365.metrics.mailbox.quota.status.mailbox.counts.under_limit
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'  
  - rename:
      field: o365.metrics.mailbox.quota.status.mailbox.counts.under_limit
      target_field: o365.metrics.mailbox.quota.status.mailbox.counts.under_limit.count
      ignore_missing: true
  - convert:
      field: o365.metrics.mailbox.quota.status.mailbox.counts.warning_issued
      tag: convert_o365.metrics.mailbox.quota.status.mailbox.counts.warning_issued
      type: long
      if: ctx.o365?.metrics?.mailbox?.quota?.status?.mailbox?.counts?.warning_issued != null && ctx.o365?.metrics?.mailbox?.quota?.status?.mailbox?.counts?.warning_issued != ''
      on_failure:
        - remove:
            field: o365.metrics.mailbox.quota.status.mailbox.counts.warning_issued
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'  
  - rename:
      field: o365.metrics.mailbox.quota.status.mailbox.counts.warning_issued
      target_field: o365.metrics.mailbox.quota.status.mailbox.counts.warning_issued.count
      ignore_missing: true
  - date:
      field: o365.metrics.mailbox.quota.status.mailbox.counts.report_date
      tag: date_o365.metrics.mailbox.quota.status.mailbox.counts.report_date
      if: ctx.o365.metrics?.mailbox?.quota?.status?.mailbox?.counts?.report_date != null && ctx.o365.metrics?.mailbox?.quota?.status?.mailbox?.counts?.report_date != ''
      timezone: UTC
      formats:
        - yyyy-MM-dd
      on_failure:
        - remove:
            field: o365.metrics.mailbox.quota.status.mailbox.counts.report_date
        - append:
            field: error.message
            value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'     
  - rename:
      field: o365.metrics.mailbox.quota.status.mailbox.counts.report_date
      target_field: o365.metrics.mailbox.quota.status.mailbox.counts.report.date
      ignore_missing: true
  - rename:
      field: o365.metrics.mailbox.quota.status.mailbox.counts.report_period
      target_field: o365.metrics.mailbox.quota.status.mailbox.counts.report.period.day
      ignore_missing: true
  - rename:
      field: o365.metrics.mailbox.quota.status.mailbox.counts.report_refresh_date
      target_field: o365.metrics.mailbox.quota.status.mailbox.counts.report.refresh_date
      ignore_missing: true
  - set:
      field: '@timestamp'
      tag: set_timestamp_from_report_date
      copy_from: o365.metrics.mailbox.quota.status.mailbox.counts.report.date
      ignore_empty_value: true
  - fingerprint:
      fields:
        - o365.metrics.mailbox.quota.status.mailbox.counts.report.date
      target_field: _id   
on_failure:
  - set:
      field: error.message
      value: 'Processor {{{_ingest.on_failure_processor_type}}} with tag {{{_ingest.on_failure_processor_tag}}} in pipeline {{{_ingest.on_failure_pipeline}}} failed with message: {{{_ingest.on_failure_message}}}'
  - append:
      field: tags
      value: preserve_original_event
      allow_duplicates: false
  - append:
      field: event.kind
      value: pipeline_error
      allow_duplicates: false
  - append:
      field: event.type
      value: error
      if: ctx.error?.message != null
