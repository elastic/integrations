---
description: Pipeline for renaming object
processors:
  - set:
      field: ecs.version
      value: "8.11.0"
  - json:
      field: active_users
      target_field: o365metrics.active_users   
  - script:
      lang: painless
      description: Replace spaces and dashes in field names under o365metrics.active_users.
      tag: painless_purge_spaces_and_dashes
      if: ctx.o365metrics?.active_users instanceof Map
      source: |
        String underscore(String s) {
          return /[ -]/.matcher(s).replaceAll('_').toLowerCase();
        }
        
        def out = [:];
        for (def item : ctx.o365metrics.active_users.entrySet()) {
          out[underscore(item.getKey())] = item.getValue();
        }
        ctx.o365metrics.active_users = out;
  - remove:
      if: ctx.active_users != null
      field: active_users
      ignore_missing: true                               

on_failure:
  - append:
      field: error.message
      value: "{{{_ingest.on_failure_message}}}"
  - append:
      field: event.kind
      value: pipeline_error
      allow_duplicates: false
