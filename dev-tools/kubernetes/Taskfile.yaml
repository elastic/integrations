# https://taskfile.dev

version: '3'

tasks:
  default:
    - task: kube-state-metrics

  kube-state-metrics:
    summary: Generate manifest for kube-state-metrics
    cmds:
      - kustomize build . > ../../packages/kubernetes/_dev/deploy/k8s/kube-state-metrics.yml
