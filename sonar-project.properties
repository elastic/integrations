sonar.projectKey=elastic_integrations_AYu5LCaKQZlFqhqWIrk_
sonar.host.url=https://sonar.elastic.dev
sonar.sources=packages

sonar.log.level=INFO
sonar.sourceEncoding=UTF-8

# Allow to analyze **/*.py so Sensor Python run is executed
sonar.exclusions=**/*.java,**/*.json,**/Dockerfile,**/*.tf,**/*.go,**/*.yaml,**/*.xml,**/*.md,**/*.hbs,**/*.jsp
# Esclude python files from the coverage, so just the files from the coverage XML files are shown
sonar.coverage.exclusions=**/*.py

# Python
sonar.python.coverage.reportPaths=build/test-coverage/coverage-*.xml

# JacoCo
# sonar.coverage.jacoco.xmlReportPaths=build/test-coverage/jacoco2.xml

# Generic
# sonar.coverageReportPaths=build/test-coverage/generic.xml

# Workaround to run python or flex cobertura changing patterns
# sonar.lang.patterns.py=**/*.yml
# sonar.lang.patterns.flex=**/*.yml
# sonar.lang.patterns.yaml=**/*.yaml

# sonar.coverage.jacoco.xmlReportPaths=build/test-coverage/coverage-*.xml
# sonar.dynamicAnalysis=reuseReports
# sonar.java.coveragePlugin=cobertura
# sonar.cobertura.reportPath=build/test-coverage/coverage_merged.xml
# sonar.cobertura.reportPath=build/test-coverage/coverage-report/coverage-*.xml
# sonar.cobertura.reportPath=build/test-coverage/coverage-*.xml
# sonar.cobertura.reportPath=build/test-coverage/coverage-report/coverage.xml

#TODO remove the line below after tests

# sonar.flex.cobertura.reportPaths=build/test-coverage/coverage-report/coverage.xml
# sonar.flex.cobertura.reportPaths=build/test-coverage/coverage_merged.xml
# sonar.flex.cobertura.reportPaths=build/test-coverage/coverage-*.xml
# sonar.flex.cobertura.reportPaths=build/test-coverage/coverage.xml
# sonar.junit.reportPaths=build/test-results/*.xml
