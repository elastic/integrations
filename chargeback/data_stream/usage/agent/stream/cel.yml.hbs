{{!-- config_version: 2
{{#if vars.indexing_weight}}
fields_under_root: true
keep_null: true
fields:
  _conf:
    indexing_weight: {{vars.indexing_weight}}
    querying_weight: {{vars.querying_weight}}
    storage_weight: {{vars.storage_weight}}
{{/if}} --}}
