config_version: 2
{{#if indexing_weight}}
fields_under_root: true
keep_null: true
fields:
  _conf:
    indexing_weight: {{indexing_weight}}
    querying_weight: {{querying_weight}}
    storage_weight: {{storage_weight}}
{{/if}}
