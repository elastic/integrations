# For testing definitions

service:
  filebeat:
    min: 7.0.0
  elasticsearch: 7.0.0
  kibana: 7.0.0
  # No running version of apache needed for logs
  # Perhaps we need some service here if we want to test syslog
  #apache: