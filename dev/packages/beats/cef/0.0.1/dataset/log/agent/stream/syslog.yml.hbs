protocol.udp:
  host: "{{syslog_host}}:{{syslog_port}}"
tags: {{tags}}
processors:
  - rename:
      fields:
        - {from: "message", to: "event.original"}
  - decode_cef:
      field: event.original
  - community_id: