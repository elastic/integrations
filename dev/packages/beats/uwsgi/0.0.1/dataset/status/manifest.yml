title: uwsgi status metrics
release: experimental
type: metrics
streams:
- input: uwsgi/metrics
  vars:
  - name: period
    type: text
    title: Period
    multi: false
    required: true
    show_user: true
    default: 10s
  title: uwsgi status metrics
  description: Collect uwsgi status metrics
